#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныйПрограммныйИнтерфейс

Процедура ОбработкаПроведенияРегистратора(РегистраторОбъект, Отказ, РежимПроведения) Экспорт
	Очистить();
	
	Если Не ЗначениеЗаполнено(РегистраторОбъект.Дата)
		Или Не ЗначениеЗаполнено(РегистраторОбъект.Страхователь)
		Или Не ЗначениеЗаполнено(РегистраторОбъект.ФизическоеЛицо) Тогда
		Возврат;
	КонецЕсли;
	
	ДанныеОЗаработке = ОбщегоНазначенияБЗК.ЗначениеСвойства(РегистраторОбъект, "ДанныеОЗаработке");
	Если ДанныеОЗаработке = Неопределено Тогда
		// В случае, если в документе нет таблицы ДанныеОЗаработке, таблица передается в свойствах набора.
		ДанныеОЗаработке = ДополнительныеСвойства["ДанныеОЗаработке"];
	КонецЕсли;
	
	Для Каждого СтрокаТаблицы Из ДанныеОЗаработке Цикл
		Если Не ЗначениеЗаполнено(СтрокаТаблицы.РасчетныйГод) Тогда
			Продолжить;
		КонецЕсли;
		Движение = Добавить();
		Движение.Регистратор            = РегистраторОбъект.Ссылка;
		Движение.Период                 = РегистраторОбъект.Дата;
		Движение.ФизическоеЛицо         = РегистраторОбъект.ФизическоеЛицо;
		Движение.Страхователь           = РегистраторОбъект.Страхователь;
		Движение.РасчетныйГод           = СтрокаТаблицы.РасчетныйГод;
		Движение.ЗаработокПоТекущему    = СтрокаТаблицы.ЗаработокПоТекущему;
		Движение.ЗаработокПоТекущемуГПХ = СтрокаТаблицы.ЗаработокПоТекущемуГПХ;
		Движение.СНИЛС                  = РегистраторОбъект.СНИЛС;
	КонецЦикла;
КонецПроцедуры

#КонецОбласти

#Иначе
ВызватьИсключение НСтр("ru = 'Недопустимый вызов объекта на клиенте.'");
#КонецЕсли

