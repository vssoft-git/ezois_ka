
&НаКлиенте
Асинх Процедура ОчиститьРегистр(Команда)
	Если КодВозвратаДиалога.Да = Ждать ВопросАсинх("Очистить регистр?", РежимДиалогаВопрос.ДаНет) Тогда 
		ОчиститьРегистрНаСервере();
		Элементы.Список.Обновить();
	КонецЕсли;
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ОчиститьРегистрНаСервере()
	РегистрыСведений.СправочникиШаблоновОтчетовСтатистики.ОчиститьСправочникиШаблонов(Неопределено);
КонецПроцедуры
