#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаПолученияФормы(ВидФормы, Параметры, ВыбраннаяФорма, ДополнительнаяИнформация, СтандартнаяОбработка)

	Если Параметры.Свойство("ОтборДляРасшифровки") Тогда
		Параметры.Вставить("Период", Параметры.ОтборДляРасшифровки.Период);
		Параметры.Вставить("Показатель", Параметры.ОтборДляРасшифровки.Показатель);
		Параметры.Вставить("ТолькоЗначения", Истина);
		ВыбраннаяФорма = "Справочник.ПравилаРаспределенияРасходов.Форма.ФормаСпискаВручную";
		СтандартнаяОбработка = Ложь;
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#КонецЕсли