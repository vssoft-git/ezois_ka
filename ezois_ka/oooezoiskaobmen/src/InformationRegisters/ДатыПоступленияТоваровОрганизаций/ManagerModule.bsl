#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныеПроцедурыИФункции

#Область ОбновлениеИнформационнойБазы

// см. ОбновлениеИнформационнойБазыБСП.ПриДобавленииОбработчиковОбновления
Процедура ПриДобавленииОбработчиковОбновления(Обработчики) Экспорт

	Обработчик = Обработчики.Добавить();
	Обработчик.Процедура = "РегистрыСведений.ДатыПоступленияТоваровОрганизаций.ОбработатьДанныеДляПереходаНаНовуюВерсию";
	Обработчик.Версия = "11.5.22.123";
	Обработчик.РежимВыполнения = "Отложенно";
	Обработчик.Идентификатор = Новый УникальныйИдентификатор("93c9920d-a0c8-42bc-91d7-e956bc1cd320");
	Обработчик.Многопоточный = Истина;
	Обработчик.ПроцедураЗаполненияДанныхОбновления = "РегистрыСведений.ДатыПоступленияТоваровОрганизаций.ЗарегистрироватьДанныеКОбработкеДляПереходаНаНовуюВерсию";
	Обработчик.ПроцедураПроверки = "ОбновлениеИнформационнойБазы.ДанныеОбновленыНаНовуюВерсиюПрограммы";
	Обработчик.Порядок = Перечисления.ПорядокОбработчиковОбновления.Некритичный;
	
	ТекстКомментария = Новый Массив;
	ТекстКомментария.Добавить(НСтр("ru = 'Актуализирует данные в регистре сведений ""Даты поступления товаров организаций"" по данным документов:'"));
	//++ НЕ УТ
	ТекстКомментария.Добавить(НСтр("ru = '- ""Поступление товаров от хранителя"" с операцией ""Поступление от переработчика"";'"));
	//-- НЕ УТ
	
	//++ Локализация
	ТекстКомментария.Добавить(НСтр("ru = '- ""Корректировка обособленного учета запасов"";'"));
	//-- Локализация
	ТекстКомментария.Добавить(НСтр("ru = '- ""Корректировка назначения товаров"".'"));
	
	Обработчик.Комментарий = СтрСоединить(ТекстКомментария, Символы.ПС);
	
	Читаемые = Новый Массив;
	Читаемые.Добавить(Метаданные.РегистрыНакопления.ТоварыОрганизаций.ПолноеИмя());
	Читаемые.Добавить(Метаданные.РегистрыСведений.ДатыПоступленияТоваровОрганизаций.ПолноеИмя());
	Обработчик.ЧитаемыеОбъекты = СтрСоединить(Читаемые, ",");
	
	Изменяемые = Новый Массив;
	Изменяемые.Добавить(Метаданные.РегистрыСведений.ДатыПоступленияТоваровОрганизаций.ПолноеИмя());
	Обработчик.ИзменяемыеОбъекты = СтрСоединить(Изменяемые, ",");
	
	Блокируемые = Новый Массив;
	Блокируемые.Добавить(Метаданные.РегистрыСведений.ДатыПоступленияТоваровОрганизаций.ПолноеИмя());
	Обработчик.БлокируемыеОбъекты = СтрСоединить(Блокируемые, ",");
	
КонецПроцедуры

// Выполняет регистрацию данных к обновлению.
//
// Параметры:
//	Параметры - см. ОбновлениеИнформационнойБазы.ОсновныеПараметрыОтметкиКОбработке.
//
Процедура ЗарегистрироватьДанныеКОбработкеДляПереходаНаНовуюВерсию(Параметры) Экспорт 
	
	ПолноеИмяРегистра = Метаданные.РегистрыСведений.ДатыПоступленияТоваровОрганизаций.ПолноеИмя();
	
	ПараметрыВыборки = Параметры.ПараметрыВыборки;
	ПараметрыВыборки.ПолныеИменаРегистров = ПолноеИмяРегистра;
	ПараметрыВыборки.СпособВыборки = ОбновлениеИнформационнойБазы.СпособВыборкиИзмеренияНезависимогоРегистраСведений();
	
	ДополнительныеПараметры = ОбновлениеИнформационнойБазы.ДополнительныеПараметрыОтметкиОбработки();
	ДополнительныеПараметры.Вставить("ЭтоНезависимыйРегистрСведений", Истина);
	ДополнительныеПараметры.Вставить("ПолноеИмяРегистра", ПолноеИмяРегистра);
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	СравнениеРегистров.ВидЗапасов КАК ВидЗапасов,
	|	СравнениеРегистров.Номенклатура КАК Номенклатура,
	|	СравнениеРегистров.Характеристика КАК Характеристика,
	|	СравнениеРегистров.Серия КАК Серия,
	|	СравнениеРегистров.Назначение КАК Назначение,
	|	СравнениеРегистров.НомерГТД КАК НомерГТД
	|ИЗ
	|	(ВЫБРАТЬ
	|		ТоварыОрганизаций.ВидЗапасов КАК ВидЗапасов,
	|		ТоварыОрганизаций.АналитикаУчетаНоменклатуры.Номенклатура КАК Номенклатура,
	|		ТоварыОрганизаций.АналитикаУчетаНоменклатуры.Характеристика КАК Характеристика,
	|		ТоварыОрганизаций.АналитикаУчетаНоменклатуры.Серия КАК Серия,
	|		ТоварыОрганизаций.АналитикаУчетаНоменклатуры.Назначение КАК Назначение,
	|		ТоварыОрганизаций.НомерГТД КАК НомерГТД,
	|		NULL КАК ДатаПоступленияТекущая,
	|		ТоварыОрганизаций.Период КАК ДатаПоступления
	|	ИЗ
	|		РегистрНакопления.ТоварыОрганизаций КАК ТоварыОрганизаций
	//++ НЕ УТ
	|			ЛЕВОЕ СОЕДИНЕНИЕ Документ.ПоступлениеТоваровОтХранителя КАК ПоступлениеТоваровОтХранителя
	|			ПО ТоварыОрганизаций.Регистратор = ПоступлениеТоваровОтХранителя.Ссылка
	//-- НЕ УТ
	|	ГДЕ 
	|		ТоварыОрганизаций.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
	|			И (ТоварыОрганизаций.Регистратор ССЫЛКА Документ.КорректировкаНазначенияТоваров
	//++ Локализация
	|					ИЛИ ТоварыОрганизаций.Регистратор ССЫЛКА Документ.КорректировкаОбособленногоУчетаЗапасов
	//-- Локализация

	//++ НЕ УТ
	|					ИЛИ ТоварыОрганизаций.Регистратор ССЫЛКА Документ.ПоступлениеТоваровОтХранителя
	//-- НЕ УТ
	|					)
	//++ НЕ УТ
	|
	// Поступление товаров от хранителя выполняет движения по датам поступления только с хоз. операцией "ПоступлениеОтПереработчика2_5":
	|		И ЕСТЬNULL(
	|			ПоступлениеТоваровОтХранителя.ХозяйственнаяОперация = ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.ПоступлениеОтПереработчика2_5),
	|			ИСТИНА)
	//-- НЕ УТ
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ДатыПоступленияТоваровОрганизаций.ВидЗапасов,
	|		ДатыПоступленияТоваровОрганизаций.Номенклатура,
	|		ДатыПоступленияТоваровОрганизаций.Характеристика,
	|		ДатыПоступленияТоваровОрганизаций.Серия,
	|		ДатыПоступленияТоваровОрганизаций.Назначение,
	|		ДатыПоступленияТоваровОрганизаций.НомерГТД,
	|		ДатыПоступленияТоваровОрганизаций.ДатаПоступления,
	|		NULL
	|	ИЗ
	|		РегистрСведений.ДатыПоступленияТоваровОрганизаций КАК ДатыПоступленияТоваровОрганизаций) КАК СравнениеРегистров
	|
	|СГРУППИРОВАТЬ ПО
	|	СравнениеРегистров.ВидЗапасов,
	|	СравнениеРегистров.Номенклатура,
	|	СравнениеРегистров.Характеристика,
	|	СравнениеРегистров.Серия,
	|	СравнениеРегистров.Назначение,
	|	СравнениеРегистров.НомерГТД
	|
	|ИМЕЮЩИЕ
	|	ЕСТЬNULL(МАКСИМУМ(СравнениеРегистров.ДатаПоступления), ДАТАВРЕМЯ(1, 1, 1)) > ЕСТЬNULL(МАКСИМУМ(СравнениеРегистров.ДатаПоступленияТекущая), ДАТАВРЕМЯ(1, 1, 1))";
	
	ОбновлениеИнформационнойБазы.ОтметитьКОбработке(Параметры, Запрос.Выполнить().Выгрузить(), ДополнительныеПараметры);
	
КонецПроцедуры

// Выполняет обработку зарегистрированных к обновлению данных.
//
// Параметры:
//	Параметры - см. ОбновлениеИнформационнойБазы.ОсновныеПараметрыОтметкиКОбработке.
//
Процедура ОбработатьДанныеДляПереходаНаНовуюВерсию(Параметры) Экспорт
	
	ПолноеИмяРегистра = Метаданные.РегистрыСведений.ДатыПоступленияТоваровОрганизаций.ПолноеИмя();
	
	Если Не ОбновлениеИнформационнойБазы.ЕстьДанныеДляОбработки(Параметры.Очередь, ПолноеИмяРегистра) Тогда
		Параметры.ОбработкаЗавершена = Истина;
		
		Возврат;
	КонецЕсли;
	
	ОбновляемыеДанные = ОбновлениеИнформационнойБазы.ДанныеДляОбновленияВМногопоточномОбработчике(Параметры);
	
	Если ОбновляемыеДанные.Количество() = 0 Тогда
		Параметры.ОбработкаЗавершена = ОбновлениеИнформационнойБазы.ОбработкаДанныхЗавершена(Параметры.Очередь,
																							ПолноеИмяРегистра);
		
		Возврат;
	КонецЕсли;
		
	МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = МенеджерВременныхТаблиц;
	Запрос.УстановитьПараметр("ОбновляемыеДанные", ОбновляемыеДанные);
	
	// Найдем ключи аналитики, попавшие в порцию обработки:
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ОбновляемыеДанные.ВидЗапасов     КАК ВидЗапасов,
	|	ОбновляемыеДанные.Номенклатура   КАК Номенклатура,
	|	ОбновляемыеДанные.Характеристика КАК Характеристика,
	|	ОбновляемыеДанные.Серия          КАК Серия,
	|	ОбновляемыеДанные.Назначение     КАК Назначение,
	|	ОбновляемыеДанные.НомерГТД       КАК НомерГТД
	|ПОМЕСТИТЬ ОбновляемыеДанные
	|ИЗ
	|	&ОбновляемыеДанные КАК ОбновляемыеДанные
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ВидЗапасов,
	|	Номенклатура,
	|	Характеристика,
	|	Серия,
	|	Назначение,
	|	НомерГТД
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ОбновляемыеДанные.ВидЗапасов     КАК ВидЗапасов,
	|	ОбновляемыеДанные.Номенклатура   КАК Номенклатура,
	|	ОбновляемыеДанные.Характеристика КАК Характеристика,
	|	ОбновляемыеДанные.Серия          КАК Серия,
	|	ОбновляемыеДанные.Назначение     КАК Назначение,
	|	ОбновляемыеДанные.НомерГТД       КАК НомерГТД,
	|	АналитикаУчетаНоменклатуры.КлючАналитики         КАК АналитикаУчетаНоменклатуры
	|ПОМЕСТИТЬ ОбновляемыеКлючи
	|ИЗ
	|	ОбновляемыеДанные КАК ОбновляемыеДанные
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.АналитикаУчетаНоменклатуры КАК АналитикаУчетаНоменклатуры
	|		ПО ОбновляемыеДанные.Номенклатура      = АналитикаУчетаНоменклатуры.Номенклатура
	|			И ОбновляемыеДанные.Характеристика = АналитикаУчетаНоменклатуры.Характеристика
	|			И ОбновляемыеДанные.Серия          = АналитикаУчетаНоменклатуры.Серия
	|			И ОбновляемыеДанные.Назначение     = АналитикаУчетаНоменклатуры.Назначение
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	АналитикаУчетаНоменклатуры
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ 
	|	ОбновляемыеКлючи.ВидЗапасов                 КАК ВидЗапасов,
	|	ОбновляемыеКлючи.Номенклатура               КАК Номенклатура,
	|	ОбновляемыеКлючи.Характеристика             КАК Характеристика,
	|	ОбновляемыеКлючи.Серия                      КАК Серия,
	|	ОбновляемыеКлючи.Назначение                 КАК Назначение,
	|	ОбновляемыеКлючи.НомерГТД                   КАК НомерГТД,
	|	ОбновляемыеКлючи.АналитикаУчетаНоменклатуры КАК АналитикаУчетаНоменклатуры
	|ИЗ ОбновляемыеКлючи";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	НачатьТранзакцию();
	Попытка
		
		Блокировка = Новый БлокировкаДанных;
		
		ЭлементБлокировки = Блокировка.Добавить(ПолноеИмяРегистра);
		ЭлементБлокировки.ИсточникДанных = ОбновляемыеДанные;
		ЭлементБлокировки.ИспользоватьИзИсточникаДанных("ВидЗапасов",     "ВидЗапасов");
		ЭлементБлокировки.ИспользоватьИзИсточникаДанных("Номенклатура",   "Номенклатура");
		ЭлементБлокировки.ИспользоватьИзИсточникаДанных("Характеристика", "Характеристика");
		ЭлементБлокировки.ИспользоватьИзИсточникаДанных("Серия",          "Серия");
		ЭлементБлокировки.ИспользоватьИзИсточникаДанных("Назначение",     "Назначение");
		ЭлементБлокировки.ИспользоватьИзИсточникаДанных("НомерГТД",       "НомерГТД");
		
		ЭлементБлокировки = Блокировка.Добавить(Метаданные.РегистрыНакопления.ТоварыОрганизаций.ПолноеИмя());
		ЭлементБлокировки.Режим = РежимБлокировкиДанных.Разделяемый;
		ЭлементБлокировки.ИсточникДанных = РезультатЗапроса;
		ЭлементБлокировки.ИспользоватьИзИсточникаДанных("АналитикаУчетаНоменклатуры", "АналитикаУчетаНоменклатуры");
		ЭлементБлокировки.ИспользоватьИзИсточникаДанных("ВидЗапасов",                 "ВидЗапасов");
		ЭлементБлокировки.ИспользоватьИзИсточникаДанных("НомерГТД",                   "НомерГТД");
		
		Блокировка.Заблокировать();
		
		Запрос.Текст = 
		"ВЫБРАТЬ
		|	СравнениеРегистров.ВидЗапасов                       КАК ВидЗапасов,
		|	СравнениеРегистров.Номенклатура                     КАК Номенклатура,
		|	СравнениеРегистров.Характеристика                   КАК Характеристика,
		|	СравнениеРегистров.Серия                            КАК Серия,
		|	СравнениеРегистров.Назначение                       КАК Назначение,
		|	СравнениеРегистров.НомерГТД                         КАК НомерГТД,
		|	МАКСИМУМ(СравнениеРегистров.ДатаПоступления)        КАК ДатаПоступления,
		|	МАКСИМУМ(СравнениеРегистров.ТекущаяДатаПоступления) КАК ТекущаяДатаПоступления
		|ИЗ
		|	(ВЫБРАТЬ
		|		ОбновляемыеКлючи.ВидЗапасов     КАК ВидЗапасов,
		|		ОбновляемыеКлючи.Номенклатура   КАК Номенклатура,
		|		ОбновляемыеКлючи.Характеристика КАК Характеристика,
		|		ОбновляемыеКлючи.Серия          КАК Серия,
		|		ОбновляемыеКлючи.Назначение     КАК Назначение,
		|		ОбновляемыеКлючи.НомерГТД       КАК НомерГТД,
		|		ТоварыОрганизаций.Период        КАК ДатаПоступления,
		|		NULL                            КАК ТекущаяДатаПоступления
		|	ИЗ
		|		РегистрНакопления.ТоварыОрганизаций КАК ТоварыОрганизаций
		|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ ОбновляемыеКлючи КАК ОбновляемыеКлючи
		|			ПО ТоварыОрганизаций.АналитикаУчетаНоменклатуры = ОбновляемыеКлючи.АналитикаУчетаНоменклатуры
		|				И ТоварыОрганизаций.ВидЗапасов              = ОбновляемыеКлючи.ВидЗапасов
		|				И ТоварыОрганизаций.НомерГТД                = ОбновляемыеКлючи.НомерГТД
		|				И ТоварыОрганизаций.ВидДвижения            = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|				И (ТоварыОрганизаций.Регистратор ССЫЛКА Документ.КорректировкаНазначенияТоваров
		//++ Локализация
		|					ИЛИ ТоварыОрганизаций.Регистратор ССЫЛКА Документ.КорректировкаОбособленногоУчетаЗапасов
		//-- Локализация
	
		//++ НЕ УТ
		|					ИЛИ ТоварыОрганизаций.Регистратор ССЫЛКА Документ.ПоступлениеТоваровОтХранителя
		//-- НЕ УТ
		|					)
		//++ НЕ УТ
		|			ЛЕВОЕ СОЕДИНЕНИЕ Документ.ПоступлениеТоваровОтХранителя КАК ПоступлениеТоваровОтХранителя
		|			ПО ТоварыОрганизаций.Регистратор = ПоступлениеТоваровОтХранителя.Ссылка
		|	ГДЕ
		// Поступление товаров от хранителя выполняет движения по датам поступления только с хоз. операцией "ПоступлениеОтПереработчика2_5":
		|		ЕСТЬNULL(
		|			ПоступлениеТоваровОтХранителя.ХозяйственнаяОперация = ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.ПоступлениеОтПереработчика2_5),
		|			ИСТИНА)
		//-- НЕ УТ
		|	
		|	ОБЪЕДИНИТЬ ВСЕ
		|	
		|	ВЫБРАТЬ
		|		ОбновляемыеКлючи.ВидЗапасов                       КАК ВидЗапасов,
		|		ОбновляемыеКлючи.Номенклатура                     КАК Номенклатура,
		|		ОбновляемыеКлючи.Характеристика                   КАК Характеристика,
		|		ОбновляемыеКлючи.Серия                            КАК Серия,
		|		ОбновляемыеКлючи.Назначение                       КАК Назначение,
		|		ОбновляемыеКлючи.НомерГТД                         КАК НомерГТД,
		|		NULL                                              КАК ДатаПоступления,
		|		ДатыПоступленияТоваровОрганизаций.ДатаПоступления КАК ТекущаяДатаПоступления
		|	ИЗ
		|		РегистрСведений.ДатыПоступленияТоваровОрганизаций КАК ДатыПоступленияТоваровОрганизаций
		|			ЛЕВОЕ СОЕДИНЕНИЕ ОбновляемыеКлючи КАК ОбновляемыеКлючи
		|			ПО ДатыПоступленияТоваровОрганизаций.ВидЗапасов        = ОбновляемыеКлючи.ВидЗапасов
		|				И ДатыПоступленияТоваровОрганизаций.Номенклатура   = ОбновляемыеКлючи.Номенклатура
		|				И ДатыПоступленияТоваровОрганизаций.Характеристика = ОбновляемыеКлючи.Характеристика
		|				И ДатыПоступленияТоваровОрганизаций.Серия          = ОбновляемыеКлючи.Серия
		|				И ДатыПоступленияТоваровОрганизаций.Назначение     = ОбновляемыеКлючи.Назначение
		|				И ДатыПоступленияТоваровОрганизаций.НомерГТД       = ОбновляемыеКлючи.НомерГТД) КАК СравнениеРегистров
		|
		|СГРУППИРОВАТЬ ПО
		|	СравнениеРегистров.ВидЗапасов,
		|	СравнениеРегистров.Номенклатура,
		|	СравнениеРегистров.Характеристика,
		|	СравнениеРегистров.Серия,
		|	СравнениеРегистров.Назначение,
		|	СравнениеРегистров.НомерГТД
		|
		|ИМЕЮЩИЕ
		|	ЕСТЬNULL(МАКСИМУМ(СравнениеРегистров.ДатаПоступления), ДАТАВРЕМЯ(1, 1, 1)) > ЕСТЬNULL(МАКСИМУМ(СравнениеРегистров.ТекущаяДатаПоступления), ДАТАВРЕМЯ(1, 1, 1))";
		
		РезультатыЗапроса = Запрос.Выполнить();
		
		НаборЗаписей = СоздатьНаборЗаписей();
		НаборЗаписей.Загрузить(РезультатыЗапроса.Выгрузить());
		НаборЗаписей.Записать(РежимЗамещения.Слияние);
		
		ДополнительныеПараметры = ОбновлениеИнформационнойБазы.ДополнительныеПараметрыОтметкиОбработки();
		ДополнительныеПараметры.ЭтоНезависимыйРегистрСведений = Истина;
		ДополнительныеПараметры.ПолноеИмяРегистра = ПолноеИмяРегистра;
		ОбновлениеИнформационнойБазы.ОтметитьВыполнениеОбработки(ОбновляемыеДанные, ДополнительныеПараметры);
		
		ЗафиксироватьТранзакцию();
		
	Исключение
		
		ОтменитьТранзакцию();
			
		ТекстСообщения = НСтр("ru = 'Не удалось обновить данные в регистре %1 по причине: %2'");
		ТекстСообщения = СтрШаблон(ТекстСообщения,
			ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()),
			ПолноеИмяРегистра);
			
		ЗаписьЖурналаРегистрации(ОбновлениеИнформационнойБазы.СобытиеЖурналаРегистрации(),
			УровеньЖурналаРегистрации.Предупреждение,
			Метаданные.РегистрыСведений.ДатыПоступленияТоваровОрганизаций,
			,
			ТекстСообщения);
		
	КонецПопытки;
	
	Параметры.ОбработкаЗавершена = ОбновлениеИнформационнойБазы.ОбработкаДанныхЗавершена(Параметры.Очередь,
																						ПолноеИмяРегистра);
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#КонецЕсли
