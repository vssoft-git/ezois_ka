#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
#Область ОбработчикиСобытий
	
Процедура ПриЗаписи(Отказ, Замещение)
	
	Если ОбменДанными.Загрузка Тогда
		
		Возврат;
		
	КонецЕсли;
	
	Для Каждого ЗаписьРегистра Из ЭтотОбъект Цикл
		
		ПараметрыСобытия = УниверсальныйОбменСБанками.ПараметрыСобытияДокументооборота();
		ПараметрыСобытия.Предмет = ЗаписьРегистра.Предмет;
		ПараметрыСобытия.Организация = ЗаписьРегистра.Организация;
		ПараметрыСобытия.Сервис = Перечисления.СервисыОбменаСБанками.ФинансоваяОтчетность;
		ПараметрыСобытия.Состояние = ЗаписьРегистра.СостояниеСдачиОтчетности;
		ПараметрыСобытия.Статус = ЗаписьРегистра.Статус;
		ПараметрыСобытия.ЕстьКритическиеОшибки = ЗаписьРегистра.ЕстьКритическиеОшибки;
		ПараметрыСобытия.Просмотрено = НЕ ЗаписьРегистра.Непрочтенное;
		ПараметрыСобытия.Скрыт = ЗаписьРегистра.Скрыт;
		ПараметрыСобытия.Банк = ЗаписьРегистра.Банк;
		
		УниверсальныйОбменСБанками.ЗаписатьНовоеСобытиеДокументооборота(ПараметрыСобытия);
		
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли


