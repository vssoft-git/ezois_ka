#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ, Замещение)

	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого Запись Из ЭтотОбъект Цикл
		Если Не ЗначениеЗаполнено(Запись.ДокументОтгрузки)
				И Запись.ДокументОтгрузки <> Неопределено Тогда
			Запись.ДокументОтгрузки = Неопределено;
		КонецЕсли;
	КонецЦикла;
	
	Если Количество() > 0
			И Не ЗначениеЗаполнено(Отбор.ДокументОтгрузки.Значение)
			И Отбор.ДокументОтгрузки.Значение <> Неопределено Тогда
		Отбор.ДокументОтгрузки.Значение = Неопределено;
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#КонецЕсли
