#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если ОбщегоНазначения.ПодсистемаСуществует("РегламентированнаяОтчетность.Интерфейс85") Тогда
		МодульРегламентированнаяОтчетностьИнтерфейс85 =
			ОбщегоНазначения.ОбщийМодуль("РегламентированнаяОтчетностьИнтерфейс85");
		
		МодульРегламентированнаяОтчетностьИнтерфейс85.УстановитьНазначениеДействияКоманды(Команды.Сохранить);
		МодульРегламентированнаяОтчетностьИнтерфейс85.УстановитьНазначениеДействияКоманды(Команды.Отмена);
		
		МодульРегламентированнаяОтчетностьИнтерфейс85.УстановитьВидФлажкаВТаблице(Элементы.СоставОтчетаПоСчетамВключен);
		
	КонецЕсли;
	
	Для Каждого СтрокаСчета Из Параметры.НастройкиСчетов Цикл
		СтрокаСчетаФормы = СоставОтчетаПоСчетам.Добавить();
		ЗаполнитьЗначенияСвойств(СтрокаСчетаФормы, СтрокаСчета);
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура УстановитьФлажки(Команда)
	
	УстановитьПометкуСтрокТаблицы(Истина, "Включен");
	
КонецПроцедуры

&НаКлиенте
Процедура СнятьФлажки(Команда)
	
	УстановитьПометкуСтрокТаблицы(Ложь, "Включен");
	
КонецПроцедуры

&НаКлиенте
Процедура Сохранить(Команда)
	
	РезультатВыбора = Новый Массив;
	
	Для Каждого СтрокаТаблицы Из СоставОтчетаПоСчетам Цикл
		РезультатВыбора.Добавить(Новый Структура("Счет, Включен", СтрокаТаблицы.Счет, СтрокаТаблицы.Включен));
	КонецЦикла;
	
	Закрыть(РезультатВыбора);
	
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	
	Закрыть();
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура УстановитьПометкуСтрокТаблицы(ЗначениеКолонки, ИмяКолонки)
	
	Для Каждого СтрокаТаблицы Из СоставОтчетаПоСчетам Цикл
		СтрокаТаблицы[ИмяКолонки] = ЗначениеКолонки;
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти
