#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ, Замещение)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;

	ОбновлениеИнформационнойБазы.ПроверитьОбъектОбработан(ЭтотОбъект);
	
	// Корректировка зависимых параметров
	Для каждого стр Из ЭтотОбъект Цикл
		стр.ВедетсяУчетПостоянныхИВременныхРазниц =
			НалогНаПрибыльБухгалтерскийУчетКлиентСервер.ВедетсяУчетПостоянныхИВременныхРазниц(стр.ВариантУчетаОтложенногоНалога);
	КонецЦикла;

КонецПроцедуры

#КонецОбласти

#КонецЕсли
