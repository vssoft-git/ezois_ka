#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если ЗначениеЗаполнено(Параметры.ВидыОтпусков) Тогда
		ВидыОтпусков.ЗагрузитьЗначения(Параметры.ВидыОтпусков);
	КонецЕсли;
	
	Если Параметры.КомпенсацияОтпуска Тогда
		Заголовок = НСтр("ru = 'Компенсация отпуска'");
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
	ВидОтпуска = Элементы.ВидыОтпусков.ТекущиеДанные.Значение;	
	Закрыть(ВидОтпуска);
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	Закрыть();
КонецПроцедуры

#КонецОбласти
