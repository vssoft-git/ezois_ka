
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Не ПустаяСтрока(Параметры.Макет) Тогда
		
		Для Каждого ТекущийЭлемент Из Элементы Цикл
			
			Если ТекущийЭлемент.Вид <> ВидДекорацииФормы.Картинка Тогда
				Продолжить;
			КонецЕсли;
			
			Если ТекущийЭлемент.Имя = Параметры.Макет Тогда
				ТекущийЭлемент.Видимость = Истина;
				Заголовок = ТекущийЭлемент.Заголовок;
			Иначе
				ТекущийЭлемент.Видимость = Ложь;
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	КлючСохраненияПоложенияОкна = Параметры.Макет;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура БланкРасчетаТранспортногоНалогаНажатие(Элемент)

	Закрыть();

КонецПроцедуры

&НаКлиенте
Процедура БланкРасчетаЗемельногоНалогаНажатие(Элемент)
		
	Закрыть();

КонецПроцедуры

&НаКлиенте
Процедура БланкРасчетаИмущественныхНалоговНажатие(Элемент)
	
	Закрыть();

КонецПроцедуры

#КонецОбласти
