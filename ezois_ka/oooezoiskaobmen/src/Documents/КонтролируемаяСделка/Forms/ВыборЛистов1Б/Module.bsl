#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	Для Каждого ДанныеЛиста Из Параметры.Листы1Б Цикл
		ЗаполнитьЗначенияСвойств(Листы1Б.Добавить(), ДанныеЛиста);
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
	Если ПроверитьЗаполнение() Тогда
		Модифицированность = Ложь;
		ВыбранныеЛисты = Новый Соответствие;
		Для Каждого СтрокаЛиста1Б Из Листы1Б Цикл
			ВыбранныеЛисты.Вставить(СтрокаЛиста1Б.ИдентификаторЛиста1Б, СтрокаЛиста1Б.Выбрано);
		КонецЦикла;
		Закрыть(ВыбранныеЛисты);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	
	Модифицированность = Ложь;
	Закрыть();
	
КонецПроцедуры

#КонецОбласти
