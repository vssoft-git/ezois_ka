#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;	
	
	ЗаполнитьЗначенияСвойств(ЭтотОбъект, Параметры.РеквизитыПечати);
	РеквизитыИсточника = Параметры.РеквизитыПечати;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура СохранитьИЗакрыть(Команда)
	
	РеквизитыПечати = Новый Структура();
	Для Каждого Реквизит Из РеквизитыИсточника Цикл
		
		Если Реквизит.Значение <> ЭтотОбъект[Реквизит.Ключ] Тогда
			РеквизитыПечати.Вставить(Реквизит.Ключ, ЭтотОбъект[Реквизит.Ключ]);
		КонецЕсли;
		
	КонецЦикла;
	ЭтотОбъект.Закрыть(РеквизитыПечати);
	
КонецПроцедуры

#КонецОбласти
