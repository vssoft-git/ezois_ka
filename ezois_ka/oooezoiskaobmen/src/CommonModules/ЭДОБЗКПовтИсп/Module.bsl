#Область СлужебныйПрограммныйИнтерфейс

Функция НастройкиПечатныхФорм() Экспорт
	
	Возврат РегистрыСведений.НастройкиПечатныхФормБЗК.Настройки();
	
КонецФункции

Функция СсылкаНаКодДокументаКадровогоМероприятия(КодКадровогоДокумента) Экспорт
	
	Возврат Справочники.КодыДокументовКадровыхМероприятий.НайтиПоКоду(КодКадровогоДокумента);
	
КонецФункции

Функция КодКадровогоДокументаПоУмолчанию() Экспорт
	
	Возврат ОбщегоНазначения.ПредопределенныйЭлемент("Справочник.КодыДокументовКадровыхМероприятий.ИныеДокументы");
	
КонецФункции

Функция СвойстваКодаКадровогоДокумента(Знач КодКадровогоДокумента) Экспорт
	Если Не ЗначениеЗаполнено(КодКадровогоДокумента) Тогда
		Возврат Неопределено;
	КонецЕсли;
	Возврат ОбщегоНазначения.ЗначенияРеквизитовОбъекта(КодКадровогоДокумента, "Код,НаименованиеПолное");
КонецФункции

Функция ДанныеМЧДПодписи(МЧД, ИдентификаторХранилища) Экспорт
	
	Возврат ЭлектроннаяПодписьЭДОБЗК.ДанныеМЧДПодписи(МЧД, Новый УникальныйИдентификатор(ИдентификаторХранилища));
	
КонецФункции

Функция ДокументЭДОБЗКЭлектронногоДокумента(ЭлектронныйДокумент) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ЭлектронныйДокумент", ЭлектронныйДокумент);
	
	Запрос.Текст =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	ДокументЭДОБЗК.Ссылка КАК Ссылка
		|ИЗ
		|	Документ.ДокументЭДОБЗК КАК ДокументЭДОБЗК
		|ГДЕ
		|	ДокументЭДОБЗК.ЭлектронныйДокумент = &ЭлектронныйДокумент
		|
		|УПОРЯДОЧИТЬ ПО
		|	ДокументЭДОБЗК.ПометкаУдаления";
	
	УстановитьПривилегированныйРежим(Истина);
	Выборка = Запрос.Выполнить().Выбрать();
	УстановитьПривилегированныйРежим(Ложь);
	
	Если Выборка.Следующий() Тогда
		Возврат Выборка.Ссылка;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

Функция КодКадровогоДокументаВидаДокумента(ВидДокумента) Экспорт
	
	РеквизитыВида = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ВидДокумента, "ДокументСвязанныйСРаботой,КодКадровогоДокумента");
	Если РеквизитыВида.ДокументСвязанныйСРаботой Тогда
		Возврат РеквизитыВида.КодКадровогоДокумента;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

#КонецОбласти