#Область СлужебныеПроцедурыИФункции

Функция ФормаОткрытияТекущейЗадачиБизнесПроцесса(БизнесПроцесс) Экспорт
	Возврат БизнесПроцессыЗаявокСотрудников.ФормаОткрытияТекущейЗадачиБизнесПроцесса(БизнесПроцесс);	
КонецФункции

Функция ФайлЗаявки(БизнесПроцесс) Экспорт
	Возврат БизнесПроцессыЗаявокСотрудников.ФайлЗаявки(БизнесПроцесс);	
КонецФункции

Процедура ВыполненоВыполнить(Заявка, СостояниеЗапроса, Исполнитель) Экспорт
	БизнесПроцессыЗаявокСотрудниковФормы.ВыполнитьБизнесПроцессЗаявки(Заявка, СостояниеЗапроса, Исполнитель);	
КонецПроцедуры

Процедура Отказать(Заявка, СостояниеЗапроса, Исполнитель) Экспорт
	БизнесПроцессыЗаявокСотрудниковФормы.ОтказатьБизнесПроцессЗаявки(Заявка, СостояниеЗапроса, Исполнитель);
КонецПроцедуры

Функция ЭтоЗаявкаСотрудника(Ссылка) Экспорт
	Возврат ТипЗнч(Ссылка) = Тип("БизнесПроцессСсылка.ЗаявкаСотрудникаОтпуск")
		 	Или ТипЗнч(Ссылка) = Тип("БизнесПроцессСсылка.ЗаявкаСотрудникаСправка2НДФЛ")
			Или ТипЗнч(Ссылка) = Тип("БизнесПроцессСсылка.ЗаявкаСотрудникаИзменитьЛичныеДанные")
		 	Или ТипЗнч(Ссылка) = Тип("БизнесПроцессСсылка.ЗаявкаСотрудникаОтсутствиеПоБолезни")
			Или ТипЗнч(Ссылка) = Тип("БизнесПроцессСсылка.ЗаявкаСотрудникаНалоговыйВычет")
		 	Или ТипЗнч(Ссылка) = БизнесПроцессыЗаявокСотрудниковВнутренний.ТипЗаявкаСотрудникаДобровольныеСтраховыеВзносы() 
		 	Или ТипЗнч(Ссылка) = БизнесПроцессыЗаявокСотрудниковВнутренний.ТипЗаявкаСотрудникаОтсутствие()
			Или ТипЗнч(Ссылка) = БизнесПроцессыЗаявокСотрудниковВнутренний.ТипЗаявкаСотрудникаСправкаОстаткиОтпусков()
		 	Или ТипЗнч(Ссылка) = БизнесПроцессыЗаявокСотрудниковВнутренний.ТипЗаявкаСотрудникаСправкаСМестаРаботы();
КонецФункции

Функция ОбращенияСотрудникаСоШтампами(ЭлектронныйДокумент) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	ДанныеФайлаПечатнойФормы = РегистрыСведений.ПодписанныеПечатныеФормы.ДанныеФайлаПечатнойФормы(ЭлектронныйДокумент);
	УстановитьПривилегированныйРежим(Ложь);
	Если ДанныеФайлаПечатнойФормы <> Неопределено Тогда
		Оригинал = ДанныеФайлаПечатнойФормы.Оригинал;
		Возврат ЭДОБЗК.ПечатнаяФормаСоШтампами(ЭлектронныйДокумент, Оригинал, Истина, Истина);
	КонецЕсли;
	Возврат Неопределено;
	
КонецФункции

#КонецОбласти