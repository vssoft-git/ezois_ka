#Область СлужебныйПрограммныйИнтерфейс

Функция ИмяРеквизита(Идентификатор) Экспорт
	
	Возврат "ДополнительныйПоказатель_" + Идентификатор;
	
КонецФункции

Функция ИмяРеквизитаФайла(Идентификатор) Экспорт
	
	Возврат "ДополнительныйФайл_" + Идентификатор;
	
КонецФункции

Функция ИмяАдресаЗначений(ИмяЭлемента) Экспорт
	
	Возврат ИмяЭлемента + "_АдресЗначений";
	
КонецФункции

Функция ИмяПредставления(ИмяЭлемента) Экспорт
	
	Возврат ИмяЭлемента + "_Представление";
	
КонецФункции

Функция ИмяОчистки(ИмяЭлемента) Экспорт
	
	Возврат ИмяЭлемента + "_Очистка";
	
КонецФункции

Функция ИмяПереключателя(ИмяЭлемента) Экспорт
	
	Возврат ИмяЭлемента + "_Переключатель";
	
КонецФункции

Функция ИмяСвязанногоЭлементаПереключателя(ИмяПереключателя) Экспорт
	
	Возврат СтрЗаменить(ИмяПереключателя, "_Переключатель", "");
	
КонецФункции

Функция ИмяЗаглушки(ИмяЭлемента) Экспорт
	
	Возврат ИмяЭлемента + "_Заглушка";
	
КонецФункции

Функция ИмяОписания(ИмяЭлемента) Экспорт
	
	Возврат ИмяЭлемента + "_Описание";
	
КонецФункции

Функция ИмяДействия(ИмяЭлемента) Экспорт
	
	Возврат ИмяЭлемента + "_Действие";
	
КонецФункции

Функция ИмяШаблона(ИмяЭлемента) Экспорт
	
	Возврат ИмяЭлемента + "_Шаблон";
	
КонецФункции

Функция РазделительЭлементовСтроки() Экспорт
	
	Возврат "#!#";
	
КонецФункции

Функция НаименованиеПравилаФинОтчетности(Получатель, ПредставлениеКомплекта, Организация) Экспорт
	
	Возврат СтрШаблон("%1 - %2 (%3)", Получатель, ПредставлениеКомплекта, Организация);
	
КонецФункции

Функция СсылкаНаИнструкциюПоУстановкеЭлектроннойПодписи() Экспорт
	
	Возврат "https://its.1c.ru/bmk/metod81_csp_setup";
	
КонецФункции

Функция СсылкаНаИнструкцииПоРаботеСПрограммамиЭлектроннойПодписи() Экспорт
	
	Возврат "https://its.1c.ru/bmk/metod81_crypt";
	
КонецФункции

Функция СсылкаОписание1СПодпись() Экспорт
	
	Возврат "https://portal.1c.ru/applications/31";
	
КонецФункции

// Конструктор для параметров выбора присоединенного файла в заявках на кредит и фин.отчетности.
//
// Возвращаемое значение:
//   Структура   - параметры для открытия общей формы ФормаВыборПрисоединенногоФайла.
//
Функция ПараметрыДиалогаВыбораФайла() Экспорт
	
	ПараметрыДиалога = Новый Структура;
	ПараметрыДиалога.Вставить("Заголовок",          НСтр("ru = 'присоединенный файл'"));
	ПараметрыДиалога.Вставить("МножественныйВыбор", Ложь);
	
	// По-умолчанию используем ограничение, установленное БСП.
	ПараметрыДиалога.Вставить("МаксимальныйРазмер",  0);  // максимально допустимый размер файла в Кб
	
	ПараметрыДиалога.Вставить("Расширение", "." + ЗаполнениеФинОтчетностиВБанкиКлиентСервер.РасширениеТабличногоДокумента());
	ПараметрыДиалога.Вставить("ФильтрДиалогаВыбора",
		НСтр("ru = 'Книга Excel (*.xls; *.xlsx)|*.xls; *.xlsx|Документ Adobe PDF (*.pdf)|*.pdf|
				|Документ Word (*.doc; *.docx)|*.doc; *.docx|Текст в формате RTF (*.rtf)|*.rtf|
				|Формат JPEG (*.jpg; *.jpeg)|*.jpg; *.jpeg|Формат PNG (*.png)|*.png|
				|Текстовый документ (*.txt)|*.txt|Веб-страница (*.htm;*.html)|*.htm;*.html|
				|Архив ZIP (*.zip)|*.zip|Все файлы (*.*)|*.*'"));
	
	Возврат ПараметрыДиалога;
	
КонецФункции

// Конструктор для параметров формы с описанием файла.
//
// Возвращаемое значение:
//   Структура   - параметры для открытия общей формы ФормаОписаниеОтчета.
//
Функция ПараметрыФормыОписания() Экспорт

	Результат = Новый Структура();
	Результат.Вставить("Сервис",            ПредопределенноеЗначение("Перечисление.СервисыОбменаСБанками.ПустаяСсылка"));
	Результат.Вставить("Банки",             Новый СписокЗначений); // список из СправочникСсылка.БанкиУниверсальногоОбмена
	Результат.Вставить("Характеристика");
	Результат.Вставить("Заголовок",         "");
	Результат.Вставить("ПодробноеОписание", "");
	Результат.Вставить("ИмяФайлаШаблона",   "");
	
	Возврат Результат;

КонецФункции

// Возвращает текст для представления пустого юридического адреса в форме заявки.
// Редактирование самого адреса в форме заявки не поддерживается, его необходимо заполнять
// в форме организации, т.к. при его изменении возможно изменение регистрации в ФНС, ПФР и т.п.
//
// Возвращаемое значение:
//	Строка - представление пустого адреса.
Функция ТекстПустогоЮрАдресаВВидеГиперссылки() Экспорт

	Возврат НСтр("ru = 'Заполнить в форме организации'");

КонецФункции

Функция СсылкаНаИнструкциюГдеПолучитьЭлектроннуюПодпись() Экспорт
	
	Возврат "https://its.1c.ru/bmk/esig_uc";
	
КонецФункции

#КонецОбласти