
#Область СлужебныйПрограммныйИнтерфейс

Процедура УдалитьУчтенныеПриРасчетеНДФЛДокументы(ТаблицаНДФЛ, ТаблицаДокументов, ФизическиеЛица) Экспорт 
	
	Для Каждого ФизическоеЛицо Из ФизическиеЛица Цикл
		Отбор = Новый Структура("ФизическоеЛицо", ФизическоеЛицо);
		
		СтрокиНДФЛ = ТаблицаНДФЛ.НайтиСтроки(Отбор);
		Если СтрокиНДФЛ.Количество() > 0 Тогда 
			Возврат;
		КонецЕсли;
		
		СтрокиКУдалению = ТаблицаДокументов.НайтиСтроки(Отбор);
		Для Каждого СтрокаКУдалению Из СтрокиКУдалению Цикл 
			ТаблицаДокументов.Удалить(СтрокаКУдалению);
		КонецЦикла;
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти
