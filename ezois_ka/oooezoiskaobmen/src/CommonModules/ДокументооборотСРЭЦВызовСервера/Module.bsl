////////////////////////////////////////////////////////////////////////////////
// Подсистема "Документооборот с РЭЦ".
// 
////////////////////////////////////////////////////////////////////////////////

#Область ПрограммныйИнтерфейс

Функция АвторизацияВСервисеРЭЦ(СоединениеHTTP = Неопределено, ПараметрыДляШага = Неопределено, ЭтоРегистрация = Ложь) Экспорт
	
	Возврат ДокументооборотСРЭЦ.АвторизацияВСервисеРЭЦ(СоединениеHTTP, ПараметрыДляШага, ЭтоРегистрация);

КонецФункции

Функция ОтправитьОтчет(КонтекстОтправки, Знач СоединениеHTTP = Неопределено) Экспорт
	
	Возврат ДокументооборотСРЭЦ.ОтправитьОтчет(КонтекстОтправки, СоединениеHTTP);

КонецФункции

Функция ПолучитьСтатусОтчета(Ссылка, ПараметрыАутентификации) Экспорт
	
	Возврат ДокументооборотСРЭЦ.ПолучитьСтатусОтчета(Ссылка, ПараметрыАутентификации);
	
КонецФункции

Функция ПолучитьНастройки(Организация) Экспорт
	
	Возврат ДокументооборотСРЭЦ.ПолучитьНастройки(Организация);
	
КонецФункции

Функция СохранитьНастройки(Организация, Сертификат) Экспорт
	
	Возврат ДокументооборотСРЭЦ.СохранитьНастройки(Организация, Сертификат);
	
КонецФункции

Функция ПолучитьНеЗавершенныеОтправки(Организация) Экспорт
	
	Возврат ДокументооборотСРЭЦ.ПолучитьНеЗавершенныеОтправки(Организация);
	
КонецФункции

Функция ПолучитьПоследнююОтправкуОтчета(ОтчетСсылка) Экспорт
	
	Возврат ДокументооборотСРЭЦ.ПолучитьПоследнююОтправкуОтчета(ОтчетСсылка);
	
КонецФункции

Функция ВыгрузитьФайлыВложений(ФайлСсылка, МассивВыгрузки, ИменаФайлов) Экспорт
	
	Возврат ДокументооборотСРЭЦ.ВыгрузитьФайлыВложений(ФайлСсылка, МассивВыгрузки, ИменаФайлов);
	
КонецФункции

Функция ЗначениеРеквизитаОбъекта(Ссылка, ИмяРеквизита, ВыбратьРазрешенные) Экспорт

	Возврат ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Ссылка, ИмяРеквизита, ВыбратьРазрешенные);
	
КонецФункции

Функция ПроверитьВозможностьРедактированияОтчета(ОтчетСсылка) Экспорт
	
	Возврат ДокументооборотСРЭЦ.ПроверитьВозможностьРедактированияОтчета(ОтчетСсылка);
	
КонецФункции   

Процедура ОбработатьМассивДанныхВыгрузки(ДокументСсылка, МассивДанныхВыгрузки) Экспорт
	
	ДокументооборотСРЭЦ.ОбработатьМассивДанныхВыгрузки(ДокументСсылка, МассивДанныхВыгрузки);
	
КонецПроцедуры

Процедура АннулироватьПредыдущиеОтправки(СсылкаНаОтчет) Экспорт
	
	ДокументооборотСРЭЦ.АннулироватьПредыдущиеОтправки(СсылкаНаОтчет);
		
КонецПроцедуры

Функция ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(ИсходноеЗначение, ЗначениеДляОтправки = Неопределено, СоединениеHTTP = Неопределено) Экспорт
	
	Если СоединениеHTTP = NULL Тогда
		Возврат ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(ИсходноеЗначение, ЗначениеДляОтправки);
	Иначе
		Возврат ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(ИсходноеЗначение, ЗначениеДляОтправки, СоединениеHTTP);
	КонецЕсли;
	
КонецФункции

#КонецОбласти