#Область ПрограммныйИнтерфейс

// Управление видимостью ЕГДС.
// 
// Параметры:
//  Форма    - ФормаКлиентскогоПриложения
//  Элементы - Строка - Имена элементов формы, разделенные запятыми.
Процедура УправлениеВидимостьюЕГДС(Форма, Элементы) Экспорт
	
	ОбесценениеВНАДоступно = ОбесценениеВНАВызовСервера.ИспользоватьОбесценениеВНА();
	
	ИменаЭлементов = СтрРазделить(Элементы, ",");
	
	Для Каждого Элемент Из ИменаЭлементов Цикл
		ИмяЭлемента = СокрЛП(Элемент);
		
		Если Не ПустаяСтрока(ИмяЭлемента) Тогда
			Форма.Элементы[ИмяЭлемента].Видимость = ОбесценениеВНАДоступно;
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

