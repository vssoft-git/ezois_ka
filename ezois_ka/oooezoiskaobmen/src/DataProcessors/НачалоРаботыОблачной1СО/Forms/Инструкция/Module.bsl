
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Макет = Обработки.НачалоРаботыОблачной1СО.ПолучитьМакет(Параметры.ИмяМакета);
	Вложения = Новый Структура();
	ПолеИнструкции.УстановитьHTML(Макет.ПолучитьТекст(), Вложения);
	
КонецПроцедуры
