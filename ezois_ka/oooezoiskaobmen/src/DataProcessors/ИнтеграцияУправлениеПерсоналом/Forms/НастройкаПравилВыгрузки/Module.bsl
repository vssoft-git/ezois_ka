#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Приложение = Параметры.Приложение;
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
				"ОтключитьНепубликуемыхСотрудников", "Видимость", Ложь);
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
				"ОтключитьНепубликуемыхСотрудников", "Видимость", Ложь);
	
	Если Приложение = Перечисления.ПриложенияДляИнтеграции.КабинетСотрудника Тогда
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
				"ОтключитьНепубликуемыхСотрудников", "Видимость", Не Параметры.РежимПодключения);
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
				"ОтключитьНепубликуемыхСотрудников", "Видимость",
				КабинетСотрудника.ИспользоватьФормат503());
			КонецЕсли;
			
	УстановитьИменаЭлементовФормы();
	
	ПоказыватьНовыеПозиции = Истина;
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
				"ГруппаНастройкаНедоступна", "Видимость", Ложь);
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
				"КоманднаяПанель", "Доступность", Ложь);
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
		Элементы,
		"ДеревоНастроекУсловиеДопускаКЭДОБЗК",
		"Видимость",
		Приложение = Перечисления.ПриложенияДляИнтеграции.КабинетСотрудника);
	
	Элементы.СтраницыФормы.ТекущаяСтраница = Элементы.СтраницаДлительнаяОперация;
	НачатьПолучениеДанных();
	
КонецПроцедуры 

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(ЭтотОбъект);
		ПараметрыОжидания.ВыводитьОкноОжидания = Ложь;
		ДлительныеОперацииКлиент.ОжидатьЗавершение(
			АктивноеЗадание.ДлительнаяОперация,
			Новый ОписаниеОповещения("ПриЗавершенииПолученияДанных", ЭтотОбъект),
			ПараметрыОжидания);
	
КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, ТекстПредупреждения, СтандартнаяОбработка)
	
	Оповещение = Новый ОписаниеОповещения("ЗаписатьНастройкиПриЗакрытииФормы", ЭтаФорма);
	ОбщегоНазначенияКлиент.ПоказатьПодтверждениеЗакрытияФормы(Оповещение, Отказ, ЗавершениеРаботы);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ПоказыватьНовыеПозицииПриИзменении(Элемент)
	
	Если ПоказыватьНовыеПозиции Тогда
		ДобавитьПозиции(НастройкиПубликации, НеУтвержденныеПозиции);
	Иначе
		УдалитьПозиции(НастройкиПубликации, НеУтвержденныеПозиции, "НеУтверждена");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказыватьЗакрытыеПозицииПриИзменении(Элемент)
	
	Если ПоказыватьЗакрытыеПозиции Тогда
		ДобавитьПозиции(НастройкиПубликации, ЗакрытыеПозиции);
	Иначе
		УдалитьПозиции(НастройкиПубликации, ЗакрытыеПозиции, "Закрыта");
	КонецЕсли;
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
			"ДеревоНастроекДатаЗакрытия", "Видимость", ИспользоватьШтатноеРасписание И ПоказыватьЗакрытыеПозиции);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыДеревоНастроек

&НаКлиенте
Процедура ДеревоНастроекПриНачалеРедактирования(Элемент, НоваяСтрока, Копирование)
	Если Не НоваяСтрока Тогда
		Если Элементы.ДеревоНастроек.ТекущиеДанные = Неопределено Тогда
			УсловиеДопускаКЭДОБЗКДоИзменения = ПредопределенноеЗначение("Перечисление.УсловияДопускаСотрудниковКЭДОБЗК.ПриПроверкеСкановПодписанногоСогласияНаЭДОБЗК");
		Иначе
			УсловиеДопускаКЭДОБЗКДоИзменения = Элементы.ДеревоНастроек.ТекущиеДанные.УсловиеДопускаКЭДОБЗК
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ДеревоНастроекВыгружатьПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.ДеревоНастроек.ТекущиеДанные;
	ПриИзмененииПометкиВыгружать(ТекущиеДанные);
	
	Если ИспользоватьШтатноеРасписание Тогда
		
		ЕстьЗакрытые = Ложь;
		ЕстьНеУтвержденные = Ложь;
		ИдентификаторСтрокиИсходногоДерева = СоответствиеСтрок[ТекущиеДанные.ИдентификаторСтроки];
		СтрокаИсходногоДерева = ИсходныеНастройкиПубликации.НайтиПоИдентификатору(ИдентификаторСтрокиИсходногоДерева);
		СтрокаИсходногоДерева.Выгружать = ТекущиеДанные.Выгружать;
		ПриИзмененииПометкиВыгружать(СтрокаИсходногоДерева, ЕстьЗакрытые, ЕстьНеУтвержденные);
		Если ЕстьЗакрытые Тогда
			ОбновитьПометкиПодчиненногоДерева(ЗакрытыеПозиции.ПолучитьЭлементы());
		КонецЕсли;
		Если ЕстьНеУтвержденные Тогда
			ОбновитьПометкиПодчиненногоДерева(НеУтвержденныеПозиции.ПолучитьЭлементы());
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ДеревоНастроекУсловиеДопускаКЭДОБЗКОчистка(Элемент, СтандартнаяОбработка)
	Если Элементы.ДеревоНастроек.ТекущиеДанные <> Неопределено Тогда
		СтандартнаяОбработка = Ложь;
		Родитель = Элементы.ДеревоНастроек.ТекущиеДанные.ПолучитьРодителя();
		Если Родитель = Неопределено Тогда
			Элементы.ДеревоНастроек.ТекущиеДанные.УсловиеДопускаКЭДОБЗК = ПредопределенноеЗначение("Перечисление.УсловияДопускаСотрудниковКЭДОБЗК.ПриПроверкеСкановПодписанногоСогласияНаЭДОБЗК");
		Иначе
			Элементы.ДеревоНастроек.ТекущиеДанные.УсловиеДопускаКЭДОБЗК = Родитель.УсловиеДопускаКЭДОБЗК;
		КонецЕсли;
		ДеревоНастроекУсловиеДопускаКЭДОБЗКПриИзмененииНаКлиенте(Элементы.ДеревоНастроек.ТекущиеДанные);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ДеревоНастроекУсловиеДопускаКЭДОБЗКПриИзменении(Элемент)
	Если Элементы.ДеревоНастроек.ТекущиеДанные <> Неопределено Тогда
		ДеревоНастроекУсловиеДопускаКЭДОБЗКПриИзмененииНаКлиенте(Элементы.ДеревоНастроек.ТекущиеДанные);
	КонецЕсли;
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ЗаписатьИЗакрыть(Команда)
	
	СохранитьНастройки(Истина);
		
КонецПроцедуры

&НаКлиенте
Процедура Записать(Команда)
	
	СохранитьНастройки();
	
КонецПроцедуры

&НаКлиенте
Процедура ПубликуемыеФизическиеЛица(Команда)
	
	Если Модифицированность Тогда
		ПоказатьВопрос(
			Новый ОписаниеОповещения("ПубликуемыеФизическиеЛицаЗавершение", ЭтаФорма),
			НСтр("ru = 'Для продолжения необходимо сохранить данные. Сохранить и продолжить?'"),
			РежимДиалогаВопрос.ДаНет);
	Иначе
		ОткрытьСписокВыгружаемыхФизическихЛиц();
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура КомандаОтметитьВсе(Команда)
	
	УстановитьОтметкиДереваНастроек(НастройкиПубликации.ПолучитьЭлементы(), 1);
	
	Если ИспользоватьШтатноеРасписание Тогда
		УстановитьОтметкиДереваНастроек(ИсходныеНастройкиПубликации.ПолучитьЭлементы(), 1);
		УстановитьОтметкиДереваНастроек(ЗакрытыеПозиции.ПолучитьЭлементы(), 1);
		УстановитьОтметкиДереваНастроек(НеУтвержденныеПозиции.ПолучитьЭлементы(), 1);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура КомандаСнятьВсе(Команда)
	
	УстановитьОтметкиДереваНастроек(НастройкиПубликации.ПолучитьЭлементы(), 0);
	
	Если ИспользоватьШтатноеРасписание Тогда
		УстановитьОтметкиДереваНастроек(ИсходныеНастройкиПубликации.ПолучитьЭлементы(), 0);
		УстановитьОтметкиДереваНастроек(ЗакрытыеПозиции.ПолучитьЭлементы(), 0);
		УстановитьОтметкиДереваНастроек(НеУтвержденныеПозиции.ПолучитьЭлементы(), 0);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтключитьКабинетыНепубликуемыхСотрудников(Команда)
	
	ОповещениеОЗавершении = Новый ОписаниеОповещения("ОтключитьКабинетыНепубликуемыхСотрудниковПродолжение", ЭтаФорма);
	ПоказатьВопрос(
		ОповещениеОЗавершении,
		НСтр("ru = 'Кабинеты сотрудников, которые не публикуются в сервисе (например, уволены), будут переведены в неактивное состояние.'"),
		ИнтеграцияУправлениеПерсоналомКлиента.ОписаниеКнопокВопроса());
		
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура НачатьПолучениеДанных()

	ПараметрыВыполненияВФоне = ДлительныеОперации.ПараметрыВыполненияВФоне(УникальныйИдентификатор);
	ПараметрыВыполненияВФоне.ОжидатьЗавершение = 0;
	
	ИмяМетода = "Обработки.ИнтеграцияУправлениеПерсоналом.ЗаполнитьДеревоНастроекПубликации";
	ПараметрыМетода = Новый Структура("Приложение", Приложение);
	РезультатВыполнения = ДлительныеОперации.ВыполнитьВФоне(ИмяМетода, ПараметрыМетода, ПараметрыВыполненияВФоне);
	
	НовоеЗадание = Новый Структура("ДлительнаяОперация");
	НовоеЗадание.ДлительнаяОперация = РезультатВыполнения;
	АктивноеЗадание = Новый ФиксированнаяСтруктура(НовоеЗадание);

КонецПроцедуры

&НаКлиенте
Процедура ПриЗавершенииПолученияДанных(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если Результат.Статус = "Ошибка" Тогда
		ТекстСообщения = НСтр("ru = 'При выполнении фонового задания возникла ошибка:'") + Символы.ПС + Результат.КраткоеПредставлениеОшибки;
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);
	КонецЕсли;
	
	ПриЗавершенииПолученияДанныхНаСервере(Результат.АдресРезультата);
	
КонецПроцедуры

&НаСервере
Процедура ПриЗавершенииПолученияДанныхНаСервере(АдресРезультата)
	
	Результат = ПолучитьИзВременногоХранилища(АдресРезультата);
	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ИспользоватьШтатноеРасписание = Результат.ИспользоватьШтатноеРасписание;
	РезультатЗапроса = Результат.РезультатЗапроса;
	ДанныеДоступны = Результат.ДанныеДоступны;
	УсловияДопуска = Результат.УсловияДопуска;
	
	Если Не ДанныеДоступны Тогда
		// У пользователя отсутствую права на все организации и/или все подразделения,
		// настройка публикации недоступна.
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
				"ФормаЗаписатьИЗакрыть", "Доступность", Ложь);
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
				"ФормаЗаписать", "Доступность", Ложь);
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
				"ДеревоНастроек", "Доступность", Ложь);
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
				"ГруппаНастройкаНедоступна", "Видимость", Истина);
	Иначе
		ЗаполнитьНастройки(РезультатЗапроса, УсловияДопуска);
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
				"ГруппаНастройкаНедоступна", "Видимость", Ложь);
	КонецЕсли;
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
				"ГруппаФлажковГоризонтальная", "Видимость", ИспользоватьШтатноеРасписание);
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
				"ДеревоНастроекДатаУтверждения", "Видимость", ИспользоватьШтатноеРасписание);
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
				"ДеревоНастроекДатаЗакрытия", "Видимость", ИспользоватьШтатноеРасписание И ПоказыватьЗакрытыеПозиции);
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
				"КоманднаяПанель", "Доступность", Истина);
	
	Элементы.СтраницыФормы.ТекущаяСтраница = Элементы.СтраницаНастройки;
	
	Если ИспользоватьШтатноеРасписание Тогда
		УстановитьУсловноеОформлениеФормы();
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьНастройки(РезультатЗапроса, УсловияДопуска)

	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ЗакрытыеПозиции", Новый Массив);
	ДополнительныеПараметры.Вставить("НеУтвержденныеПозиции", Новый Массив);
	ДополнительныеПараметры.Вставить("ИдентификаторСтроки", 1);
	
	Если Не РезультатЗапроса.Пустой() Тогда
		
		ДополнительныеПараметры.Вставить("УсловияДопуска", УсловияДопуска);
		
		ИсходноеДерево = РеквизитФормыВЗначение("НастройкиПубликации");
		
		ВыборкаЗапроса = РезультатЗапроса.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкамСИерархией);
		
		НомерСтроки = 1;
		Пока ВыборкаЗапроса.Следующий() Цикл
			
			НоваяСтрока = ИсходноеДерево.Строки.Добавить();
			НоваяСтрока.ИдентификаторСтроки = ДополнительныеПараметры.ИдентификаторСтроки;
			ДополнительныеПараметры.ИдентификаторСтроки = ДополнительныеПараметры.ИдентификаторСтроки + 1;
			НоваяСтрока.НомерСтроки = НомерСтроки;
			ЗаполнитьЗначенияСвойств(НоваяСтрока, ВыборкаЗапроса);
			НоваяСтрока.Наименование = Строка(НоваяСтрока.Организация);
			
			НоваяСтрока.УсловиеДопускаКЭДОБЗК = УсловияДопуска.Получить(НоваяСтрока.Организация);
			
			ДочерняяВыборка = ВыборкаЗапроса.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкамСИерархией);
			
			Пометка = 0;
			ДочерниеЗакрыты = Истина;
			ДочерниеНеУтверждены = Истина;
			ЗаполнитьСтрокиДерева(НоваяСтрока, ДочерняяВыборка, Пометка, ДочерниеЗакрыты, ДочерниеНеУтверждены, ДополнительныеПараметры);
			НоваяСтрока.Выгружать = Пометка;
			НоваяСтрока.Закрыта = ДочерниеЗакрыты;
			НоваяСтрока.НеУтверждена = ДочерниеНеУтверждены;
			НоваяСтрока.НомерКартинки = 0;
			
			НомерСтроки = НомерСтроки + 1;
			
		КонецЦикла;
		
		Если ИспользоватьШтатноеРасписание Тогда
			
			ЗначениеВРеквизитФормы(ИсходноеДерево, "ИсходныеНастройкиПубликации");
			КоллекцияЭлементов = ИсходныеНастройкиПубликации.ПолучитьЭлементы();
			СтрокиДерева = Новый Соответствие;
			Для каждого ЭлементКоллекции Из КоллекцияЭлементов Цикл
				СтрокиДерева.Вставить(ЭлементКоллекции.ИдентификаторСтроки, ЭлементКоллекции.ПолучитьИдентификатор());
				ЗаполнитьСоответствиеСтрок(СтрокиДерева, ЭлементКоллекции.ПолучитьЭлементы());
			КонецЦикла;
			СоответствиеСтрок = Новый ФиксированноеСоответствие(СтрокиДерева);
			
			ДеревоПозиций = РеквизитФормыВЗначение("ЗакрытыеПозиции");
			ЗаполнитьДеревоПоДетальнымСтрокам(ИсходноеДерево, ДеревоПозиций, ДополнительныеПараметры.ЗакрытыеПозиции);
			ЗначениеВРеквизитФормы(ДеревоПозиций, "ЗакрытыеПозиции");
			
			ДеревоПозиций = РеквизитФормыВЗначение("НеУтвержденныеПозиции");
			ЗаполнитьДеревоПоДетальнымСтрокам(ИсходноеДерево, ДеревоПозиций, ДополнительныеПараметры.НеУтвержденныеПозиции);
			ЗначениеВРеквизитФормы(ДеревоПозиций, "НеУтвержденныеПозиции");
			
			ЗначениеВРеквизитФормы(ИсходноеДерево, "НастройкиПубликации");
			
			УдалитьПозиции(НастройкиПубликации, ЗакрытыеПозиции, "Закрыта");
			
		Иначе
			ЗначениеВРеквизитФормы(ИсходноеДерево, "НастройкиПубликации");
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьСтрокиДерева(СтрокаДерева, ИерархическаяВыборка, Пометка, Закрыты, НеУтверждены, ДополнительныеПараметры)
	
	БылиПометки = Ложь;
	БылиПустые = Ложь;
	ВсеЗакрыты = Истина;
	ВсеНеУтверждены = Истина;
	
	НомерСтроки = 1;
	Пока ИерархическаяВыборка.Следующий() Цикл
		
		УсловияДопуска = ДополнительныеПараметры.УсловияДопуска;
		
		НоваяСтрока = СтрокаДерева.Строки.Добавить();
		ЗаполнитьЗначенияСвойств(НоваяСтрока, ИерархическаяВыборка);
		НоваяСтрока.ИдентификаторСтроки = ДополнительныеПараметры.ИдентификаторСтроки;
		ДополнительныеПараметры.ИдентификаторСтроки = ДополнительныеПараметры.ИдентификаторСтроки + 1;
		НоваяСтрока.НомерСтроки = НомерСтроки;
		
		Если ИерархическаяВыборка.ТипЗаписи() = ТипЗаписиЗапроса.ДетальнаяЗапись Тогда
			
			НоваяСтрока.Наименование = Строка(НоваяСтрока.ПозицияШтатногоРасписания);
			НоваяСтрока.ДетальнаяЗапись = Истина;
			НоваяСтрока.НомерКартинки = 3;
			НоваяСтрока.УсловиеДопускаКЭДОБЗК = УсловияДопуска.Получить(НоваяСтрока.ПозицияШтатногоРасписания);
			
			ВсеЗакрыты = ВсеЗакрыты И НоваяСтрока.Закрыта;
			ВсеНеУтверждены = ВсеНеУтверждены И НоваяСтрока.НеУтверждена;
			
			Если НоваяСтрока.Закрыта Тогда
				ДополнительныеПараметры.ЗакрытыеПозиции.Добавить(НоваяСтрока);
			Иначе
				БылиПометки = БылиПометки Или (НоваяСтрока.Выгружать = 1);
				БылиПустые  = БылиПустые Или (НоваяСтрока.Выгружать = 0);
			КонецЕсли;
			
			Если НоваяСтрока.НеУтверждена Тогда
				ДополнительныеПараметры.НеУтвержденныеПозиции.Добавить(НоваяСтрока);
			КонецЕсли;
			
		Иначе
			
			НоваяСтрока.Наименование = Строка(НоваяСтрока.Подразделение);
			НоваяСтрока.НомерКартинки = 0;
			
			Если ИерархическаяВыборка.ТипЗаписи() = ТипЗаписиЗапроса.ИтогПоИерархии Тогда
				ДочерняяВыборка = ИерархическаяВыборка.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкамСИерархией, ИерархическаяВыборка.Группировка());
			Иначе
				ДочерняяВыборка = ИерархическаяВыборка.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкамСИерархией);
			КонецЕсли;
			
			ПометкаДочерних = 0;
			ДочерниеЗакрыты = Истина;
			ДочерниеНеУтверждены = Истина;
			ЗаполнитьСтрокиДерева(НоваяСтрока, ДочерняяВыборка, ПометкаДочерних, ДочерниеЗакрыты, ДочерниеНеУтверждены, ДополнительныеПараметры);
			НоваяСтрока.Выгружать = ПометкаДочерних;
			НоваяСтрока.Закрыта = ДочерниеЗакрыты;
			НоваяСтрока.НеУтверждена = ДочерниеНеУтверждены;
			
			ВсеЗакрыты = ВсеЗакрыты И ДочерниеЗакрыты;
			ВсеНеУтверждены = ВсеНеУтверждены И ДочерниеНеУтверждены;
			
			Если НЕ ДочерниеЗакрыты Тогда
				Если ПометкаДочерних = 2 Тогда
					БылиПометки = Истина;
					БылиПустые  = Истина;
				Иначе
					БылиПометки = БылиПометки Или (ПометкаДочерних = 1);
					БылиПустые  = БылиПустые Или (ПометкаДочерних = 0);
				КонецЕсли;
			КонецЕсли;
			
			НоваяСтрока.УсловиеДопускаКЭДОБЗК = УсловияДопуска.Получить(НоваяСтрока.Подразделение);
			
		КонецЕсли;
		
		НомерСтроки = НомерСтроки + 1;
		
	КонецЦикла;
	
	Если БылиПометки И БылиПустые Тогда
		Пометка = 2;
	ИначеЕсли БылиПометки Тогда
		Пометка = 1;
	Иначе
		Пометка = 0;
	КонецЕсли;
	
	Закрыты = ВсеЗакрыты;
	НеУтверждены = ВсеНеУтверждены;
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьДеревоПоДетальнымСтрокам(ИсходноеДерево, ДеревоПозиций, ДетальныеСтроки)
	
	РодителиСтрок = Новый Соответствие;
	Для каждого СтрокаДерева Из ДетальныеСтроки Цикл
		Родитель = РодителиСтрок[СтрокаДерева.Родитель];
		Если Родитель = Неопределено Тогда
			Родитель = РодительДереваНастроек(ДеревоПозиций.Строки, РодителиСтрок, СтрокаДерева.Родитель);
		КонецЕсли;
		ЗаполнитьЗначенияСвойств(Родитель.Строки.Добавить(), СтрокаДерева);
	КонецЦикла;

КонецПроцедуры

&НаСервере
Функция РодительДереваНастроек(СтрокиДерева, РодителиСтрок, РодительИсходногоДерева)

	Если РодительИсходногоДерева.Родитель = Неопределено Тогда
		РодительДереваНастроек = РодителиСтрок[РодительИсходногоДерева];
		Если РодительДереваНастроек = Неопределено Тогда
			РодительДереваНастроек = СтрокиДерева.Добавить();
			ЗаполнитьЗначенияСвойств(РодительДереваНастроек, РодительИсходногоДерева);
			РодителиСтрок.Вставить(РодительИсходногоДерева, РодительДереваНастроек);
		КонецЕсли;
		Возврат РодительДереваНастроек;
	Иначе
		Родитель = РодителиСтрок[РодительИсходногоДерева];
		Если Родитель = Неопределено Тогда
			РодительДереваНастроек = РодительДереваНастроек(СтрокиДерева, РодителиСтрок, РодительИсходногоДерева.Родитель);
			Родитель = РодительДереваНастроек.Строки.Добавить();
			ЗаполнитьЗначенияСвойств(Родитель, РодительИсходногоДерева);
			РодителиСтрок.Вставить(РодительИсходногоДерева, Родитель);
		КонецЕсли;
		Возврат Родитель;
	КонецЕсли;

КонецФункции

&НаСервере
Процедура ЗаполнитьСоответствиеСтрок(СтрокиДерева, КоллекцияЭлементов)

	Для каждого ЭлементКоллекции Из КоллекцияЭлементов Цикл
		СтрокиДерева.Вставить(ЭлементКоллекции.ИдентификаторСтроки, ЭлементКоллекции.ПолучитьИдентификатор());
		ЗаполнитьСоответствиеСтрок(СтрокиДерева, ЭлементКоллекции.ПолучитьЭлементы());
	КонецЦикла;

КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Процедура УдалитьПозиции(ДеревоНастройкиПубликации, УдаляемыеПозиции, ИмяПоляУдалить)
	
	ДеревоНастроек = ДеревоНастройкиПубликации.ПолучитьЭлементы();
	
	Для каждого УдаляемаяСтрокаДерева Из УдаляемыеПозиции.ПолучитьЭлементы() Цикл
		
		ИндексУдаляемойСтроки = -1;
		Для каждого СтрокаДереваНастроек Из ДеревоНастроек Цикл
		
			Если СтрокаДереваНастроек.НомерСтроки <> УдаляемаяСтрокаДерева.НомерСтроки Тогда
				Продолжить;
			Иначе
				
				Если УдаляемаяСтрокаДерева[ИмяПоляУдалить] Тогда
					ИндексУдаляемойСтроки = ДеревоНастроек.Индекс(СтрокаДереваНастроек);
				Иначе
					УдалитьПозиции(СтрокаДереваНастроек, УдаляемаяСтрокаДерева, ИмяПоляУдалить);
					Если СтрокаДереваНастроек.ПолучитьЭлементы().Количество() = 0 Тогда
						ИндексУдаляемойСтроки = ДеревоНастроек.Индекс(СтрокаДереваНастроек);
					КонецЕсли;
				КонецЕсли;
				
				Прервать;
				
			КонецЕсли;
		
		КонецЦикла;
		
		Если ИндексУдаляемойСтроки >= 0 Тогда
			ДеревоНастроек.Удалить(ИндексУдаляемойСтроки);
		КонецЕсли;
			
	КонецЦикла;
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Процедура ДобавитьПозиции(ДеревоНастройкиПубликации, ДобавляемыеПозиции)
	
	ДеревоНастроек = ДеревоНастройкиПубликации.ПолучитьЭлементы();
	
	Для каждого ДобавляемаяСтрокаДерева Из ДобавляемыеПозиции.ПолучитьЭлементы() Цикл
		
		НомерДобавляемойСтроки = ДобавляемаяСтрокаДерева.НомерСтроки;
		ИндексПредыдущейСтроки = -1;
		ДобавлятьСтроку = Истина;
		
		Если ДеревоНастроек.Количество() = 0 Тогда
			
			НоваяСтрока = ДеревоНастроек.Вставить(0);
			ЗаполнитьЗначенияСвойств(НоваяСтрока, ДобавляемаяСтрокаДерева);
			ДобавитьПозиции(НоваяСтрока, ДобавляемаяСтрокаДерева);
			
		Иначе
			
			Для каждого СтрокаДереваНастроек Из ДеревоНастроек Цикл
				
				Если СтрокаДереваНастроек.НомерСтроки < НомерДобавляемойСтроки Тогда
					ИндексПредыдущейСтроки = ДеревоНастроек.Индекс(СтрокаДереваНастроек);
				ИначеЕсли СтрокаДереваНастроек.НомерСтроки = НомерДобавляемойСтроки Тогда
					ДобавитьПозиции(СтрокаДереваНастроек, ДобавляемаяСтрокаДерева);
					ДобавлятьСтроку = Ложь;
				ИначеЕсли СтрокаДереваНастроек.НомерСтроки > НомерДобавляемойСтроки Тогда
					Прервать;
				КонецЕсли;
				
			КонецЦикла;
			
			Если ДобавлятьСтроку Тогда
				НоваяСтрока = ДеревоНастроек.Вставить(ИндексПредыдущейСтроки +1);
				ЗаполнитьЗначенияСвойств(НоваяСтрока, ДобавляемаяСтрокаДерева);
				ДобавитьПозиции(НоваяСтрока, ДобавляемаяСтрокаДерева);
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьОтметкиДереваНастроек(СтрокиДерева, ЗначениеОтметки)

	Для Каждого СтрокаДерева Из СтрокиДерева Цикл
		СтрокаДерева.Выгружать = ЗначениеОтметки;
		УстановитьОтметкиДереваНастроек(СтрокаДерева.ПолучитьЭлементы(), ЗначениеОтметки);
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриИзмененииПометкиВыгружать(ТекущиеДанные, ЕстьЗакрытые = Ложь, ЕстьНеУтвержденные = Ложь)
	
	Если ТекущиеДанные.Выгружать = 2 Тогда
		ТекущиеДанные.Выгружать = 0;
	КонецЕсли;
	Если ИспользоватьШтатноеРасписание И ТекущиеДанные.Закрыта Тогда
		ЕстьЗакрытые = Истина;
		Возврат;
	КонецЕсли;
	ЕстьНеУтвержденные = ТекущиеДанные.НеУтверждена;
	УстановитьПометкуПодчиненных(ТекущиеДанные, ЕстьЗакрытые, ЕстьНеУтвержденные);
	УстановитьПометкуРодителя(ТекущиеДанные.ПолучитьРодителя(), ЕстьЗакрытые, ЕстьНеУтвержденные);
	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьПометкуРодителя(Родитель, ЕстьЗакрытые, ЕстьНеУтвержденные)
	
	Если Родитель = Неопределено Тогда
		Возврат;
	КонецЕсли;
	БылиПометки = Ложь;
	БылиПустые = Ложь;
	ДочерниеЭлементыРодителя = Родитель.ПолучитьЭлементы();
	Для Каждого ДочернийЭлемент Из ДочерниеЭлементыРодителя Цикл
		ЕстьЗакрытые = ЕстьЗакрытые Или ДочернийЭлемент.Закрыта;
		ЕстьНеУтвержденные = ЕстьНеУтвержденные Или ДочернийЭлемент.НеУтверждена;
		Если ИспользоватьШтатноеРасписание И ДочернийЭлемент.Закрыта Тогда
			Продолжить;
		КонецЕсли;
		Если ДочернийЭлемент.Выгружать = 2 Тогда
			БылиПометки = Истина;
			БылиПустые  = Истина;
		Иначе
			БылиПометки = БылиПометки Или ДочернийЭлемент.Выгружать;
			БылиПустые  = БылиПустые Или Не ДочернийЭлемент.Выгружать;
		КонецЕсли;
	КонецЦикла;
	Если БылиПометки И Не БылиПустые Тогда
		Родитель.Выгружать = 1;
	ИначеЕсли БылиПометки И БылиПустые Тогда
		Родитель.Выгружать = 2;
	Иначе
		Родитель.Выгружать = 0;
	КонецЕсли;
	Прародитель = Родитель.ПолучитьРодителя();
	Если Прародитель <> Неопределено Тогда
		УстановитьПометкуРодителя(Прародитель, ЕстьЗакрытые, ЕстьНеУтвержденные);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьПометкуПодчиненных(Родитель, ЕстьЗакрытые, ЕстьНеУтвержденные)
	
	Пометка = Родитель.Выгружать;
	
	ДочерниеЭлементы = Родитель.ПолучитьЭлементы();
	Для Каждого ДочернийЭлемент Из ДочерниеЭлементы Цикл
		ЕстьЗакрытые = ЕстьЗакрытые Или ДочернийЭлемент.Закрыта;
		ЕстьНеУтвержденные = ЕстьНеУтвержденные Или ДочернийЭлемент.НеУтверждена;
		Если ИспользоватьШтатноеРасписание И ДочернийЭлемент.Закрыта И Пометка = 1 Тогда
			Продолжить;
		КонецЕсли;
		ДочернийЭлемент.Выгружать = Родитель.Выгружать;
		УстановитьПометкуПодчиненных(ДочернийЭлемент, ЕстьЗакрытые, ЕстьНеУтвержденные);
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьПометкиПодчиненногоДерева(КоллекцияЭлементов)
	
	Для каждого ЭллементКоллекции Из КоллекцияЭлементов Цикл
		ИдентификаторСтрокиИсходногоДерева = СоответствиеСтрок[ЭллементКоллекции.ИдентификаторСтроки];
		СтрокаИсходногоДерева = ИсходныеНастройкиПубликации.НайтиПоИдентификатору(ИдентификаторСтрокиИсходногоДерева);
		ЗаполнитьЗначенияСвойств(ЭллементКоллекции, СтрокаИсходногоДерева);
		ОбновитьПометкиПодчиненногоДерева(ЭллементКоллекции.ПолучитьЭлементы())
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПубликуемыеФизическиеЛицаЗавершение(Результат, Параметры) Экспорт
	
	Если Результат = КодВозвратаДиалога.Да Тогда
		СохранитьНастройки(,,Истина);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьСписокВыгружаемыхФизическихЛиц()
	
	ПараметрыОткрытия = Новый Структура("Приложение", Приложение);
	ОткрытьФорму("Обработка.ИнтеграцияУправлениеПерсоналом.Форма.ПубликуемыеФизическиеЛица",ПараметрыОткрытия,
				ЭтаФорма,,,,,РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
КонецПроцедуры

&НаКлиенте
Функция ЗаписатьНастройкиПриЗакрытииФормы(Результат, ДополнительныеПараметры) Экспорт
	
	СохранитьНастройки(Истина);

КонецФункции

&НаКлиенте
Процедура СохранитьНастройки(ЗакрытьФорму = Ложь, ОбновитьПубликацию = Ложь, ПоказатьСписок = Ложь)
	
	ОчиститьСообщения();
	
	ДлительнаяОперация = СохранитьНастройкиНаСервере();
	ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(ЭтотОбъект);
	ПараметрыОжидания.ВыводитьОкноОжидания = Истина;
	ПараметрыОжидания.ОповещениеПользователя.Показать = Истина;
	ПараметрыОжидания.ОповещениеПользователя.Текст = НСтр("ru = 'Настройки сохранены.'");
	ДополнительныеПараметры = Новый Структура("ЗакрытьФорму,ПоказатьСписок", ЗакрытьФорму, ОбновитьПубликацию, ПоказатьСписок);
	ОповещениеОЗавершении = Новый ОписаниеОповещения("СохранитьНастройкиЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, ОповещениеОЗавершении, ПараметрыОжидания);
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьНастройкиЗавершение(Результат, ДополнительныеПараметры) Экспорт

	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если Результат.Статус = "Выполнено" Тогда
		Модифицированность = Ложь;
		Если Приложение = ПредопределенноеЗначение("Перечисление.ПриложенияДляИнтеграции.УправлениеПерсоналом") Тогда
			Оповестить("ИзменениеПравилВыгрузкиИнтеграцияУправлениеПерсоналом");
		Иначе
			Оповестить("ИзменениеПравилВыгрузкиИнтеграцияУправлениеПерсоналом");
		КонецЕсли;
		Если ДополнительныеПараметры.ЗакрытьФорму Тогда
			Закрыть();
		ИначеЕсли ДополнительныеПараметры.ПоказатьСписок Тогда
			ОткрытьСписокВыгружаемыхФизическихЛиц();
		КонецЕсли;
	ИначеЕсли Результат.Статус = "Ошибка" Тогда
		ШаблонТекста =  НСтр("ru = 'При сохранении настроек возникла ошибка:
		|%1'");
		ТекстСообщения = СтрШаблон(ШаблонТекста, Результат.КраткоеПредставлениеОшибки);
		ИнформированиеПользователяКлиент.Предупредить(ТекстСообщения);
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);
	КонецЕсли;

КонецПроцедуры

&НаСервере
Функция СохранитьНастройкиНаСервере()
	
	НаборЗаписей = РегистрыСведений.ПравилаВыгрузкиУправлениеПерсоналом.СоздатьНаборЗаписей();
	Если ИспользоватьШтатноеРасписание Тогда
		ДеревоЗначений = РеквизитФормыВЗначение("ИсходныеНастройкиПубликации");
	Иначе
		ДеревоЗначений = РеквизитФормыВЗначение("НастройкиПубликации");
	КонецЕсли;
	Таблица = НаборЗаписей.Выгрузить();
	ДеревоВТаблицуДляЗаписи(Таблица, ДеревоЗначений);
	Таблица.ЗаполнитьЗначения(Приложение, "Приложение");
	
	ПараметрыСохранения = Новый Структура;
	ПараметрыСохранения.Вставить("Приложение", Приложение);
	ПараметрыСохранения.Вставить("НовыеНастройки", Таблица);
	Если Приложение = Перечисления.ПриложенияДляИнтеграции.КабинетСотрудника Тогда
		ПараметрыСохранения.Вставить("УсловияДопуска", УсловияДопускаДляСохранения());
	КонецЕсли;
	
	ПараметрыВыполненияВФоне = ДлительныеОперации.ПараметрыВыполненияВФоне(УникальныйИдентификатор);
	ПараметрыВыполненияВФоне.Вставить("НаименованиеФоновогоЗадания", НСтр("ru = 'Сохранение настроек'"));
	ПараметрыВыполненияВФоне.ОжидатьЗавершение = 0;
	Возврат ДлительныеОперации.ВыполнитьВФоне(
		"Обработки.ИнтеграцияУправлениеПерсоналом.СохранитьПравилаВыгрузки",
		ПараметрыСохранения,
		ПараметрыВыполненияВФоне);
	
КонецФункции
	
&НаСервере
Функция ДеревоВТаблицуДляЗаписи(Таблица, Дерево)
	
	Для Каждого СтрокаДерева Из Дерево.Строки Цикл
		Если ЗначениеЗаполнено(СтрокаДерева.ПозицияШтатногоРасписания) Тогда
			Ссылка = СтрокаДерева.ПозицияШтатногоРасписания;
		ИначеЕсли ЗначениеЗаполнено(СтрокаДерева.Подразделение) Тогда
			Ссылка = СтрокаДерева.Подразделение;
		Иначе
			Ссылка = СтрокаДерева.Организация;
		КонецЕсли;
		НайденныеСтроки = Таблица.НайтиСтроки(Новый Структура("Ссылка", Ссылка));
		Если Не ЗначениеЗаполнено(НайденныеСтроки) Тогда
			Выгружать = ТриСостоянияВБулево(СтрокаДерева.Выгружать);
			Если Выгружать Тогда
				Запись = Таблица.Добавить();
				Запись.Ссылка = Ссылка;
			КонецЕсли;
		КонецЕсли;
		ДеревоВТаблицуДляЗаписи(Таблица, СтрокаДерева);
	КонецЦикла;
	
КонецФункции

&НаСервере
Функция ТриСостоянияВБулево(Значение)
	Возврат ?(Значение = 2, Ложь, Булево(Значение));
КонецФункции

&НаСервере
Процедура УстановитьУсловноеОформлениеФормы()

	ТекущийШрифт = Элементы["ДеревоНастроек"].Шрифт;
	ЗачеркнутыйШрифт 	= Новый Шрифт(ТекущийШрифт, , , , , , Истина);
	ПолужирныйШрифт 	= Новый Шрифт(ТекущийШрифт, , , Истина);
	
	// Зарытые позиции
	ЭлементОформления = УсловноеОформление.Элементы.Добавить();
	// Вид оформления
	ЭлементОформления.Оформление.УстановитьЗначениеПараметра("Шрифт", ЗачеркнутыйШрифт);
	// Условие для оформления
	ЭлементОтбора = ЭлементОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.Равно;
	ЭлементОтбора.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("НастройкиПубликации.Закрыта");
	ЭлементОтбора.ПравоеЗначение = Истина;
	// Оформляемое поле
	ПолеОформления = ЭлементОформления.Поля.Элементы.Добавить();
	ПолеОформления.Поле = Новый ПолеКомпоновкиДанных("ДеревоНастроекНаименование");
	ПолеОформления = ЭлементОформления.Поля.Элементы.Добавить();
	ПолеОформления.Поле = Новый ПолеКомпоновкиДанных("ДеревоНастроекДолжность");
	
	// Неутвержденные позиции цвет
	ЭлементОформления = УсловноеОформление.Элементы.Добавить();
	// Вид оформления
	ЭлементОформления.Оформление.УстановитьЗначениеПараметра("ЦветТекста", ЦветаСтиля.ТекстНеутвержденныеПозиции);
	// Условие для оформления
	ЭлементОтбора = ЭлементОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.Равно;
	ЭлементОтбора.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("НастройкиПубликации.НеУтверждена");
	ЭлементОтбора.ПравоеЗначение = Истина;
	// Оформляемое поле
	ПолеОформления = ЭлементОформления.Поля.Элементы.Добавить();
	ПолеОформления.Поле = Новый ПолеКомпоновкиДанных("ДеревоНастроекНаименование");
	ПолеОформления = ЭлементОформления.Поля.Элементы.Добавить();
	ПолеОформления.Поле = Новый ПолеКомпоновкиДанных("ДеревоНастроекДолжность");
	ПолеОформления = ЭлементОформления.Поля.Элементы.Добавить();
	ПолеОформления.Поле = Новый ПолеКомпоновкиДанных("ДеревоНастроекДатаУтверждения");
	
	// Неутвержденные позиции текст
	ТекстНадписи = НСтр("ru = 'не утверждена'");
	ЭлементОформления = УсловноеОформление.Элементы.Добавить();
	// Вид оформления
	ЭлементОформления.Оформление.УстановитьЗначениеПараметра("Текст", ТекстНадписи);
	// Условие для оформления
	ЭлементОтбора = ЭлементОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.Равно;
	ЭлементОтбора.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("НастройкиПубликации.НеУтверждена");
	ЭлементОтбора.ПравоеЗначение = Истина;
	// Оформляемое поле
	ПолеОформления = ЭлементОформления.Поля.Элементы.Добавить();
	ПолеОформления.Поле = Новый ПолеКомпоновкиДанных("ДеревоНастроекДатаУтверждения");
	
КонецПроцедуры

&НаСервере
Процедура УстановитьИменаЭлементовФормы()

	Если Приложение = Перечисления.ПриложенияДляИнтеграции.УправлениеПерсоналом Тогда
		ЗаголовокФормы = НСтр("ru = 'Настройка правил выгрузки'");
		ЗаголовокПубликуемыеФизическиеЛица = НСтр("ru = 'Выгружаемые физические лица'");
	Иначе
		ЗаголовокФормы = НСтр("ru = 'Настройка правил публикации кабинетов'");
		ЗаголовокПубликуемыеФизическиеЛица = НСтр("ru = 'Публикуемые кабинеты сотрудников'");
	КонецЕсли;
	Заголовок = СтрШаблон("%1: %2", Приложение, ЗаголовокФормы);
	Элементы.ПубликуемыеФизическиеЛица.Заголовок = ЗаголовокПубликуемыеФизическиеЛица;

КонецПроцедуры

&НаКлиенте
Процедура ОтключитьКабинетыНепубликуемыхСотрудниковПродолжение(Результат, Параметры) Экспорт

	Если Результат = КодВозвратаДиалога.Да Тогда
		
		Если СервисЗаблокирован() Тогда
			ТекстСообщения = ИнтеграцияКабинетСотрудникаКлиент.ТекстСообщенияОБлокировкеСервиса();
			ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);
			Возврат;
		КонецЕсли;
		
		ОчиститьСообщения();
		
		ДлительнаяОперация = ДлительнаяОперацияОтключитьКабинетыНепубликуемыхСотрудников();
		ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(ЭтотОбъект);
		ПараметрыОжидания.ВыводитьОкноОжидания = Истина;
		ПараметрыОжидания.ОповещениеПользователя.Показать = Истина;
		ПараметрыОжидания.ОповещениеПользователя.Текст = НСтр("ru = 'Отключение непубликуемых сотрудников выполнено.'");
		ОповещениеОЗавершении = Новый ОписаниеОповещения("ОтключитьКабинетыНепубликуемыхСотрудниковЗавершение", ЭтотОбъект);
		ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, ОповещениеОЗавершении, ПараметрыОжидания);
		
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОтключитьКабинетыНепубликуемыхСотрудниковЗавершение(Результат, Параметры) Экспорт

	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если Результат.Статус = "Ошибка" Тогда
		ТекстСообщения = НСтр("ru = 'При выполнении фонового задания возникла ошибка:'") + Символы.ПС + Результат.КраткоеПредставлениеОшибки;
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);
		Возврат;
	КонецЕсли;
	
	РезультатВыполнения = ПолучитьИзВременногоХранилища(Результат.АдресРезультата);
	Если РезультатВыполнения.СообщениеОбОшибке <> Неопределено Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(РезультатВыполнения.СообщениеОбОшибке);
	КонецЕсли;

КонецПроцедуры

&НаСервере
Функция ДлительнаяОперацияОтключитьКабинетыНепубликуемыхСотрудников()

	ПараметрыВыполненияВФоне = ДлительныеОперации.ПараметрыВыполненияВФоне(УникальныйИдентификатор);
	ПараметрыВыполненияВФоне.Вставить("НаименованиеФоновогоЗадания", НСтр("ru = 'Отключение непубликуемых сотрудников'"));
	ПараметрыВыполненияВФоне.ОжидатьЗавершение = 0;
	ДлительнаяОперация = ДлительныеОперации.ВыполнитьВФоне(
		"ИнтеграцияКабинетСотрудника.ОтключитьКабинетыНепубликуемыхСотрудниковФоновоеЗадание",
		Неопределено,
		ПараметрыВыполненияВФоне);
		
	Возврат ДлительнаяОперация;

КонецФункции

&НаСервереБезКонтекста
Функция СервисЗаблокирован()

	Возврат ИнтеграцияКабинетСотрудника.ПриложениеЗаблокировано();

КонецФункции

&НаКлиенте
Процедура ДеревоНастроекУсловиеДопускаКЭДОБЗКПриИзмененииНаКлиенте(ДанныеСтроки)
	СтрокиСИндивидуальнойНастройкой = ДеревоНастроекУсловиеДопускаКЭДОБЗКПриИзмененииНаСервере(ДанныеСтроки.ПолучитьИдентификатор());
	Если ЗначениеЗаполнено(СтрокиСИндивидуальнойНастройкой) Тогда
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("СтрокиСИндивидуальнойНастройкой", СтрокиСИндивидуальнойНастройкой);
		ДополнительныеПараметры.Вставить("УсловиеДопускаКЭДОБЗК", ДанныеСтроки.УсловиеДопускаКЭДОБЗК);
		Оповещение = Новый ОписаниеОповещения("ОбработатьОтветНаВопросОЗаменеУсловийДоступаКЭДОБЗК", ЭтотОбъект, ДополнительныеПараметры);
		ТекстВопроса = НСтр("ru = 'Заменить условие доступа к ЭДО структурных единиц имеющих условия доступа отличные от выбранного?'");
		ПоказатьВопрос(Оповещение, ТекстВопроса, РежимДиалогаВопрос.ДаНет);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОтветНаВопросОЗаменеУсловийДоступаКЭДОБЗК(Ответ, ДополнительныеПараметры) Экспорт
	Если Ответ = КодВозвратаДиалога.Да Тогда
		ЗаменитьУсловиеДопускаКЭДОБЗК(
			ДополнительныеПараметры.СтрокиСИндивидуальнойНастройкой,
			ДополнительныеПараметры.УсловиеДопускаКЭДОБЗК);
	КонецЕсли;
КонецПроцедуры

&НаСервере
Процедура ЗаменитьУсловиеДопускаКЭДОБЗК(СтрокиСИндивидуальнойНастройкой, УсловиеДопускаКЭДОБЗК)
	
	ДанныеСтрок = Новый Массив;
	Для Каждого ИдентификаторСтроки Из СтрокиСИндивидуальнойНастройкой Цикл
		ДанныеСтроки = НастройкиПубликации.НайтиПоИдентификатору(ИдентификаторСтроки);
		Если ДанныеСтроки <> Неопределено Тогда
			ДанныеСтрок.Добавить(ДанныеСтроки);
		КонецЕсли;
	КонецЦикла;
	Если ЗначениеЗаполнено(ДанныеСтрок) Тогда
		УстановитьУсловиеДопускаКЭДОБЗК(
			ДанныеСтрок,
			УсловиеДопускаКЭДОБЗК,
			Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ДеревоНастроекУсловиеДопускаКЭДОБЗКПриИзмененииНаСервере(ИдентификаторСтроки)
	СтрокиСИндивидуальнойНастройкой = Новый Массив;
	СтрокаРодителя = НастройкиПубликации.НайтиПоИдентификатору(ИдентификаторСтроки);
	Если СтрокаРодителя <> Неопределено Тогда
		УстановитьУсловиеДопускаКЭДОБЗК(
			СтрокаРодителя.ПолучитьЭлементы(),
			СтрокаРодителя.УсловиеДопускаКЭДОБЗК,
			Ложь,
			СтрокиСИндивидуальнойНастройкой);
	КонецЕсли;
	Возврат СтрокиСИндивидуальнойНастройкой;
КонецФункции

&НаСервере
Процедура УстановитьУсловиеДопускаКЭДОБЗК(СтрокиДерева, УсловиеДопускаКЭДОБЗК, МенятьИндивидуальныеНастройки, СтрокиСИндивидуальнойНастройкой = Неопределено)
	Для Каждого СтрокаДерева Из СтрокиДерева Цикл
		Если Не МенятьИндивидуальныеНастройки
			И СтрокиСИндивидуальнойНастройкой <> Неопределено
			И СтрокаДерева.УсловиеДопускаКЭДОБЗК <> УсловиеДопускаКЭДОБЗК
			И СтрокаДерева.УсловиеДопускаКЭДОБЗК <> УсловиеДопускаКЭДОБЗКДоИзменения Тогда
			
			СтрокиСИндивидуальнойНастройкой.Добавить(СтрокаДерева.ПолучитьИдентификатор());
		Иначе
			СтрокаДерева.УсловиеДопускаКЭДОБЗК = УсловиеДопускаКЭДОБЗК;
			УстановитьУсловиеДопускаКЭДОБЗК(
				СтрокаДерева.ПолучитьЭлементы(),
				УсловиеДопускаКЭДОБЗК,
				МенятьИндивидуальныеНастройки,
				СтрокиСИндивидуальнойНастройкой);
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

&НаСервере
Функция УсловияДопускаДляСохранения()
	УсловияДляСохранения = Новый Соответствие;
	ЗаполнитьУсловияДляСохраненияПоКоллекцииСтрок(НастройкиПубликации.ПолучитьЭлементы(), УсловияДляСохранения, Перечисления.УсловияДопускаСотрудниковКЭДОБЗК.ПриПроверкеСкановПодписанногоСогласияНаЭДОБЗК);
	Возврат УсловияДляСохранения;
КонецФункции

&НаСервере
Функция ЗаполнитьУсловияДляСохраненияПоКоллекцииСтрок(СтрокиДерева, УсловияДляСохранения, УсловияДляСохраненияВышестоящего)
	Для Каждого СтрокаДерева Из СтрокиДерева Цикл
		Если СтрокаДерева.УсловиеДопускаКЭДОБЗК <> УсловияДляСохраненияВышестоящего Тогда
			Если ЗначениеЗаполнено(СтрокаДерева.ПозицияШтатногоРасписания) Тогда
				СтруктурнаяЕдиница = СтрокаДерева.ПозицияШтатногоРасписания;
			ИначеЕсли ЗначениеЗаполнено(СтрокаДерева.Подразделение) Тогда
				СтруктурнаяЕдиница = СтрокаДерева.Подразделение;
			Иначе
				СтруктурнаяЕдиница = СтрокаДерева.Организация;
			КонецЕсли;
			УсловияДляСохранения.Вставить(СтруктурнаяЕдиница, СтрокаДерева.УсловиеДопускаКЭДОБЗК);
		КонецЕсли;
		ЗаполнитьУсловияДляСохраненияПоКоллекцииСтрок(СтрокаДерева.ПолучитьЭлементы(), УсловияДляСохранения, СтрокаДерева.УсловиеДопускаКЭДОБЗК)
	КонецЦикла;
КонецФункции

#КонецОбласти