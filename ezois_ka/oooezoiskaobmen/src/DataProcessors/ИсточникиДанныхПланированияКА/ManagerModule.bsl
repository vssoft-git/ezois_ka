#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныеПроцедурыИФункции

#Область Прочее

Функция ШаблоныСхемыКомпоновкиДанных() Экспорт
	
	Шаблоны = Новый Массив;
	
	Для каждого Макет Из Метаданные.Обработки.ИсточникиДанныхПланированияКА.Макеты Цикл
		
		Если Макет.ТипМакета <> Метаданные.СвойстваОбъектов.ТипМакета.СхемаКомпоновкиДанных Тогда
			
			Продолжить;
			
		КонецЕсли;
		
		Если (Макет.Имя = "ПланыПроизводстваМатериалыПредопределенный"
			ИЛИ Макет.Имя = "ПланыПроизводстваПредопределенный"
			ИЛИ Макет.Имя = "ПланыВыпускаПредопределенный")
			И НЕ ПолучитьФункциональнуюОпцию("ИспользоватьПланированиеПроизводства")
			ИЛИ Макет.Имя = "ЗаказыМатериаловВПроизводствоПредопределенный" И НЕ ПолучитьФункциональнуюОпцию("ИспользоватьПроизводство")
			ИЛИ Макет.Имя = "ПроизводствоПредопределенный" И (НЕ ПолучитьФункциональнуюОпцию("ИспользоватьПроизводство") ИЛИ НЕ ПолучитьФункциональнуюОпцию("КомплекснаяАвтоматизация"))
			ИЛИ Макет.Имя = "ОборотыБюджетовПредопределенный" И НЕ ПолучитьФункциональнуюОпцию("ИспользоватьБюджетирование") Тогда
			
			Продолжить;
			
		КонецЕсли;
		
		Шаблоны.Добавить(Новый Структура("Имя, Синоним, ПолноеИмяИсточникаШаблонов", Макет.Имя, Макет.Синоним, "Обработка.ИсточникиДанныхПланированияКА"));
		
	КонецЦикла;
	
	Возврат Шаблоны;
	
КонецФункции

#КонецОбласти

#КонецОбласти

#КонецЕсли