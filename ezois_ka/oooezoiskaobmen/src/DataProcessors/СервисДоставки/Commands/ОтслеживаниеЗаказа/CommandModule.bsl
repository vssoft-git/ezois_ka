
#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ОчиститьСообщения();
	
	ТипГрузоперевозки = Неопределено;
	Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ПараметрыВыполненияКоманды.Источник, "ТипГрузоперевозки") Тогда
		ТипГрузоперевозки = ПараметрыВыполненияКоманды.Источник.ТипГрузоперевозки;
	КонецЕсли;
	
	ОрганизацияБизнесСети = Неопределено;
	Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ПараметрыВыполненияКоманды.Источник, "ОрганизацияБизнесСетиСсылка") Тогда
		ОрганизацияБизнесСети = ПараметрыВыполненияКоманды.Источник.ОрганизацияБизнесСетиСсылка;
	КонецЕсли;
	
	ПараметрыФормы = СервисДоставкиКлиент.НовыйПараметрыОткрытьФормуСервисаДоставки();
	ПараметрыФормы.ИмяФормы = "ОтслеживаниеЗаказа";
	ПараметрыФормы.ТипГрузоперевозки = ТипГрузоперевозки;
	ПараметрыФормы.ОрганизацияБизнесСети = ОрганизацияБизнесСети;
	
	СервисДоставкиКлиент.ОткрытьФормуСервисаДоставки(ПараметрыФормы);
	
КонецПроцедуры

#КонецОбласти
