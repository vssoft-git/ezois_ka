#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Версия формата выгрузки.
// 
// Параметры:
//  НаДату - Дата
//  ВыбраннаяФорма - Строка
// 
// Возвращаемое значение:
//   ПеречислениеСсылка.ВерсииФорматовВыгрузки
//
Функция ВерсияФорматаВыгрузки(Знач НаДату = Неопределено, ВыбраннаяФорма = Неопределено) Экспорт
	
	Возврат Перечисления.ВерсииФорматовВыгрузки.Версия500;
	
КонецФункции

// Возвращает таблицу форм отчета с реквизитами и периодами действия.
// 
// Возвращаемое значение:
//   ТаблицаЗначений - Таблица форм отчета:
//     * ФормаОтчета - Строка
//     * ОписаниеОтчета - Строка
//     * ДатаНачалоДействия - Дата
//     * ДатаКонецДействия - Дата
//     * РедакцияФормы - Строка
//
Функция ТаблицаФормОтчета() Экспорт
	
	Неограниченный = '0001-01-01';
	
	ОписаниеТиповСтрока254 = Новый ОписаниеТипов("Строка", , Новый КвалификаторыСтроки(254));
	
	МассивТипов = Новый Массив;
	МассивТипов.Добавить(Тип("Дата"));
	ОписаниеТиповДата = Новый ОписаниеТипов(МассивТипов, , Новый КвалификаторыДаты(ЧастиДаты.Дата));
	
	ТаблицаФормОтчета = Новый ТаблицаЗначений;
	ТаблицаФормОтчета.Колонки.Добавить("ФормаОтчета",        ОписаниеТиповСтрока254);
	ТаблицаФормОтчета.Колонки.Добавить("ОписаниеОтчета",     ОписаниеТиповСтрока254, "Утверждена",  20);
	ТаблицаФормОтчета.Колонки.Добавить("ДатаНачалоДействия", ОписаниеТиповДата,      "Действует с", 5);
	ТаблицаФормОтчета.Колонки.Добавить("ДатаКонецДействия",  ОписаниеТиповДата,      "         по", 5);
	ТаблицаФормОтчета.Колонки.Добавить("РедакцияФормы",      ОписаниеТиповСтрока254, "Редакция формы", 20);
	
	НоваяФорма = ТаблицаФормОтчета.Добавить();
	НоваяФорма.ФормаОтчета        = "ФормаОтчета2025Кв1";
	НоваяФорма.ОписаниеОтчета     = "Утверждена приказом ФНС России от 10.12.2024 № ЕД-7-13/1115@.";
	НоваяФорма.РедакцияФормы      = "от 10.12.2024 № ЕД-7-13/1115@";
	НоваяФорма.ДатаНачалоДействия = '2024-01-01';
	НоваяФорма.ДатаКонецДействия  = Неограниченный;
	
	Возврат ТаблицаФормОтчета;
	
КонецФункции

// Возвращает сведения об отдельных показателях регламентированного отчета.
// 
// Параметры:
//   ЭкземплярРеглОтчета - ДокументСсылка.РегламентированныйОтчет
//   ДополнительныеПараметры - Произвольный
// 
// Возвращаемое значение:
//   Неопределено - получение сведений из отчета не предусмотрено
//
Функция ДанныеРеглОтчета(ЭкземплярРеглОтчета, ДополнительныеПараметры = Неопределено) Экспорт
	
	Возврат Неопределено;
	
КонецФункции

// Возвращает дерево форм и форматов регламентированного отчета.
// 
// Возвращаемое значение:
//  ДеревоЗначений:
//    * Код - Строка
//    * ДатаПриказа - Дата
//    * НомерПриказа - Строка
//    * ДатаНачалаДействия - Дата
//    * ДатаОкончанияДействия - Дата
//    * ИмяОбъекта - Строка
//    * Описание - Строка
//
Функция ДеревоФормИФорматов() Экспорт
	
	ФормыИФорматы = Новый ДеревоЗначений;
	ФормыИФорматы.Колонки.Добавить("Код",                   Новый ОписаниеТипов("Строка"));
	ФормыИФорматы.Колонки.Добавить("ДатаПриказа",           Новый ОписаниеТипов("Дата"));
	ФормыИФорматы.Колонки.Добавить("НомерПриказа",          Новый ОписаниеТипов("Строка"));
	ФормыИФорматы.Колонки.Добавить("ДатаНачалаДействия",    Новый ОписаниеТипов("Дата"));
	ФормыИФорматы.Колонки.Добавить("ДатаОкончанияДействия", Новый ОписаниеТипов("Дата"));
	ФормыИФорматы.Колонки.Добавить("ИмяОбъекта",            Новый ОписаниеТипов("Строка"));
	ФормыИФорматы.Колонки.Добавить("Описание",              Новый ОписаниеТипов("Строка"));
	
	ДобавитьФормуИФорматыФормаОтчета2025Кв1(ФормыИФорматы);
	
	Возврат ФормыИФорматы;
	
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ДобавитьФормуИФорматыФормаОтчета2025Кв1(ФормыИФорматы)
	
	ФормаОтчета = ФормыИФорматы.Строки.Добавить();
	ФормаОтчета.Код = "1184067";
	ФормаОтчета.ДатаПриказа = '2024-12-10';
	ФормаОтчета.НомерПриказа = "ЕД-7-13/1115@";
	ФормаОтчета.ИмяОбъекта = "ФормаОтчета2025Кв1";
	
	ФорматФормы = ФормаОтчета.Строки.Добавить();
	ФорматФормы.Код = "5.01";
	ФорматФормы.ДатаНачалаДействия    = ФормаОтчета.ДатаНачалаДействия;
	ФорматФормы.ДатаОкончанияДействия = ФормаОтчета.ДатаОкончанияДействия;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли
