#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если ТипЗнч(Параметры.Данные) = Тип("ХранилищеЗначения") Тогда 
		ЗаполнитьЗначенияСвойств(ЭтотОбъект, Параметры.Данные.Получить());
	КонецЕсли;
	_ИННФЛ = (Не ЗначениеЗаполнено(ИННФЛ)) Или РегламентированныеДанныеКлиентСервер.ИННСоответствуетТребованиям(ИННФЛ, Ложь, "");
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ИННФЛПриИзменении(Элемент)
	_ИННФЛ = (Не ЗначениеЗаполнено(ИННФЛ)) Или РегламентированныеДанныеКлиентСервер.ИННСоответствуетТребованиям(ИННФЛ, Ложь, "");
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Ок(Команда)
	Если _ИННФЛ И ЗначениеЗаполнено(Фамилия) И ЗначениеЗаполнено(Имя) И ЗначениеЗаполнено(КодВидДок) И ЗначениеЗаполнено(СерНомДок) Тогда 
		Закрыть(ОписаниеРезультата());
	Иначе
		ОбщегоНазначенияКлиент.СообщитьПользователю("Необходимо корректно заплонить данные");
	КонецЕсли;
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Функция ОписаниеРезультата()
	Данные = Новый Структура("ИННФЛ,Фамилия,Имя,Отчество,КодВидДок,СерНомДок", ИННФЛ, Фамилия, Имя, Отчество, КодВидДок, СерНомДок);
	Возврат Новый Структура("Описание, Данные, Тип", СокрЛП(Фамилия + " " + Имя + " " + Отчество), Новый ХранилищеЗначения(Данные), "3");
КонецФункции

#КонецОбласти
