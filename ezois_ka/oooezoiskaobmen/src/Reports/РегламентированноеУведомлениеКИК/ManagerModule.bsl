#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

#КонецОбласти

#Область СлужебныеПроцедурыИФункции
Функция ПолучитьТаблицуПримененияФорматов() Экспорт 
	Результат = Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюТаблицуПримененияФорматов();
	
	Стр = Результат.Добавить();
	Стр.ИмяФормы = "Форма2023_1";
	Стр.КНД = "1120416";
	Стр.ВерсияФормата = "5.04";
	
	Стр = Результат.Добавить();
	Стр.ИмяФормы = "Форма2021_1";
	Стр.КНД = "1120416";
	Стр.ВерсияФормата = "5.03";
	
	Стр = Результат.Добавить();
	Стр.ИмяФормы = "Форма2019_1";
	Стр.КНД = "1120416";
	Стр.ВерсияФормата = "5.02";
	
	Стр = Результат.Добавить();
	Стр.ИмяФормы = "Форма2017_1";
	Стр.КНД = "1120416";
	Стр.ВерсияФормата = "5.01";
	
	Возврат Результат;
КонецФункции

Функция ДанноеУведомлениеДоступноДляОрганизации() Экспорт 
	Возврат Истина;
КонецФункции

Функция ДанноеУведомлениеДоступноДляИП() Экспорт 
	Возврат Истина;
КонецФункции

Функция ПолучитьОсновнуюФорму() Экспорт 
	Возврат "";
КонецФункции

Функция ПолучитьФормуПоУмолчанию() Экспорт 
	Возврат "";
КонецФункции

Функция ПолучитьТаблицуФорм() Экспорт 
	Результат = Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюТаблицуФормУведомления();
	
	Стр = Результат.Добавить();
	Стр.ИмяФормы = "Форма2017_1";
	Стр.ОписаниеФормы = "Приказ ФНС России от 13.12.2016 № ММВ-7-13/679@ (за 2016-2018 годы), формат 5.01";
	Стр.ДатаНачала = '20150101';
	Стр.ДатаКонца = '20991231';
	
	Стр = Результат.Добавить();
	Стр.ИмяФормы = "Форма2019_1";
	Стр.ОписаниеФормы = "Приказ ФНС России от 26.08.2019 № ММВ-7-13/422@ (за 2019-2021 годы), формат 5.02";
	Стр.ДатаНачала = '20190101';
	Стр.ДатаКонца = '20991231';
	
	Стр = Результат.Добавить();
	Стр.ИмяФормы = "Форма2021_1";
	Стр.ОписаниеФормы = "Приказ ФНС России от 19.07.2021 № ЕД-7-13/671@ (за 2022 год), формат 5.03";
	Стр.ДатаНачала = '20220101';
	Стр.ДатаКонца = '20991231';
	
	Стр = Результат.Добавить();
	Стр.ИмяФормы = "Форма2023_1";
	Стр.ОписаниеФормы = "Приказ ФНС России от 09.10.2023 № ЕД-7-13/721@. (за 2023- годы), формат 5.04";
	Стр.ДатаНачала = '20230101';
	Стр.ДатаКонца = '20991231';
	
	Возврат Результат;
КонецФункции

Функция СформироватьВыгрузкуИПолучитьДанные(ДокОбъект) Экспорт 
	Выгрузка = ДокОбъект.ВыгрузитьДокумент();
	Если Выгрузка = Неопределено Тогда 
		Возврат Неопределено;
	КонецЕсли;
	СтруктураВыгрузки = Новый Структура("ТестВыгрузки,КодировкаВыгрузки", Выгрузка[0].ТестВыгрузки, Выгрузка[0].КодировкаВыгрузки);
	
	ПараметрыЗагрузкиМакета = Новый Структура("Объект, АдресВХранилищеЭкранныеФормы, УникальныйИдентификатор", ДокОбъект, "", Новый УникальныйИдентификатор);
	Если ДокОбъект.ИмяФормы = "Форма2023_1" Тогда
		ПараметрыЗагрузкиМакета.Вставить("ИмяМакетаАрхиваТабличныхДокументов", "ЭкранныеФормы_2023");
		СтруктураВыгрузки.Вставить("Данные", УведомлениеОСпецрежимахНалогообложения.ПолучитьМакетТабличногоДокумента(ПараметрыЗагрузкиМакета, "TIFF_2023_1"));
		СтруктураВыгрузки.Вставить("ИмяФайла", "1120416_5.04000_05.tif");
		ПоместитьВоВременноеХранилище(Неопределено, ПараметрыЗагрузкиМакета.АдресВХранилищеЭкранныеФормы);
		Возврат СтруктураВыгрузки;
	ИначеЕсли ДокОбъект.ИмяФормы = "Форма2021_1" Тогда 
		ПараметрыЗагрузкиМакета.Вставить("ИмяМакетаАрхиваТабличныхДокументов", "ЭкранныеФормы");
		СтруктураВыгрузки.Вставить("Данные", УведомлениеОСпецрежимахНалогообложения.ПолучитьМакетТабличногоДокумента(ПараметрыЗагрузкиМакета, "TIFF_2021_1"));
		СтруктураВыгрузки.Вставить("ИмяФайла", "1120416_5.03000_04.tif");
		ПоместитьВоВременноеХранилище(Неопределено, ПараметрыЗагрузкиМакета.АдресВХранилищеЭкранныеФормы);
		Возврат СтруктураВыгрузки;
	ИначеЕсли ДокОбъект.ИмяФормы = "Форма2019_1" Тогда 
		ПараметрыЗагрузкиМакета.Вставить("ИмяМакетаАрхиваТабличныхДокументов", "ЭкранныеФормы");
		СтруктураВыгрузки.Вставить("Данные", УведомлениеОСпецрежимахНалогообложения.ПолучитьМакетТабличногоДокумента(ПараметрыЗагрузкиМакета, "TIFF_2019_1"));
		СтруктураВыгрузки.Вставить("ИмяФайла", "1120416_5.02000_03.tif");
		ПоместитьВоВременноеХранилище(Неопределено, ПараметрыЗагрузкиМакета.АдресВХранилищеЭкранныеФормы);
		Возврат СтруктураВыгрузки;
	ИначеЕсли ДокОбъект.ИмяФормы = "Форма2017_1" Тогда 
		ПараметрыЗагрузкиМакета.Вставить("ИмяМакетаАрхиваТабличныхДокументов", "ЭкранныеФормы");
		СтруктураВыгрузки.Вставить("Данные", УведомлениеОСпецрежимахНалогообложения.ПолучитьМакетТабличногоДокумента(ПараметрыЗагрузкиМакета, "TIFF_2017_1"));
		СтруктураВыгрузки.Вставить("ИмяФайла", "1120416_5.01000_01.tif");
		ПоместитьВоВременноеХранилище(Неопределено, ПараметрыЗагрузкиМакета.АдресВХранилищеЭкранныеФормы);
		Возврат СтруктураВыгрузки;
	КонецЕсли;
КонецФункции

Функция ЭлектронноеПредставление(Объект, ИмяФормы, УникальныйИдентификатор) Экспорт
	Если ИмяФормы = "Форма2017_1" Тогда
		Возврат ЭлектронноеПредставление_Форма2017_1(Объект, УникальныйИдентификатор);
	ИначеЕсли ИмяФормы = "Форма2019_1" Тогда
		Возврат ЭлектронноеПредставление_Форма2019_1(Объект, УникальныйИдентификатор);
	ИначеЕсли ИмяФормы = "Форма2021_1" Тогда
		Возврат ЭлектронноеПредставление_Форма2021_1(Объект, УникальныйИдентификатор);
	ИначеЕсли ИмяФормы = "Форма2023_1" Тогда
		Возврат ЭлектронноеПредставление_Форма2023_1(Объект, УникальныйИдентификатор);
	КонецЕсли;
	Возврат Неопределено;
КонецФункции

Функция СформироватьСписокЛистов(Объект) Экспорт
	Если Объект.ИмяФормы = "Форма2017_1" 
		Или Объект.ИмяФормы = "Форма2019_1"
		Или Объект.ИмяФормы = "Форма2021_1" 
		Или Объект.ИмяФормы = "Форма2023_1" Тогда 
		Возврат СформироватьСписокЛистовФорма2017_1(Объект);
	КонецЕсли;
КонецФункции

Функция ПроверитьДокументСВыводомВТаблицу(Объект, ИмяФормы, УникальныйИдентификатор) Экспорт 
	Если ИмяФормы = "Форма2017_1" Тогда 
		Возврат ПроверитьДокументСВыводомВТаблицу_Форма2017_1(
			УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект), УникальныйИдентификатор);
	ИначеЕсли ИмяФормы = "Форма2019_1" Тогда
		Возврат ПроверитьДокументСВыводомВТаблицу_Форма2019_1(
			УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект), УникальныйИдентификатор);
	ИначеЕсли ИмяФормы = "Форма2021_1" Тогда
		Возврат ПроверитьДокументСВыводомВТаблицу_Форма2021_1(
			УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект), УникальныйИдентификатор);
	ИначеЕсли ИмяФормы = "Форма2023_1" Тогда
		Возврат ПроверитьДокументСВыводомВТаблицу_Форма2023_1(
			УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект), УникальныйИдентификатор);
	КонецЕсли;
КонецФункции

Функция ИдентификаторФайлаЭлектронногоПредставления(СведенияОтправки)
	Префикс = "UT_UVKIK";
	Возврат Документы.УведомлениеОСпецрежимахНалогообложения.ИдентификаторФайлаЭлектронногоПредставления(Префикс, СведенияОтправки);
КонецФункции

Функция ОсновныеСведенияЭлектронногоПредставления_Форма2017_1(Объект, УникальныйИдентификатор)
	ОсновныеСведения = Документы.УведомлениеОСпецрежимахНалогообложения.НачальнаяИнициализацияОбщихРеквизитовВыгрузки(Объект);
	ИдентификаторФайла = ИдентификаторФайлаЭлектронногоПредставления(ОсновныеСведения);
	ОсновныеСведения.Вставить("ИдФайл", ИдентификаторФайла);
	Возврат ОсновныеСведения;
КонецФункции

Функция ОсновныеСведенияЭлектронногоПредставления_Форма2019_1(Объект, УникальныйИдентификатор)
	ОсновныеСведения = Документы.УведомлениеОСпецрежимахНалогообложения.НачальнаяИнициализацияОбщихРеквизитовВыгрузки(Объект);
	ИдентификаторФайла = ИдентификаторФайлаЭлектронногоПредставления(ОсновныеСведения);
	ОсновныеСведения.Вставить("ИдФайл", ИдентификаторФайла);
	Возврат ОсновныеСведения;
КонецФункции

Функция ЭлектронноеПредставление_Форма2017_1(Объект, УникальныйИдентификатор)
	СведенияЭлектронногоПредставления = УведомлениеОСпецрежимахНалогообложения.СведенияЭлектронногоПредставления();
	
	ДанныеУведомления = Объект.ДанныеУведомления.Получить();
	Ошибки = ПроверитьДокументСВыводомВТаблицу_Форма2017_1(ДанныеУведомления, УникальныйИдентификатор);
	УведомлениеОСпецрежимахНалогообложения.СообщитьОшибкиПриПроверкеВыгрузки(Объект, Ошибки, ДанныеУведомления);
	
	ОсновныеСведения = ОсновныеСведенияЭлектронногоПредставления_Форма2017_1(Объект, УникальныйИдентификатор);
	СтруктураВыгрузки = Документы.УведомлениеОСпецрежимахНалогообложения.ИзвлечьСтруктуруXMLУведомления(Объект.ИмяОтчета, "СхемаВыгрузкиФорма2017_1");
	ДополнитьПараметры_2017(ДанныеУведомления);
	УведомлениеОСпецрежимахНалогообложения.ТиповоеЗаполнениеДанными(ДанныеУведомления, ОсновныеСведения, СтруктураВыгрузки);
	УведомлениеОСпецрежимахНалогообложения.ВыгрузитьДеревоВТаблицу(СтруктураВыгрузки, ОсновныеСведения, СведенияЭлектронногоПредставления);
	Возврат СведенияЭлектронногоПредставления;
КонецФункции

Функция ЭлектронноеПредставление_Форма2019_1(Объект, УникальныйИдентификатор)
	СведенияЭлектронногоПредставления = УведомлениеОСпецрежимахНалогообложения.СведенияЭлектронногоПредставления();
	ДанныеУведомления = УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект);
	Ошибки = ПроверитьДокументСВыводомВТаблицу_Форма2019_1(ДанныеУведомления, УникальныйИдентификатор);
	УведомлениеОСпецрежимахНалогообложения.СообщитьОшибкиПриПроверкеВыгрузки(Объект, Ошибки, ДанныеУведомления);
	
	ОсновныеСведения = ОсновныеСведенияЭлектронногоПредставления_Форма2019_1(Объект, УникальныйИдентификатор);
	СтруктураВыгрузки = Документы.УведомлениеОСпецрежимахНалогообложения.ИзвлечьСтруктуруXMLУведомления(Объект.ИмяОтчета, "СхемаВыгрузкиФорма2019_1");
	ДополнитьПараметры_2019(ДанныеУведомления);
	УведомлениеОСпецрежимахНалогообложения.ТиповоеЗаполнениеДанными(ДанныеУведомления, ОсновныеСведения, СтруктураВыгрузки);
	УведомлениеОСпецрежимахНалогообложения.ВыгрузитьДеревоВТаблицу(СтруктураВыгрузки, ОсновныеСведения, СведенияЭлектронногоПредставления);
	Возврат СведенияЭлектронногоПредставления;
КонецФункции

Функция ЭлектронноеПредставление_Форма2021_1(Объект, УникальныйИдентификатор)
	СведенияЭлектронногоПредставления = УведомлениеОСпецрежимахНалогообложения.СведенияЭлектронногоПредставления();
	ДанныеУведомления = УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект);
	Ошибки = ПроверитьДокументСВыводомВТаблицу_Форма2021_1(ДанныеУведомления, УникальныйИдентификатор);
	УведомлениеОСпецрежимахНалогообложения.СообщитьОшибкиПриПроверкеВыгрузки(Объект, Ошибки, ДанныеУведомления);
	
	ОсновныеСведения = ОсновныеСведенияЭлектронногоПредставления_Форма2019_1(Объект, УникальныйИдентификатор);
	СтруктураВыгрузки = Документы.УведомлениеОСпецрежимахНалогообложения.ИзвлечьСтруктуруXMLУведомления(Объект.ИмяОтчета, "СхемаВыгрузкиФорма2021_1");
	ДополнитьПараметры_2019(ДанныеУведомления);
	УведомлениеОСпецрежимахНалогообложения.ТиповоеЗаполнениеДанными(ДанныеУведомления, ОсновныеСведения, СтруктураВыгрузки);
	УведомлениеОСпецрежимахНалогообложения.ВыгрузитьДеревоВТаблицу(СтруктураВыгрузки, ОсновныеСведения, СведенияЭлектронногоПредставления);
	Возврат СведенияЭлектронногоПредставления;
КонецФункции

Функция ЭлектронноеПредставление_Форма2023_1(Объект, УникальныйИдентификатор)
	СведенияЭлектронногоПредставления = УведомлениеОСпецрежимахНалогообложения.СведенияЭлектронногоПредставления();
	ДанныеУведомления = УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект);
	Ошибки = ПроверитьДокументСВыводомВТаблицу_Форма2023_1(ДанныеУведомления, УникальныйИдентификатор);
	УведомлениеОСпецрежимахНалогообложения.СообщитьОшибкиПриПроверкеВыгрузки(Объект, Ошибки, ДанныеУведомления);
	
	ОсновныеСведения = ОсновныеСведенияЭлектронногоПредставления_Форма2019_1(Объект, УникальныйИдентификатор);
	СтруктураВыгрузки = Документы.УведомлениеОСпецрежимахНалогообложения.ИзвлечьСтруктуруXMLУведомления(Объект.ИмяОтчета, "СхемаВыгрузкиФорма2023_1");
	ДополнитьПараметры_2019(ДанныеУведомления);
	УведомлениеОСпецрежимахНалогообложения.ТиповоеЗаполнениеДанными(ДанныеУведомления, ОсновныеСведения, СтруктураВыгрузки);
	УведомлениеОСпецрежимахНалогообложения.ВыгрузитьДеревоВТаблицу(СтруктураВыгрузки, ОсновныеСведения, СведенияЭлектронногоПредставления);
	Возврат СведенияЭлектронногоПредставления;
КонецФункции

Функция ПолучитьСтруктураЛистГ1ДляВыгрузки_2017()
	Возврат Новый Структура("УИД, УИДРодителя, НомерРосОрг, ОГРН, ИННЮЛ, КПП, НаимОрг");
КонецФункции

Функция ПолучитьСтруктураЛистГ2ДляВыгрузки_2017()
	Рез = Новый Структура("УИД, УИДРодителя");
	Рез.Вставить("НомерИО");
	Рез.Вставить("НаимОрг");
	Рез.Вставить("НаимОргЛат");
	Рез.Вставить("СтрРег");
	Рез.Вставить("РегНомер");
	Рез.Вставить("КодНПРег");
	Рез.Вставить("АдрСтрРег");
	Возврат Рез;
КонецФункции

Функция ПолучитьСтруктураЛистГ00ДляВыгрузки_2017()
	Возврат Новый Структура("УИД, УИДРодителя, ДоляУчКосв, ДоляУчПрям, НомерУч");
КонецФункции

Функция ПолучитьСтруктураЛистГ0ДляВыгрузки_2017()
	Возврат Новый Структура("УИД, УИДРодителя, НомерПосл, ДоляКосвПослИт");
КонецФункции

Функция ПолучитьСтруктураЛистГДляВыгрузки_2017()
	Возврат Новый Структура("УИД, НомерКИК, НаимОрг, ДоляУчКосвИт");
КонецФункции

Процедура СформироватьПараметрыДляЛистовГ_2017(Параметры)
	УИДыЛистГ = Новый Соответствие;
	ВсеУчастникиЛистыГ = Новый Соответствие;
	Для Каждого Стр Из Параметры.ДанныеМногостраничныхРазделов.ЛистГ Цикл
		ПарамСтр = Стр.Значение;
		Если ЗначениеЗаполнено(ПарамСтр.НомерКИК) Тогда 
			Если ВсеУчастникиЛистыГ[ПарамСтр.НомерКИК] = Неопределено Тогда 
				ВсеУчастникиЛистыГ.Вставить(ПарамСтр.НомерКИК, Новый СписокЗначений);
				УИДыЛистГ.Вставить(ПарамСтр.НомерКИК, ПарамСтр.УИД);
			КонецЕсли;
			ВсеУчастникиЛистыГ[ПарамСтр.НомерКИК].Добавить(ПарамСтр);
		КонецЕсли;
	КонецЦикла;
	
	Параметры.ДанныеМногостраничныхРазделов.Вставить("ЛистГДляВыгрузки", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("ЛистГ0ДляВыгрузки", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("ЛистГ00ДляВыгрузки", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("ЛистГ1ДляВыгрузки", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("ЛистГ2ДляВыгрузки", Новый СписокЗначений);
	
	Для Каждого КЗ Из ВсеУчастникиЛистыГ Цикл
		Стр = ПолучитьСтруктураЛистГДляВыгрузки_2017();
		ЗаполнитьЗначенияСвойств(Стр, КЗ.Значение[0].Значение);
		Параметры.ДанныеМногостраничныхРазделов.ЛистГДляВыгрузки.Добавить(Стр);
		
		УИДРодителя = КЗ.Значение[0].Значение.УИД;
		Для Каждого Элт Из КЗ.Значение Цикл
			Стр = ПолучитьСтруктураЛистГ0ДляВыгрузки_2017();
			ЗаполнитьЗначенияСвойств(Стр, Элт.Значение);
			Стр.УИДРодителя = УИДРодителя;
			Стр.НомерПосл = Формат(Стр.НомерПосл, "ЧЦ=5; ЧН=; ЧВН=; ЧГ=");
			Параметры.ДанныеМногостраничныхРазделов.ЛистГ0ДляВыгрузки.Добавить(Стр);
			
			МнгСтрСтроки = Параметры.ДанныеДопСтрокБД.МнгСтр.НайтиСтроки(Новый Структура("УИД", Элт.Значение.УИД));
			Для Каждого СтрокаМнг Из МнгСтрСтроки Цикл
				СтрПодч = ПолучитьСтруктураЛистГ00ДляВыгрузки_2017();
				ЗаполнитьЗначенияСвойств(СтрПодч, СтрокаМнг);
				СтрПодч.УИДРодителя = Стр.УИД;
				Параметры.ДанныеМногостраничныхРазделов.ЛистГ00ДляВыгрузки.Добавить(СтрПодч);
			КонецЦикла;
			
			Для Каждого ЛистГ1 Из Параметры.ДанныеМногостраничныхРазделов.ЛистГ1 Цикл 
				Если ЛистГ1.Значение.УИДРодителя = Элт.Значение.УИД 
					И ЗначениеЗаполнено(ЛистГ1.Значение.НомерРосОрг) Тогда 
					
					СтрГ1 = ПолучитьСтруктураЛистГ1ДляВыгрузки_2017();
					ЗаполнитьЗначенияСвойств(СтрГ1, ЛистГ1.Значение);
					СтрГ1.УИДРодителя = УИДРодителя;
					СтрГ1.НомерРосОрг = "РО-" + Формат(СтрГ1.НомерРосОрг, "ЧЦ=5; ЧН=; ЧВН=; ЧГ=");
					Параметры.ДанныеМногостраничныхРазделов.ЛистГ1ДляВыгрузки.Добавить(СтрГ1);
				КонецЕсли;
			КонецЦикла;
			
			Для Каждого ЛистГ2 Из Параметры.ДанныеМногостраничныхРазделов.ЛистГ2 Цикл 
				Если ЛистГ2.Значение.УИДРодителя = Элт.Значение.УИД 
					И ЗначениеЗаполнено(ЛистГ2.Значение.НомерИО) Тогда 
					
					СтрГ2 = ПолучитьСтруктураЛистГ2ДляВыгрузки_2017();
					ЗаполнитьЗначенияСвойств(СтрГ2, ЛистГ2.Значение);
					СтрГ2.УИДРодителя = УИДРодителя;
					СтрГ2.НомерИО = "ИО-" + Формат(СтрГ2.НомерИО, "ЧЦ=5; ЧН=; ЧВН=; ЧГ=");
					Параметры.ДанныеМногостраничныхРазделов.ЛистГ2ДляВыгрузки.Добавить(СтрГ2);
				КонецЕсли;
			КонецЦикла;
		КонецЦикла;
	КонецЦикла;
КонецПроцедуры

Функция ПолучитьСтруктураЛистГ1ДляВыгрузки_2019()
	Возврат Новый Структура("УИД, УИДРодителя, НомерРосОрг, ОГРН, ИННЮЛ, КПП, НаимОрг, ПрПредДокОсв");
КонецФункции

Функция ПолучитьСтруктураЛистГ2ДляВыгрузки_2019()
	Рез = Новый Структура("УИД, УИДРодителя");
	Рез.Вставить("НомерИО");
	Рез.Вставить("НомерКИК");
	Рез.Вставить("НаимОргЛат");
	Рез.Вставить("СтрРег");
	Рез.Вставить("РегНомер");
	Рез.Вставить("КодНПРег");
	Рез.Вставить("АдрСтрРег");
	Возврат Рез;
КонецФункции

Функция ПолучитьСтруктураЛистГ00ДляВыгрузки_2019()
	Возврат Новый Структура("УИД, УИДРодителя, ДоляУчКосв, ДоляУчПрям, НомерУч");
КонецФункции

Функция ПолучитьСтруктураЛистГ0ДляВыгрузки_2019()
	Возврат Новый Структура("УИД, УИДРодителя, НомерПосл, ДоляКосвПослИт");
КонецФункции

Функция ПолучитьСтруктураЛистГДляВыгрузки_2019()
	Возврат Новый Структура("УИД, НомерКИК, НаимОрг, ДоляУчКосвИт");
КонецФункции

Процедура СформироватьПараметрыДляЛистовГ_2019(Параметры)
	УИДыЛистГ = Новый Соответствие;
	ВсеУчастникиЛистыГ = Новый Соответствие;
	Для Каждого Стр Из Параметры.ДанныеМногостраничныхРазделов.ЛистГ Цикл
		ПарамСтр = Стр.Значение;
		Если ЗначениеЗаполнено(ПарамСтр.НомерКИК) Тогда 
			Если ВсеУчастникиЛистыГ[ПарамСтр.НомерКИК] = Неопределено Тогда 
				ВсеУчастникиЛистыГ.Вставить(ПарамСтр.НомерКИК, Новый СписокЗначений);
				УИДыЛистГ.Вставить(ПарамСтр.НомерКИК, ПарамСтр.УИД);
			КонецЕсли;
			ВсеУчастникиЛистыГ[ПарамСтр.НомерКИК].Добавить(ПарамСтр);
		КонецЕсли;
	КонецЦикла;
	
	Параметры.ДанныеМногостраничныхРазделов.Вставить("ЛистГДляВыгрузки", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("ЛистГ0ДляВыгрузки", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("ЛистГ00ДляВыгрузки", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("ЛистГ1ДляВыгрузки", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("ЛистГ2ДляВыгрузки", Новый СписокЗначений);
	
	Для Каждого КЗ Из ВсеУчастникиЛистыГ Цикл
		Стр = ПолучитьСтруктураЛистГДляВыгрузки_2019();
		ЗаполнитьЗначенияСвойств(Стр, КЗ.Значение[0].Значение);
		Параметры.ДанныеМногостраничныхРазделов.ЛистГДляВыгрузки.Добавить(Стр);
		
		УИДРодителя = КЗ.Значение[0].Значение.УИД;
		Для Каждого Элт Из КЗ.Значение Цикл
			Стр = ПолучитьСтруктураЛистГ0ДляВыгрузки_2019();
			ЗаполнитьЗначенияСвойств(Стр, Элт.Значение);
			Стр.УИДРодителя = УИДРодителя;
			Стр.НомерПосл = Формат(Стр.НомерПосл, "ЧЦ=5; ЧН=; ЧВН=; ЧГ=");
			Параметры.ДанныеМногостраничныхРазделов.ЛистГ0ДляВыгрузки.Добавить(Стр);
			
			МнгСтрСтроки = Параметры.ДанныеДопСтрокБД.МнгСтр.НайтиСтроки(Новый Структура("УИД", Элт.Значение.УИД));
			Для Каждого СтрокаМнг Из МнгСтрСтроки Цикл
				СтрПодч = ПолучитьСтруктураЛистГ00ДляВыгрузки_2019();
				ЗаполнитьЗначенияСвойств(СтрПодч, СтрокаМнг);
				СтрПодч.УИДРодителя = Стр.УИД;
				Параметры.ДанныеМногостраничныхРазделов.ЛистГ00ДляВыгрузки.Добавить(СтрПодч);
			КонецЦикла;
			
			Для Каждого ЛистГ1 Из Параметры.ДанныеМногостраничныхРазделов.ЛистГ1 Цикл 
				Если ЛистГ1.Значение.УИДРодителя = Элт.Значение.УИД 
					И ЗначениеЗаполнено(ЛистГ1.Значение.НомерРосОрг) Тогда 
					
					СтрГ1 = ПолучитьСтруктураЛистГ1ДляВыгрузки_2019();
					ЗаполнитьЗначенияСвойств(СтрГ1, ЛистГ1.Значение);
					СтрГ1.УИДРодителя = УИДРодителя;
					СтрГ1.НомерРосОрг = "РО-" + Формат(СтрГ1.НомерРосОрг, "ЧЦ=5; ЧН=; ЧВН=; ЧГ=");
					Параметры.ДанныеМногостраничныхРазделов.ЛистГ1ДляВыгрузки.Добавить(СтрГ1);
				КонецЕсли;
			КонецЦикла;
			
			Для Каждого ЛистГ2 Из Параметры.ДанныеМногостраничныхРазделов.ЛистГ2 Цикл 
				Если Не ЛистГ2.Значение.Свойство("НомерИО")
					И ЛистГ2.Значение.Свойство("НомерКИК") Тогда 
					
					ЛистГ2.Значение.Вставить("НомерИО", ЛистГ2.Значение.НомерКИК);
				КонецЕсли;
				Если ЛистГ2.Значение.УИДРодителя = Элт.Значение.УИД 
					И ЗначениеЗаполнено(ЛистГ2.Значение.НомерИО) Тогда 
					
					СтрГ2 = ПолучитьСтруктураЛистГ2ДляВыгрузки_2019();
					ЗаполнитьЗначенияСвойств(СтрГ2, ЛистГ2.Значение);
					СтрГ2.УИДРодителя = УИДРодителя;
					Если СтрГ2.Свойство("НомерИО") Тогда 
						СтрГ2.НомерИО = "ИО-" + Формат(СтрГ2.НомерИО, "ЧЦ=5; ЧН=; ЧВН=; ЧГ=");
					КонецЕсли;
					Параметры.ДанныеМногостраничныхРазделов.ЛистГ2ДляВыгрузки.Добавить(СтрГ2);
				КонецЕсли;
			КонецЦикла;
		КонецЦикла;
	КонецЦикла;
КонецПроцедуры

Функция СтраницаЗаполнена(Данные)
	Если ТипЗнч(Данные) = Тип("Структура") Тогда 
		Для Каждого КЗ Из Данные Цикл 
			Если ТипЗнч(КЗ.Значение) = Тип("УникальныйИдентификатор") Тогда 
				Продолжить;
			КонецЕсли;
			Если ЗначениеЗаполнено(КЗ.Значение) Тогда 
				Возврат Истина
			КонецЕсли;
		КонецЦикла;
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Истина;
КонецФункции

Процедура ДополнитьПараметры_2017(Параметры)
	СформироватьПараметрыДляЛистовГ_2017(Параметры);
	
	Если Не ЗначениеЗаполнено(Параметры.ДанныеУведомления.Титульная.НомерКорректировки) Тогда 
		Параметры.ДанныеУведомления.Титульная.НомерКорректировки = "0";
	КонецЕсли;
	
	Если Параметры.ДанныеУведомления.Свойство("ЛистСведФЛ") Тогда 
		ПарамСтр = Параметры.ДанныеУведомления.ЛистСведФЛ;
		ПарамСтр.Вставить("ДатаДок", Формат(ПарамСтр.ДатаДокДата, "ДФ=dd.MM.yyyy; ДП=' '"));
		ПарамСтр.Вставить("ДатаДок1", Формат(ПарамСтр.ДатаДокДата1, "ДФ=dd.MM.yyyy; ДП=' '"));
		ПарамСтр.Вставить("ДатаРожд", Формат(ПарамСтр.ДатаРождДата, "ДФ=dd.MM.yyyy; ДП=' '"));
	КонецЕсли;
	
	Для Каждого Стр Из Параметры.ДанныеМногостраничныхРазделов.ЛистА Цикл
		ПарамСтр = Стр.Значение;
		Если СтраницаЗаполнена(ПарамСтр) Тогда
			ПарамСтр.Вставить("НомерКИК", "ИО-" + Формат(ПарамСтр.НомерКИКЧисло, "ЧЦ=5; ЧВН=; ЧГ="));
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Стр Из Параметры.ДанныеМногостраничныхРазделов.ЛистБ Цикл
		ПарамСтр = Стр.Значение;
		Если СтраницаЗаполнена(ПарамСтр) Тогда
			ПарамСтр.Вставить("НомерКИК", "ИС-" + Формат(ПарамСтр.НомерКИКЧисло, "ЧЦ=5; ЧВН=; ЧГ="));
			ПарамСтр.Вставить("ДатаУчрИС", Формат(ПарамСтр.ДатаУчрИСДата, "ДФ=dd.MM.yyyy; ДП=' '"));
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Стр Из Параметры.ДанныеМногостраничныхРазделов.ЛистВ Цикл
		ПарамСтр = Стр.Значение;
		Если СтраницаЗаполнена(ПарамСтр) Тогда
			ПарамСтр.Вставить("ДатаПослДнПер", Формат(ПарамСтр.ДатаПослДнПерДата, "ДФ=dd.MM.yyyy; ДП=' '"));
			ПарамСтр.Вставить("ДатаСостФО", Формат(ПарамСтр.ДатаСостФОДата, "ДФ=dd.MM.yyyy; ДП=' '"));
			ПарамСтр.Вставить("ДатаПослДнПерГл25", Формат(ПарамСтр.ДатаПослДнПерГл25Дата, "ДФ=dd.MM.yyyy; ДП=' '"));
			ПарамСтр.Вставить("ДатаСостАудЗакл", Формат(ПарамСтр.ДатаСостАудЗаклДата, "ДФ=dd.MM.yyyy; ДП=' '"));
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

Процедура ДополнитьПараметры_2019(Параметры)
	СформироватьПараметрыДляЛистовГ_2019(Параметры);
	
	Если Не ЗначениеЗаполнено(Параметры.ДанныеУведомления.Титульная.НомерКорректировки) Тогда 
		Параметры.ДанныеУведомления.Титульная.НомерКорректировки = "0";
	КонецЕсли;
	
	Если Параметры.ДанныеУведомления.Свойство("ЛистСведФЛ") Тогда 
		ПарамСтр = Параметры.ДанныеУведомления.ЛистСведФЛ;
		ПарамСтр.Вставить("ДатаДок", Формат(ПарамСтр.ДатаДокДата, "ДФ=dd.MM.yyyy; ДП=' '"));
		ПарамСтр.Вставить("ДатаДок1", Формат(ПарамСтр.ДатаДокДата1, "ДФ=dd.MM.yyyy; ДП=' '"));
		ПарамСтр.Вставить("ДатаРожд", Формат(ПарамСтр.ДатаРождДата, "ДФ=dd.MM.yyyy; ДП=' '"));
	КонецЕсли;
	
	Для Каждого Стр Из Параметры.ДанныеМногостраничныхРазделов.ЛистА Цикл
		ПарамСтр = Стр.Значение;
		Если СтраницаЗаполнена(ПарамСтр) Тогда
			ПарамСтр.Вставить("НомерКИК", "ИО-" + Формат(ПарамСтр.НомерКИКЧисло, "ЧЦ=5; ЧВН=; ЧГ="));
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Стр Из Параметры.ДанныеМногостраничныхРазделов.ЛистБ Цикл
		ПарамСтр = Стр.Значение;
		Если СтраницаЗаполнена(ПарамСтр) Тогда
			ПарамСтр.Вставить("НомерКИК", "ИС-" + Формат(ПарамСтр.НомерКИКЧисло, "ЧЦ=5; ЧВН=; ЧГ="));
			ПарамСтр.Вставить("ДатаУчрИС", Формат(ПарамСтр.ДатаУчрИСДата, "ДФ=dd.MM.yyyy; ДП=' '"));
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Стр Из Параметры.ДанныеМногостраничныхРазделов.ЛистВ Цикл
		ПарамСтр = Стр.Значение;
		Если СтраницаЗаполнена(ПарамСтр) Тогда
			ПарамСтр.Вставить("ДатаПослДнПер", Формат(ПарамСтр.ДатаПослДнПерДата, "ДФ=dd.MM.yyyy; ДП=' '"));
			ПарамСтр.Вставить("ДатаСостФО", Формат(ПарамСтр.ДатаСостФОДата, "ДФ=dd.MM.yyyy; ДП=' '"));
			ПарамСтр.Вставить("ДатаПослДнПерГл25", Формат(ПарамСтр.ДатаПослДнПерГл25Дата, "ДФ=dd.MM.yyyy; ДП=' '"));
			ПарамСтр.Вставить("ДатаСостАудЗакл", Формат(ПарамСтр.ДатаСостАудЗаклДата, "ДФ=dd.MM.yyyy; ДП=' '"));
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

Процедура ВывестиЧислоНаПечать(Число, ПрефиксПараметра, ОбластиМакета) Экспорт
	Инд = 1;
	ИмяПараметра = ПрефиксПараметра + "_1";
	Область = ОбластиМакета.Найти(ИмяПараметра);
	ЗнаковПослеЗапятой = 0;
	ЗнаковДоЗапятой = 1;
	
	Если Область = Неопределено 
		Или Область.Лево <> Область.Право Тогда 
		Возврат;
	КонецЕсли;
	
	Координата = Область.Лево;
	
	Пока Истина Цикл 
		Инд = Инд + 1;
		ИмяПараметра = ПрефиксПараметра + "_" + Инд;
		Область = ОбластиМакета.Найти(ИмяПараметра);
		Если Область = Неопределено 
			Или Область.Лево <> Область.Право Тогда 
			Прервать;
		КонецЕсли;
		
		ЗнаковДоЗапятой = ЗнаковДоЗапятой + 1;
		Если ЗнаковПослеЗапятой > 0 
			Или Область.Лево > Координата + 1 Тогда 
			
			ЗнаковПослеЗапятой = ЗнаковПослеЗапятой + 1;
			ЗнаковДоЗапятой = ЗнаковДоЗапятой - 1;
		КонецЕсли;
		
		Координата = Координата + 1;
	КонецЦикла;
	
	Если Не ЗначениеЗаполнено(Число) Тогда 
		Ч = "-------------------------------------------------";
	Иначе
		Ч = Лев(Формат(Цел(Число), "ЧН=0; ЧГ=") + "-------------------------------------------------", ЗнаковДоЗапятой);
		Ч2 = Прав("00000000000000000000000000000000000000000000" + Формат(   (Число - Цел(Число)) * Pow(10, ЗнаковПослеЗапятой), "ЧГ="), ЗнаковПослеЗапятой);
		Пока Прав(Ч2, 1) = "0" Цикл 
			Ч2 = Лев(Ч2, СтрДлина(Ч2) - 1);
		КонецЦикла;
		Ч = Ч + Ч2 + "-------------------------------------------------";
	КонецЕсли;
	УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать(Ч, ПрефиксПараметра, ОбластиМакета);
КонецПроцедуры

Процедура ДополнитьТекстовоеПредставление(ПечатнаяФорма, ТекстовоеПредставлениеДатыПодписи, ИмяОбласти = "ТекстовоеПредставлениеДатыПодписи")
	Обл = ПечатнаяФорма.Области.Найти(ИмяОбласти);
	Если Не Обл = Неопределено Тогда 
		Обл.Текст = ТекстовоеПредставлениеДатыПодписи;
	КонецЕсли;
КонецПроцедуры

Процедура НапечататьСтроку(Объект, СтруктураПараметров, Листы, СтрПарам, ПечатнаяФорма, НомСтр, ИННКПП)
	МакетыПФ = СтрПарам.МакетыПФ;
	ИмяМакета = СтрПарам.ИмяМакета;
	
	Если Не ЗначениеЗаполнено(МакетыПФ) И Не ЗначениеЗаполнено(ИмяМакета) Тогда 
		Для Каждого СтрПодч Из СтрПарам.Строки Цикл
			НапечататьСтроку(Объект, СтруктураПараметров, Листы, СтрПодч, ПечатнаяФорма, НомСтр, ИННКПП);
		КонецЦикла;
		
		Возврат;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(МакетыПФ) Тогда 
		МассивИменМакетов = СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок(МакетыПФ, ";");
	Иначе 
		МассивИменМакетов = Новый Массив;
		МассивИменМакетов.Добавить("Печать_" + ИмяМакета);
	КонецЕсли;
	
	Если СтруктураПараметров.ДанныеУведомления.Свойство(СтрПарам.ИДНаименования) Тогда 
		Для Каждого ИмяМакета Из МассивИменМакетов Цикл 
			СтруктураДанныхСтраницы = СтруктураПараметров.ДанныеУведомления[СтрПарам.ИДНаименования];
			Если СтрПарам.ИДНаименования = "ЛистСведФЛ" И ЗначениеЗаполнено(ИННКПП.ИНН) Тогда
				Продолжить;
			КонецЕсли;
			
			Если УведомлениеОСпецрежимахНалогообложенияКлиентСервер.СтраницаЗаполнена(СтруктураДанныхСтраницы) Тогда
				УведомлениеОСпецрежимахНалогообложения.НапечататьСтруктуру(Объект.ИмяОтчета, СтруктураДанныхСтраницы, НомСтр,
					УведомлениеОСпецрежимахНалогообложения.ПустойМакетИзСписка(Объект, СтруктураПараметров.СписокИспользуемыхМакетов, ИмяМакета), ПечатнаяФорма, ИННКПП);
				Если СтрПарам.ИДНаименования = "Титульная" Тогда
					УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать(Объект.ПодписантФамилия, "ПодпФ", ПечатнаяФорма.Области, "-");
					УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать(Объект.ПодписантИмя, "ПодпИ", ПечатнаяФорма.Области, "-");
					УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать(Объект.ПодписантОтчество, "ПодпО", ПечатнаяФорма.Области, "-");
					ПечатнаяФорма.Области.ЭлАдрес.Текст = СтруктураДанныхСтраницы.ЭлАдрес;
					Если Не ЗначениеЗаполнено(СтруктураДанныхСтраницы.НомерКорректировки) Тогда
						УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать("0--", "НомерКорректировки", ПечатнаяФорма.Области);
					КонецЕсли;
				КонецЕсли;
				ДополнитьТекстовоеПредставление(ПечатнаяФорма, ИННКПП.ТекстовоеПредставлениеДатыПодписи);
				ДополнитьТекстовоеПредставление(ПечатнаяФорма, ИННКПП.ФамилияВерх, "ФамилияВерх");
				ДополнитьТекстовоеПредставление(ПечатнаяФорма, ИННКПП.ИмяВерх, "ИмяВерх");
				ДополнитьТекстовоеПредставление(ПечатнаяФорма, ИННКПП.ОтчествоВерх, "ОтчествоВерх");
				УведомлениеОСпецрежимахНалогообложения.ПоложитьПФВСписокЛистов(Объект, Листы, ПечатнаяФорма, НомСтр);
			КонецЕсли;
		КонецЦикла;
	ИначеЕсли СтруктураПараметров.ДанныеМногостраничныхРазделов.Свойство(СтрПарам.ИДНаименования) Тогда 
		Для Каждого МнгСтр Из СтруктураПараметров.ДанныеМногостраничныхРазделов[СтрПарам.ИДНаименования] Цикл
			Если СтрПарам.УИД <> МнгСтр.Значение.УИД Тогда 
				Продолжить;
			КонецЕсли;
			
			Для Каждого ИмяМакета Из МассивИменМакетов Цикл 
				Если УведомлениеОСпецрежимахНалогообложенияКлиентСервер.СтраницаЗаполнена(МнгСтр.Значение) Тогда 
					Если СтрПарам.ИДНаименования = "ЛистГ" Тогда
						ДопСтроки = СтруктураПараметров.ДанныеДопСтрокБД.МнгСтр.НайтиСтроки(Новый Структура("УИД", МнгСтр.Значение.УИД));
						
						Для Инд = 0 По ДопСтроки.ВГраница() Цикл 
							Если Инд%8 = 0 Тогда 
								УведомлениеОСпецрежимахНалогообложения.НапечататьСтруктуру(Объект.ИмяОтчета, МнгСтр.Значение, НомСтр,
									УведомлениеОСпецрежимахНалогообложения.ПустойМакетИзСписка(Объект, СтруктураПараметров.СписокИспользуемыхМакетов, ИмяМакета), ПечатнаяФорма, ИННКПП);
							КонецЕсли;
							
							СтрокаНаЛисте = "_" + (Инд%8 + 1);
							ТекМнгСтрока = ДопСтроки[Инд];
							УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать(ТекМнгСтрока.НомерУч, "НомерУч"+СтрокаНаЛисте, ПечатнаяФорма.Области, "-");
							ВывестиЧислоНаПечать(ТекМнгСтрока.ДоляУчПрям, "ДоляУчПрям"+СтрокаНаЛисте, ПечатнаяФорма.Области);
							ВывестиЧислоНаПечать(ТекМнгСтрока.ДоляУчКосв, "ДоляУчКосв"+СтрокаНаЛисте, ПечатнаяФорма.Области);
							
							Если Инд%8 = 7 Или Инд = ДопСтроки.ВГраница() Тогда
								Если Инд = ДопСтроки.ВГраница() И Инд%8 <> 7 Тогда 
									Пока Инд%8 <> 7 Цикл
										Инд = Инд + 1;
										СтрокаНаЛисте = "_" + (Инд%8 + 1);
										УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать("-------------", "НомерУч"+СтрокаНаЛисте, ПечатнаяФорма.Области, "-");
										ВывестиЧислоНаПечать(0, "ДоляУчПрям"+СтрокаНаЛисте, ПечатнаяФорма.Области);
										ВывестиЧислоНаПечать(0, "ДоляУчКосв"+СтрокаНаЛисте, ПечатнаяФорма.Области);
									КонецЦикла;
								КонецЕсли;
								ДополнитьТекстовоеПредставление(ПечатнаяФорма, ИННКПП.ТекстовоеПредставлениеДатыПодписи);
								ДополнитьТекстовоеПредставление(ПечатнаяФорма, ИННКПП.ФамилияВерх, "ФамилияВерх");
								ДополнитьТекстовоеПредставление(ПечатнаяФорма, ИННКПП.ИмяВерх, "ИмяВерх");
								ДополнитьТекстовоеПредставление(ПечатнаяФорма, ИННКПП.ОтчествоВерх, "ОтчествоВерх");
								УведомлениеОСпецрежимахНалогообложения.ПоложитьПФВСписокЛистов(Объект, Листы, ПечатнаяФорма, НомСтр);
								ПечатнаяФорма = УведомлениеОСпецрежимахНалогообложения.НовыйПустойЛист();
							КонецЕсли;
						КонецЦикла;
					Иначе 
						УведомлениеОСпецрежимахНалогообложения.НапечататьСтруктуру(Объект.ИмяОтчета, МнгСтр.Значение, НомСтр,
							УведомлениеОСпецрежимахНалогообложения.ПустойМакетИзСписка(Объект, СтруктураПараметров.СписокИспользуемыхМакетов, ИмяМакета), ПечатнаяФорма, ИННКПП);
						Если СтрПарам.ИДНаименования = "ЛистА" Или СтрПарам.ИДНаименования = "ЛистБ" Тогда 
							УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать(Формат(МнгСтр.Значение.НомерКИКЧисло, "ЧЦ=5; ЧН=; ЧВН=; ЧГ="), "НомерКИКЧисло", ПечатнаяФорма.Области);
						ИначеЕсли СтрПарам.ИДНаименования = "ЛистГ2" Тогда 
							Если МнгСтр.Значение.Свойство("НомерИО") Тогда 
								УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать(Формат(МнгСтр.Значение.НомерИО, "ЧЦ=5; ЧН=; ЧВН=; ЧГ="), "НомерИО", ПечатнаяФорма.Области);
							КонецЕсли;
						ИначеЕсли СтрПарам.ИДНаименования = "ЛистГ1" Тогда 
							УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать(Формат(МнгСтр.Значение.НомерРосОрг, "ЧЦ=5; ЧН=; ЧВН=; ЧГ="), "НомерРосОрг", ПечатнаяФорма.Области);
						КонецЕсли;
						Если СтрПарам.ИДНаименования = "ЛистА" Тогда 
							Если Не ЗначениеЗаполнено(МнгСтр.Значение.ДоляПрямУч) Тогда 
								УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать("0-------------------------------", "ДоляПрямУч", ПечатнаяФорма.Области);
							КонецЕсли;
							Если Не ЗначениеЗаполнено(МнгСтр.Значение.ДоляКосУч) Тогда 
								УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать("0-------------------------------", "ДоляКосУч", ПечатнаяФорма.Области);
							КонецЕсли;
							Если Не ЗначениеЗаполнено(МнгСтр.Значение.ДоляСмешУч) Тогда 
								УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать("0-------------------------------", "ДоляСмешУч", ПечатнаяФорма.Области);
							КонецЕсли;
						КонецЕсли;
						ДополнитьТекстовоеПредставление(ПечатнаяФорма, ИННКПП.ТекстовоеПредставлениеДатыПодписи);
						ДополнитьТекстовоеПредставление(ПечатнаяФорма, ИННКПП.ФамилияВерх, "ФамилияВерх");
						ДополнитьТекстовоеПредставление(ПечатнаяФорма, ИННКПП.ИмяВерх, "ИмяВерх");
						ДополнитьТекстовоеПредставление(ПечатнаяФорма, ИННКПП.ОтчествоВерх, "ОтчествоВерх");
						УведомлениеОСпецрежимахНалогообложения.ПоложитьПФВСписокЛистов(Объект, Листы, ПечатнаяФорма, НомСтр);
					КонецЕсли;
				КонецЕсли;
			КонецЦикла;
			
			Для Каждого СтрПодч Из СтрПарам.Строки Цикл
				НапечататьСтроку(Объект, СтруктураПараметров, Листы, СтрПодч, ПечатнаяФорма, НомСтр, ИННКПП);
			КонецЦикла;
		КонецЦикла;
	КонецЕсли;
КонецПроцедуры

Функция СформироватьСписокЛистовФорма2017_1(Объект) Экспорт
	СписокИспользуемыхМакетов = УведомлениеОСпецрежимахНалогообложения.ЗагрузитьМакетыИзАрхива(Объект, "БланкиПечати_");
	Листы = Новый СписокЗначений;
	
	ПечатнаяФорма = УведомлениеОСпецрежимахНалогообложения.НовыйПустойЛист();
	СтруктураПараметров = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Объект.Ссылка, "ДанныеУведомления").Получить();
	СтруктураПараметров.Вставить("СписокИспользуемыхМакетов", СписокИспользуемыхМакетов);
	ИННКПП = УведомлениеОСпецрежимахНалогообложения.ТиповаяСтруктураИННКППДляПечати(Объект, СтруктураПараметров.ДанныеУведомления.Титульная);
	
	НомСтр = 0;
	Для Каждого Стр Из СтруктураПараметров.ДеревоСтраниц.Строки Цикл
		НапечататьСтроку(Объект, СтруктураПараметров, Листы, Стр, ПечатнаяФорма, НомСтр, ИННКПП);
	КонецЦикла;
	
	Возврат Листы;
КонецФункции

Функция ПроверитьДокументСВыводомВТаблицу_Форма2017_1(Данные, УникальныйИдентификатор)
	ТаблицаОшибок = Новый СписокЗначений;
	ОбщегоНазначения.СообщитьПользователю("Проверка выгрузки редакции 2016 года не поддерживается");
	Возврат ТаблицаОшибок;
КонецФункции

Функция ПроверитьДокументСВыводомВТаблицу_Форма2019_1(Данные, УникальныйИдентификатор)
	ТаблицаОшибок = Новый СписокЗначений;
	Данные["АрхивМакетов"] = "ЭкранныеФормы";
	Титульная = Данные.ДанныеУведомления.Титульная;
	
	Если ЗначениеЗаполнено(Титульная.ФормРеорг) И Титульная.ФормРеорг <> "0"  Тогда
		УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИНН(Титульная, ТаблицаОшибок, "Титульная", "ИННРеорг", Истина, Истина);
		УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаКПП(Титульная, ТаблицаОшибок, "Титульная", "КППРеорг", Истина);
	КонецЕсли;
	
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаГода(Титульная, ТаблицаОшибок, "Титульная", "НалПер", Истина, , 2019, 2021);
	Если Не ЗначениеЗаполнено(Титульная.ПрНП) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан признак налогоплательщика на титульной странице", "Титульная", "ПрНП"));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Титульная.ДАТА_ПОДПИСИ) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указана дата подписи", "Титульная", "ДАТА_ПОДПИСИ"));
	КонецЕсли;
	Если ЗначениеЗаполнено(Титульная.ФИО_РУКОВОДИТЕЛЯ_ПРЕДСТАВИТЕЛЯ)
		И (Не ЗначениеЗаполнено(Данные.ПодписантФамилия) Или Не ЗначениеЗаполнено(Данные.ПодписантИмя))Тогда 
		
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указаны фамилия/имя представителя", "Титульная", "ФИО_РУКОВОДИТЕЛЯ_ПРЕДСТАВИТЕЛЯ"));
	КонецЕсли;
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаКодаНО(Титульная.КодНО, ТаблицаОшибок, "Титульная", "КодНО");
	Если Не ЗначениеЗаполнено(Титульная.ПРИЗНАК_НП_ПОДВАЛ) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан признак подписанта", "Титульная", "ПРИЗНАК_НП_ПОДВАЛ"));
	КонецЕсли;
	Если Титульная.ПРИЗНАК_НП_ПОДВАЛ = "2" Или Титульная.ПРИЗНАК_НП_ПОДВАЛ = "3" Или Титульная.ПРИЗНАК_НП_ПОДВАЛ = "4" Тогда 
		Если Не ЗначениеЗаполнено(Данные.ПодписантИмя) Или Не ЗначениеЗаполнено(Данные.ПодписантФамилия) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан подписант", "Титульная", "ФИО_РУКОВОДИТЕЛЯ_ПРЕДСТАВИТЕЛЯ"));
		КонецЕсли;
	КонецЕсли;
	Если (Титульная.ПРИЗНАК_НП_ПОДВАЛ = "2" Или Титульная.ПРИЗНАК_НП_ПОДВАЛ = "4")
		И Не ЗначениеЗаполнено(Титульная.НаимДок) Тогда 
		
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан документ представителя", "Титульная", "НаимДок"));
	КонецЕсли;
	
	Если РегламентированнаяОтчетностьПереопределяемый.ЭтоЮридическоеЛицо(Данные.Организация) Тогда
		УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИННКПП(Истина, Титульная, ТаблицаОшибок, "Титульная");
	Иначе
		Если ЗначениеЗаполнено(Титульная.ИНН) Тогда 
			УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИНН(Титульная, ТаблицаОшибок, "Титульная", "ИНН", Истина, Ложь);
		Иначе
			ЛистСведФЛ = Данные.ДанныеУведомления.ЛистСведФЛ;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.ДатаРождДата) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указана дата рождения", "ЛистСведФЛ", "ДатаРождДата"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.МестоРожд) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указано место рождения", "ЛистСведФЛ", "МестоРожд"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.ПрГражд) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указан признак гражданства", "ЛистСведФЛ", "ПрГражд"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.КодВидДок) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указан код вида документа", "ЛистСведФЛ", "КодВидДок"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.СерНомДок) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указана серия и номер документа", "ЛистСведФЛ", "СерНомДок"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.ДатаДокДата) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указана дата выдачи документа", "ЛистСведФЛ", "ДатаДокДата"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.ВыдДок) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указан орган, выдавший документ", "ЛистСведФЛ", "ВыдДок"));
			КонецЕсли;
			
			Если ЗначениеЗаполнено(ЛистСведФЛ.КодВидДок1) 
				Или ЗначениеЗаполнено(ЛистСведФЛ.СерНомДок1) 
				Или ЗначениеЗаполнено(ЛистСведФЛ.ДатаДокДата1) 
				Или ЗначениеЗаполнено(ЛистСведФЛ.ВыдДок1) Тогда 
				
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.КодВидДок1) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указан код вида документа", "ЛистСведФЛ", "КодВидДок1"));
				КонецЕсли;
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.СерНомДок1) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указана серия и номер документа", "ЛистСведФЛ", "СерНомДок1"));
				КонецЕсли;
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.ДатаДокДата1) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указана дата выдачи документа", "ЛистСведФЛ", "ДатаДокДата1"));
				КонецЕсли;
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.ВыдДок1) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указан орган, выдавший документ", "ЛистСведФЛ", "ВыдДок1"));
				КонецЕсли;
			КонецЕсли;
			
			Если ЗначениеЗаполнено(ЛистСведФЛ.ОКСМ) 
				Или ЗначениеЗаполнено(ЛистСведФЛ.АдрИно) Тогда 
				
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.ОКСМ) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указан код государства", "ЛистСведФЛ", "ОКСМ"));
				КонецЕсли;
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.АдрИно) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указан адрес", "ЛистСведФЛ", "АдрИно"));
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИНН(Титульная, ТаблицаОшибок, "Титульная", "ИННФЛ", Ложь, Ложь);
	ЛистыАБ = 0;
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистА Цикл 
		Если ЗначениеЗаполнено(Лист.Значение.НомерКИКЧисло) Тогда 
			ЛистыАБ = 1;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистБ Цикл 
		Если ЗначениеЗаполнено(Лист.Значение.НомерКИКЧисло) Тогда 
			ЛистыАБ = 1;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если ЛистыАБ = 0 Тогда
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Отсутствуют Листы А и Листы Б", "ЛистА", "НомерКИКЧисло", Данные.ДанныеМногостраничныхРазделов.ЛистА[0].Значение.УИД));
	КонецЕсли;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистА Цикл
		ЛистТек = Лист.Значение;
		Если ЗначениеЗаполнено(ЛистТек.НомерКИКЧисло) Тогда
			Если (Не ЗначениеЗаполнено(ЛистТек.ДоляПрямУч) И Не ЗначениеЗаполнено(ЛистТек.ДоляКосУч)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Необходимо заполнить пункт 2.2 или 2.3 на листе А", "ЛистА", "ДоляПрямУч", ЛистТек.УИД));
			КонецЕсли;
			Если (ЗначениеЗаполнено(ЛистТек.ДоляПрямУч) И ЗначениеЗаполнено(ЛистТек.ДоляКосУч) И (Не ЗначениеЗаполнено(ЛистТек.ДоляСмешУч))) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 2.4 на листе А", "ЛистА", "ДоляСмешУч", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.ПрСамПризн)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 1.3 на листе А", "ЛистА", "ПрСамПризн", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.НаимОргЛат)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено наименование организации на листе А", "ЛистА", "НаимОргЛат", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.СтрРег)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена страна регистрации на листе А", "ЛистА", "СтрРег", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.ДатаРегИО)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена дата регистрации на листе А", "ЛистА", "ДатаРегИО", ЛистТек.УИД));
			КонецЕсли;
			
			Для Инд = 1 По 5 Цикл
				СтрИмя = "Код" + (100 + Инд);
				Если (Не ЗначениеЗаполнено(Лист.Значение[СтрИмя])) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не заполнен пункт "+(100+Инд)+" на листе А", "ЛистА", СтрИмя, ЛистТек.УИД));
				КонецЕсли;
			КонецЦикла;
			Для Инд = 1 По 5 Цикл
				СтрИмя = "Код" + (200 + Инд);
				Если (Не ЗначениеЗаполнено(Лист.Значение[СтрИмя])) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не заполнен пункт "+(200+Инд)+" на листе А", "ЛистА", СтрИмя, ЛистТек.УИД));
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистБ Цикл
		ЛистТек = Лист.Значение;
		Если ЗначениеЗаполнено(ЛистТек.НомерКИКЧисло) Тогда
			Если (Не ЗначениеЗаполнено(ЛистТек.ПрСамПризн)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт признак самостоятельного признания на листе Б", "ЛистБ", "ПрСамПризн", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.НаимОргЛат)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено наименование организации листе Б", "ЛистБ", "НаимОргЛат", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.НаимДокУчр)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено наименование документа на листе Б", "ЛистБ", "НаимДокУчр", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.ДатаУчрИСДата)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена дата учреждения на листе Б", "ЛистБ", "ДатаУчрИСДата", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.СтрРег)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен код государства листе Б", "ЛистБ", "СтрРег", ЛистТек.УИД));
			КонецЕсли;
			Для Инд = 6 По 12 Цикл
				СтрИмя = "Код" + (100 + Инд);
				Если (Не ЗначениеЗаполнено(Лист.Значение[СтрИмя])) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не заполнен пункт "+(100+Инд)+" на листе Б", "ЛистБ", СтрИмя, ЛистТек.УИД));
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистВ Цикл
		ЛистТек = Лист.Значение;
		Если ЗначениеЗаполнено(ЛистТек.НомерКИК) Тогда
			Если (Не ЗначениеЗаполнено(ЛистТек.ПорОпрПрКИК)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 2 на листе В", "ЛистВ", "ПорОпрПрКИК", ЛистТек.УИД));
			ИначеЕсли ЛистТек.ПорОпрПрКИК <> "1" И ЛистТек.ПорОпрПрКИК <> "2" Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Пункт 2 на листе В должен быть равен 1 или 2", "ЛистВ", "ПорОпрПрКИК", ЛистТек.УИД));
			КонецЕсли;
			Если ЛистТек.ПорОпрПрКИК = "1" И Не ЗначениеЗаполнено(ЛистТек.ДатаПослДнПерДата) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 3 на листе В при значении п.2 = 1", "ЛистВ", "ДатаПослДнПерДата", ЛистТек.УИД));
			КонецЕсли;
			Если ЛистТек.ПорОпрПрКИК = "1" И Не ЗначениеЗаполнено(ЛистТек.ДатаСостФОДата) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 4 на листе В при значении п.2 = 1", "ЛистВ", "ДатаСостФОДата", ЛистТек.УИД));
			КонецЕсли;
			Если ЛистТек.ПорОпрПрКИК = "2" И Не ЗначениеЗаполнено(ЛистТек.ДатаПослДнПерГл25Дата) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 5 на листе В при значении п.2 = 2", "ЛистВ", "ДатаПослДнПерГл25Дата", ЛистТек.УИД));
			КонецЕсли;
			Для Инд = 1 По 9 Цикл
				СтрИмя = "Код100" + Инд;
				Если (Не ЗначениеЗаполнено(Лист.Значение[СтрИмя])) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не заполнен пункт 100"+Инд+" на листе В", "ЛистВ", СтрИмя, ЛистТек.УИД));
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистГ Цикл
		ЛистТек = Лист.Значение;
		Если ЗначениеЗаполнено(ЛистТек.НомерКИК) Тогда
			
			Если Не ЗначениеЗаполнено(ЛистТек.НаимОрг) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено название организации", "ЛистГ", "НаимОрг", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.ДоляУчКосвИт) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена доля участия", "ЛистГ", "ДоляУчКосвИт", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.НомерПосл) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен номер последовательности", "ЛистГ", "НомерПосл", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.ДоляКосвПослИт) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена доля участия в последовательности", "ЛистГ", "ДоляКосвПослИт", ЛистТек.УИД));
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистГ1 Цикл
		Если ЗначениеЗаполнено(Лист.Значение.НомерРосОрг) Тогда 
			ЛистТек = Лист.Значение;
			Если ЗначениеЗаполнено(ЛистТек.ОГРН) 
				И Не РегламентированныеДанныеКлиентСервер.ОГРНСоответствуетТребованиям(ЛистТек.ОГРН, Истина, "") Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен/неправильно заполнен ОГРН на листе Г1", "ЛистГ1", "ОГРН", ЛистТек.УИД));
			КонецЕсли;
			УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИНН(ЛистТек, ТаблицаОшибок, "ЛистГ1", "ИННЮЛ", Истина, Истина);
			УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаКПП(ЛистТек, ТаблицаОшибок, "ЛистГ1", "КПП", Истина);
			Если Не ЗначениеЗаполнено(ЛистТек.НаимОрг) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено наименование организации на листе Г1", "ЛистГ1", "НаимОрг", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.ПрПредДокОсв) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен признак на листе Г1", "ЛистГ1", "ПрПредДокОсв", ЛистТек.УИД));
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистГ2 Цикл
		Если ЗначениеЗаполнено(Лист.Значение.НомерИО) Тогда 
			ЛистТек = Лист.Значение;
			Если Не ЗначениеЗаполнено(ЛистТек.НаимОргЛат) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено наименование организации латиницей на листе Г2", "ЛистГ2", "НаимОргЛат", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.СтрРег) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена страна регистрации на листе Г2/", "ЛистГ2", "СтрРег", ЛистТек.УИД));
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	ВсеУИД = Новый Соответствие;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр Цикл
		ВсеУИД.Вставить(Стр.УИД);
	КонецЦикла;
	
	Для Каждого КЗ Из ВсеУИД Цикл 
		Строки = Данные.ДанныеДопСтрокБД.МнгСтр.НайтиСтроки(Новый Структура("УИД", КЗ.Ключ));
		Инд = 0;
		Для Каждого Стр Из Строки Цикл 
			Инд = Инд + 1;
			Если ЗначениеЗаполнено(Стр.НомерУч) И Не (ЗначениеЗаполнено(Стр.ДоляУчКосв) Или ЗначениеЗаполнено(Стр.ДоляУчПрям)) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"В свободных строках листа Г должен быть заполнен пункт 3.2 или 3.3", "ЛистГ", "ДоляУчКосв___" + Формат(Инд, "ЧГ="), КЗ.Ключ));
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	
	УведомлениеОСпецрежимахНалогообложения.ПроверкаДатВУведомлении(Данные, ТаблицаОшибок);
	Возврат ТаблицаОшибок;
КонецФункции

Функция ПроверитьДокументСВыводомВТаблицу_Форма2021_1(Данные, УникальныйИдентификатор)
	ТаблицаОшибок = Новый СписокЗначений;
	Данные["АрхивМакетов"] = "ЭкранныеФормы";
	УведомлениеОСпецрежимахНалогообложения.ПроверкаАктуальностиФормыПриВыгрузке(
		Данные.Объект.ИмяФормы, ТаблицаОшибок, ПолучитьТаблицуФорм());
	СпискиВыбора = УведомлениеОСпецрежимахНалогообложения.СформироватьСпискиВыбораДляПроверкиВыгрузки(
		Данные.Объект.ИмяОтчета, "СпискиВыбора2021_1", "ЭкранныеФормы");
	Титульная = Данные.ДанныеУведомления.Титульная;
	
	Если ЗначениеЗаполнено(Титульная.ФормРеорг)  Тогда
		УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИНН(Титульная, ТаблицаОшибок, "Титульная", "ИННРеорг", Истина, Истина);
		УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаКПП(Титульная, ТаблицаОшибок, "Титульная", "КППРеорг", Истина);
	Иначе
		Если ЗначениеЗаполнено(Титульная.ИННРеорг) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"ИНН реорганизованной организации не указывается при отсутствии реорганизации", "Титульная", "ИННРеорг"));
		КонецЕсли;
		Если ЗначениеЗаполнено(Титульная.КППРеорг) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"КПП реорганизованной организации не указывается при отсутствии реорганизации", "Титульная", "КППРеорг"));
		КонецЕсли;
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Титульная.Наименование) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указано наименование организации", "Титульная", "Наименование"));
	КонецЕсли;
	
	Если Титульная.ПрНП <> "2" И ЗначениеЗаполнено(Титульная.ДатаУвПереход) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Дата перехода указывается только при признаке налогоплательщика 2", "Титульная", "ДатаУвПереход"));
	КонецЕсли;
	Если (Титульная.ПрНП <> "2" Или Не ЗначениеЗаполнено(Титульная.ДатаУвПереход)) И ЗначениеЗаполнено(Титульная.ДатаУвОтказ) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Дата отказа указывается только при признаке налогоплательщика 2 и указанной дате перехода", "Титульная", "ДатаУвОтказ"));
	КонецЕсли;
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаГода(Титульная, ТаблицаОшибок, "Титульная", "НалПер", Истина, , 2022, 2022);
	Если Не ЗначениеЗаполнено(Титульная.ПрНП) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан признак налогоплательщика на титульной странице", "Титульная", "ПрНП"));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Титульная.ДАТА_ПОДПИСИ) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указана дата подписи", "Титульная", "ДАТА_ПОДПИСИ"));
	КонецЕсли;
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаКодаНО(Титульная.КодНО, ТаблицаОшибок, "Титульная");
	Если Не ЗначениеЗаполнено(Титульная.ПРИЗНАК_НП_ПОДВАЛ) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан признак подписанта", "Титульная", "ПРИЗНАК_НП_ПОДВАЛ"));
	КонецЕсли;
	Если Титульная.ПРИЗНАК_НП_ПОДВАЛ = "2" Или Титульная.ПРИЗНАК_НП_ПОДВАЛ = "3" Или Титульная.ПРИЗНАК_НП_ПОДВАЛ = "4" Тогда 
		УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаПодписантаНалоговойОтчетности(Данные, ТаблицаОшибок, "Титульная", Истина);
	Иначе
		УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаПодписантаНалоговойОтчетности(Данные, ТаблицаОшибок, "Титульная", Ложь);
	КонецЕсли;
	Если (Титульная.ПРИЗНАК_НП_ПОДВАЛ = "2" Или Титульная.ПРИЗНАК_НП_ПОДВАЛ = "4")
		И Не ЗначениеЗаполнено(Титульная.НаимДок) Тогда 
		
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан документ представителя", "Титульная", "НаимДок"));
	КонецЕсли;
	
	Если РегламентированнаяОтчетностьПереопределяемый.ЭтоЮридическоеЛицо(Данные.Организация) Тогда
		УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИННКПП(Истина, Титульная, ТаблицаОшибок);
	Иначе
		Если ЗначениеЗаполнено(Титульная.ИНН) Тогда 
			УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИННКПП(Ложь, Титульная, ТаблицаОшибок);
		Иначе
			ЛистСведФЛ = Данные.ДанныеУведомления.ЛистСведФЛ;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.ДатаРождДата) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указана дата рождения", "ЛистСведФЛ", "ДатаРождДата"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.МестоРожд) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указано место рождения", "ЛистСведФЛ", "МестоРожд"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.ПрГражд) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указан признак гражданства", "ЛистСведФЛ", "ПрГражд"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.КодВидДок) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указан код вида документа", "ЛистСведФЛ", "КодВидДок"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.СерНомДок) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указана серия и номер документа", "ЛистСведФЛ", "СерНомДок"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.ДатаДокДата) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указана дата выдачи документа", "ЛистСведФЛ", "ДатаДокДата"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.ВыдДок) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указан орган, выдавший документ", "ЛистСведФЛ", "ВыдДок"));
			КонецЕсли;
			Если ЗначениеЗаполнено(ЛистСведФЛ.СтрРег)
				И (СтрДлина(ЛистСведФЛ.СтрРег) < 3
				Или Не СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(ЛистСведФЛ.СтрРег)) Тогда
				
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указан / неправильно указан код государства", "ЛистСведФЛ", "СтрРег"));
			КонецЕсли;
			
			Если ЗначениеЗаполнено(ЛистСведФЛ.КодВидДок1) 
				Или ЗначениеЗаполнено(ЛистСведФЛ.СерНомДок1) 
				Или ЗначениеЗаполнено(ЛистСведФЛ.ДатаДокДата1) 
				Или ЗначениеЗаполнено(ЛистСведФЛ.ВыдДок1) Тогда 
				
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.КодВидДок1) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указан код вида документа", "ЛистСведФЛ", "КодВидДок1"));
				КонецЕсли;
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.СерНомДок1) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указана серия и номер документа", "ЛистСведФЛ", "СерНомДок1"));
				КонецЕсли;
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.ДатаДокДата1) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указана дата выдачи документа", "ЛистСведФЛ", "ДатаДокДата1"));
				КонецЕсли;
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.ВыдДок1) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указан орган, выдавший документ", "ЛистСведФЛ", "ВыдДок1"));
				КонецЕсли;
			КонецЕсли;
			
			Если ЗначениеЗаполнено(ЛистСведФЛ.ОКСМ) 
				Или ЗначениеЗаполнено(ЛистСведФЛ.АдрИно) Тогда 
				
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.ОКСМ)
					Или СтрДлина(ЛистСведФЛ.ОКСМ) < 3
					Или Не СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(ЛистСведФЛ.ОКСМ) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указан / неправильно указан код государства", "ЛистСведФЛ", "ОКСМ"));
				КонецЕсли;
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.АдрИно) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указан адрес", "ЛистСведФЛ", "АдрИно"));
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИНН(Титульная, ТаблицаОшибок, "Титульная", "ИННФЛ", Ложь, Ложь);
	ЛистыАБ = 0;
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистА Цикл 
		Если ЗначениеЗаполнено(Лист.Значение.НомерКИКЧисло) Тогда 
			ЛистыАБ = 1;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистБ Цикл 
		Если ЗначениеЗаполнено(Лист.Значение.НомерКИКЧисло) Тогда 
			ЛистыАБ = 1;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если ЛистыАБ = 0 Тогда
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Отсутствуют Листы А и Листы Б", "ЛистА", "НомерКИКЧисло", Данные.ДанныеМногостраничныхРазделов.ЛистА[0].Значение.УИД));
	КонецЕсли;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистА Цикл
		ЛистТек = Лист.Значение;
		Если ЗначениеЗаполнено(ЛистТек.НомерКИКЧисло) Тогда
			Если Не ЗначениеЗаполнено(ЛистТек.ОснКонтрИО)
				И Не ЗначениеЗаполнено(ЛистТек.ДоляПрямУч)
				И Не ЗначениеЗаполнено(ЛистТек.ДоляКосУч)
				И Не ЗначениеЗаполнено(ЛистТек.ДоляСмешУч)
				И Не ЗначениеЗаполнено(ЛистТек.ДоляУчСупДет)
				И Не ЗначениеЗаполнено(ЛистТек.ДоляУчНалРезРФ) Тогда 
				
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Необходимо указать доли участия или основание на листе А", "ЛистА", "ОснКонтрИО", ЛистТек.УИД));
			КонецЕсли;
			
			Если ЗначениеЗаполнено(ЛистТек.ДоляПрямУч)
				Или ЗначениеЗаполнено(ЛистТек.ДоляКосУч)
				Или ЗначениеЗаполнено(ЛистТек.ДоляСмешУч)
				Или ЗначениеЗаполнено(ЛистТек.ДоляУчСупДет)
				Или ЗначениеЗаполнено(ЛистТек.ДоляУчНалРезРФ) Тогда 
				Если (Не ЗначениеЗаполнено(ЛистТек.ДоляПрямУч) И Не ЗначениеЗаполнено(ЛистТек.ДоляКосУч)) Тогда
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Необходимо заполнить пункт 2.2 или 2.3 на листе А", "ЛистА", "ДоляПрямУч", ЛистТек.УИД));
				КонецЕсли;
				Если (ЗначениеЗаполнено(ЛистТек.ДоляПрямУч) И ЗначениеЗаполнено(ЛистТек.ДоляКосУч) И (Не ЗначениеЗаполнено(ЛистТек.ДоляСмешУч))) Тогда
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не заполнен пункт 2.4 на листе А", "ЛистА", "ДоляСмешУч", ЛистТек.УИД));
				КонецЕсли;
				Если ЛистТек.Код204 = "1" И Не ЗначениеЗаполнено(ЛистТек.ДоляУчСупДет) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"При Код204 = 1 необходимо заполнить долю совместного участия на листе А", "ЛистА", "ДоляУчСупДет", ЛистТек.УИД));
				ИначеЕсли ЛистТек.Код204 = "0" И ЗначениеЗаполнено(ЛистТек.ДоляУчСупДет) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"При Код204 = 0 доля совместного участия не заполняется на листе А", "ЛистА", "ДоляУчСупДет", ЛистТек.УИД));
				КонецЕсли;
				Если ЛистТек.Код205 = "1" И Не ЗначениеЗаполнено(ЛистТек.ДоляУчНалРезРФ) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"При Код205 = 1 необходимо заполнить долю совместного участия на листе А", "ЛистА", "ДоляУчНалРезРФ", ЛистТек.УИД));
				ИначеЕсли (ЛистТек.Код205 = "0" Или Не ЗначениеЗаполнено(ЛистТек.Код205))
					И ЗначениеЗаполнено(ЛистТек.ДоляУчНалРезРФ) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"При Код205 = 0 или его отсутствии доля совместного участия не заполняется на листе А", "ЛистА", "ДоляУчНалРезРФ", ЛистТек.УИД));
				КонецЕсли;
			КонецЕсли;
			
			Если (Не ЗначениеЗаполнено(ЛистТек.ПрСамПризн)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 1.3 на листе А", "ЛистА", "ПрСамПризн", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.НаимОргЛат)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено наименование организации на листе А", "ЛистА", "НаимОргЛат", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.СтрРег)
				Или СтрДлина(ЛистТек.СтрРег) < 3
				Или Не СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(ЛистТек.СтрРег) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена / неправильно заполнена страна регистрации на листе А", "ЛистА", "СтрРег", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.ДатаРегИО)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена дата регистрации на листе А", "ЛистА", "ДатаРегИО", ЛистТек.УИД));
			КонецЕсли;
			
			Для Инд = 1 По 5 Цикл
				СтрИмя = "Код" + (100 + Инд);
				Если (Не ЗначениеЗаполнено(ЛистТек[СтрИмя])) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не заполнен пункт "+(100+Инд)+" на листе А", "ЛистА", СтрИмя, ЛистТек.УИД));
				КонецЕсли;
			КонецЦикла;
			Для Инд = 1 По 4 Цикл
				СтрИмя = "Код" + (200 + Инд);
				Если (Не ЗначениеЗаполнено(ЛистТек[СтрИмя])) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не заполнен пункт "+(200+Инд)+" на листе А", "ЛистА", СтрИмя, ЛистТек.УИД));
				КонецЕсли;
			КонецЦикла;
			Если ЛистТек.Код102 = "1" И Не ЗначениеЗаполнено(ЛистТек.Код205) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"При Код102 = 1 необходимо заполнить Код205 на листе А", "ЛистА", "Код205", ЛистТек.УИД));
			ИначеЕсли (ЛистТек.Код102 = "0" Или ЛистТек.Код102 = "2") И ЗначениеЗаполнено(ЛистТек.Код205) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"При Код102 = 0 или Код102 = 2 Код205 не заполняется на листе А", "ЛистА", "Код205", ЛистТек.УИД));
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистБ Цикл
		ЛистТек = Лист.Значение;
		Если ЗначениеЗаполнено(ЛистТек.НомерКИКЧисло) Тогда
			Если (Не ЗначениеЗаполнено(ЛистТек.ПрСамПризн)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт признак самостоятельного признания на листе Б", "ЛистБ", "ПрСамПризн", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.НаимОргЛат)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено наименование организации листе Б", "ЛистБ", "НаимОргЛат", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.ОргФорм)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена организационная форма листе Б", "ЛистБ", "ОргФорм", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.НаимДокУчр)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено наименование документа на листе Б", "ЛистБ", "НаимДокУчр", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.ДатаУчрИСДата)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена дата учреждения на листе Б", "ЛистБ", "ДатаУчрИСДата", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.СтрРег)
				Или СтрДлина(ЛистТек.СтрРег) < 3
				Или Не СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(ЛистТек.СтрРег) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен / неправильно заполнен код государства листе Б", "ЛистБ", "СтрРег", ЛистТек.УИД));
			КонецЕсли;
			Для Инд = 6 По 12 Цикл
				СтрИмя = "Код" + (100 + Инд);
				Если (Не ЗначениеЗаполнено(Лист.Значение[СтрИмя])) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не заполнен пункт "+(100+Инд)+" на листе Б", "ЛистБ", СтрИмя, ЛистТек.УИД));
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистВ Цикл
		ЛистТек = Лист.Значение;
		Если ЗначениеЗаполнено(ЛистТек.НомерКИК) Тогда
			Если (Не ЗначениеЗаполнено(ЛистТек.ПорОпрПрКИК)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 2 на листе В", "ЛистВ", "ПорОпрПрКИК", ЛистТек.УИД));
			КонецЕсли;
			Если ЛистТек.ПорОпрПрКИК = "1" И Не ЗначениеЗаполнено(ЛистТек.ДатаПослДнПерДата) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 3 на листе В при значении п.2 = 1", "ЛистВ", "ДатаПослДнПерДата", ЛистТек.УИД));
			КонецЕсли;
			Если ЛистТек.ПорОпрПрКИК = "1" И Не ЗначениеЗаполнено(ЛистТек.ДатаСостФОДата) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 4 на листе В при значении п.2 = 1", "ЛистВ", "ДатаСостФОДата", ЛистТек.УИД));
			КонецЕсли;
			Если ЛистТек.ПорОпрПрКИК = "2" И Не ЗначениеЗаполнено(ЛистТек.ДатаПослДнПерГл25Дата) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 5 на листе В при значении п.2 = 2", "ЛистВ", "ДатаПослДнПерГл25Дата", ЛистТек.УИД));
			КонецЕсли;
			Для Инд = 1 По 9 Цикл
				СтрИмя = "Код100" + Инд;
				Если (Не ЗначениеЗаполнено(Лист.Значение[СтрИмя])) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не заполнен пункт 100"+Инд+" на листе В", "ЛистВ", СтрИмя, ЛистТек.УИД));
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистГ Цикл
		ЛистТек = Лист.Значение;
		Если ЗначениеЗаполнено(ЛистТек.НомерКИК) Тогда
			
			Если Не ЗначениеЗаполнено(ЛистТек.НаимОрг) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено название организации", "ЛистГ", "НаимОрг", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.ДоляУчКосвИт) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена доля участия", "ЛистГ", "ДоляУчКосвИт", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.НомерПосл) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен номер последовательности", "ЛистГ", "НомерПосл", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.ДоляКосвПослИт) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена доля участия в последовательности", "ЛистГ", "ДоляКосвПослИт", ЛистТек.УИД));
			КонецЕсли;
			
			Строки = Данные.ДанныеДопСтрокБД.МнгСтр.НайтиСтроки(Новый Структура("УИД", ЛистТек.УИД));
			Инд = 0;
			Для Каждого Стр Из Строки Цикл 
				Инд = Инд + 1;
				Если Не ЗначениеЗаполнено(Стр.НомерУч) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"В свободных строках листа Г должен быть заполнен пункт 3.1", "ЛистГ", "НомерУч___" + Формат(Инд, "ЧГ="), ЛистТек.УИД));
				ИначеЕсли ЗначениеЗаполнено(Стр.НомерУч) И Не (ЗначениеЗаполнено(Стр.ДоляУчКосв) Или ЗначениеЗаполнено(Стр.ДоляУчПрям)) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"В свободных строках листа Г должен быть заполнен пункт 3.2 или 3.3", "ЛистГ", "ДоляУчКосв___" + Формат(Инд, "ЧГ="), ЛистТек.УИД));
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистГ1 Цикл
		Если ЗначениеЗаполнено(Лист.Значение.НомерРосОрг) Тогда 
			ЛистТек = Лист.Значение;
			Если ЗначениеЗаполнено(ЛистТек.ОГРН) 
				И Не РегламентированныеДанныеКлиентСервер.ОГРНСоответствуетТребованиям(ЛистТек.ОГРН, Истина, "") Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен/неправильно заполнен ОГРН на листе Г1", "ЛистГ1", "ОГРН", ЛистТек.УИД));
			КонецЕсли;
			УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИНН(ЛистТек, ТаблицаОшибок, "ЛистГ1", "ИННЮЛ", Истина, Истина);
			УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаКПП(ЛистТек, ТаблицаОшибок, "ЛистГ1", "КПП", Истина);
			Если Не ЗначениеЗаполнено(ЛистТек.НаимОрг) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено наименование организации на листе Г1", "ЛистГ1", "НаимОрг", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.ПрПредДокОсв) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен признак на листе Г1", "ЛистГ1", "ПрПредДокОсв", ЛистТек.УИД));
			Иначе
				УведомлениеОСпецрежимахНалогообложения.ПроверитьПоказательНаСоотвествиеСписку(
					ЛистТек, СпискиВыбора, "ПрПредДокОсв", "ЛистГ1", Истина, "признак представленных документов", ТаблицаОшибок);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистГ2 Цикл
		Если ЗначениеЗаполнено(Лист.Значение.НомерКИК) Тогда 
			
			ЛистТек = Лист.Значение;
			Если Не ЗначениеЗаполнено(ЛистТек.НаимОргЛат) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено наименование организации латиницей на листе Г2", "ЛистГ2", "НаимОргЛат", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.СтрРег)
				Или СтрДлина(ЛистТек.СтрРег) < 3
				Или Не СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(ЛистТек.СтрРег) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена / неправильно заполнена страна регистрации на листе Г2", "ЛистГ2", "СтрРег", ЛистТек.УИД));
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	УведомлениеОСпецрежимахНалогообложения.ПроверкаДатВУведомлении(Данные, ТаблицаОшибок);
	УведомлениеОСпецрежимахНалогообложения.ПолнаяПроверкаЗаполненныхПоказателейНаСоотвествиеСписку(
		"СпискиВыбора2021_1", "СхемаВыгрузкиФорма2021_1", Данные.Объект.ИмяОтчета, ТаблицаОшибок, Данные);
	
	Возврат ТаблицаОшибок;
КонецФункции

Функция ПроверитьДокументСВыводомВТаблицу_Форма2023_1(Данные, УникальныйИдентификатор)
	ТаблицаОшибок = Новый СписокЗначений;
	Данные["АрхивМакетов"] = "ЭкранныеФормы_2023";
	УведомлениеОСпецрежимахНалогообложения.ПроверкаАктуальностиФормыПриВыгрузке(
		Данные.Объект.ИмяФормы, ТаблицаОшибок, ПолучитьТаблицуФорм());
	СпискиВыбора = УведомлениеОСпецрежимахНалогообложения.СформироватьСпискиВыбораДляПроверкиВыгрузки(
		Данные.Объект.ИмяОтчета, "СпискиВыбора2023_1", "ЭкранныеФормы_2023");
	Титульная = Данные.ДанныеУведомления.Титульная;
	
	Если ЗначениеЗаполнено(Титульная.ФормРеорг)  Тогда
		УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИНН(Титульная, ТаблицаОшибок, "Титульная", "ИННРеорг", Истина, Истина);
		УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаКПП(Титульная, ТаблицаОшибок, "Титульная", "КППРеорг", Истина);
	Иначе
		Если ЗначениеЗаполнено(Титульная.ИННРеорг) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"ИНН реорганизованной организации не указывается при отсутствии реорганизации", "Титульная", "ИННРеорг"));
		КонецЕсли;
		Если ЗначениеЗаполнено(Титульная.КППРеорг) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"КПП реорганизованной организации не указывается при отсутствии реорганизации", "Титульная", "КППРеорг"));
		КонецЕсли;
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Титульная.Наименование) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указано наименование организации", "Титульная", "Наименование"));
	КонецЕсли;
	
	Если Титульная.ПрНП <> "2" И ЗначениеЗаполнено(Титульная.ДатаУвПереход) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Дата перехода указывается только при признаке налогоплательщика 2", "Титульная", "ДатаУвПереход"));
	КонецЕсли;
	Если (Титульная.ПрНП <> "2" Или Не ЗначениеЗаполнено(Титульная.ДатаУвПереход)) И ЗначениеЗаполнено(Титульная.ДатаУвОтказ) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Дата отказа указывается только при признаке налогоплательщика 2 и указанной дате перехода", "Титульная", "ДатаУвОтказ"));
	КонецЕсли;
	
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаГода(Титульная, ТаблицаОшибок, "Титульная", "НалПер", Истина, , 2023);
	Если Не ЗначениеЗаполнено(Титульная.ПрНП) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан признак налогоплательщика на титульной странице", "Титульная", "ПрНП"));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Титульная.ДАТА_ПОДПИСИ) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указана дата подписи", "Титульная", "ДАТА_ПОДПИСИ"));
	КонецЕсли;
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаКодаНО(Титульная.КодНО, ТаблицаОшибок, "Титульная");
	Если Не ЗначениеЗаполнено(Титульная.ПРИЗНАК_НП_ПОДВАЛ) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан признак подписанта", "Титульная", "ПРИЗНАК_НП_ПОДВАЛ"));
	КонецЕсли;
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаПодписантаНалоговойОтчетности(
		Данные, ТаблицаОшибок, "Титульная", Титульная.ПРИЗНАК_НП_ПОДВАЛ = "2" Или Титульная.ПРИЗНАК_НП_ПОДВАЛ = "3" Или Титульная.ПРИЗНАК_НП_ПОДВАЛ = "4");
	Если (Титульная.ПРИЗНАК_НП_ПОДВАЛ = "2" Или Титульная.ПРИЗНАК_НП_ПОДВАЛ = "4")
		И Не ЗначениеЗаполнено(Титульная.НаимДок) Тогда 
		
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан документ представителя", "Титульная", "НаимДок"));
	КонецЕсли;
	
	Если РегламентированнаяОтчетностьПереопределяемый.ЭтоЮридическоеЛицо(Данные.Организация) Тогда
		УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИННКПП(Истина, Титульная, ТаблицаОшибок);
	Иначе
		Если ЗначениеЗаполнено(Титульная.ИНН) Тогда 
			УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИННКПП(Ложь, Титульная, ТаблицаОшибок);
		Иначе
			ЛистСведФЛ = Данные.ДанныеУведомления.ЛистСведФЛ;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.ДатаРождДата) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указана дата рождения", "ЛистСведФЛ", "ДатаРождДата"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.МестоРожд) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указано место рождения", "ЛистСведФЛ", "МестоРожд"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.ПрГражд) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указан признак гражданства", "ЛистСведФЛ", "ПрГражд"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.КодВидДок) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указан код вида документа", "ЛистСведФЛ", "КодВидДок"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.СерНомДок) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указана серия и номер документа", "ЛистСведФЛ", "СерНомДок"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.ДатаДокДата) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указана дата выдачи документа", "ЛистСведФЛ", "ДатаДокДата"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистСведФЛ.ВыдДок) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указан орган, выдавший документ", "ЛистСведФЛ", "ВыдДок"));
			КонецЕсли;
			УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаОКСМ(ЛистСведФЛ, ТаблицаОшибок, "ЛистСведФЛ", "СтрРег", Ложь);
			
			Если ЗначениеЗаполнено(ЛистСведФЛ.КодВидДок1) 
				Или ЗначениеЗаполнено(ЛистСведФЛ.СерНомДок1) 
				Или ЗначениеЗаполнено(ЛистСведФЛ.ДатаДокДата1) 
				Или ЗначениеЗаполнено(ЛистСведФЛ.ВыдДок1) Тогда 
				
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.КодВидДок1) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указан код вида документа", "ЛистСведФЛ", "КодВидДок1"));
				КонецЕсли;
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.СерНомДок1) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указана серия и номер документа", "ЛистСведФЛ", "СерНомДок1"));
				КонецЕсли;
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.ДатаДокДата1) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указана дата выдачи документа", "ЛистСведФЛ", "ДатаДокДата1"));
				КонецЕсли;
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.ВыдДок1) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указан орган, выдавший документ", "ЛистСведФЛ", "ВыдДок1"));
				КонецЕсли;
			КонецЕсли;
			
			Если ЗначениеЗаполнено(ЛистСведФЛ.ОКСМ) 
				Или ЗначениеЗаполнено(ЛистСведФЛ.АдрИно) Тогда
				
				УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаОКСМ(ЛистСведФЛ, ТаблицаОшибок, "ЛистСведФЛ", "ОКСМ", Истина);
				Если Не ЗначениеЗаполнено(ЛистСведФЛ.АдрИно) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указан адрес", "ЛистСведФЛ", "АдрИно"));
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИНН(Титульная, ТаблицаОшибок, "Титульная", "ИННФЛ", Ложь, Ложь);
	ЛистыАБ = 0;
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистА Цикл 
		Если ЗначениеЗаполнено(Лист.Значение.НомерКИКЧисло) Тогда 
			ЛистыАБ = 1;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистБ Цикл 
		Если ЗначениеЗаполнено(Лист.Значение.НомерКИКЧисло) Тогда 
			ЛистыАБ = 1;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если ЛистыАБ = 0 Тогда
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Отсутствуют Листы А и Листы Б", "ЛистА", "НомерКИКЧисло", Данные.ДанныеМногостраничныхРазделов.ЛистА[0].Значение.УИД));
	КонецЕсли;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистА Цикл
		ЛистТек = Лист.Значение;
		Если ЗначениеЗаполнено(ЛистТек.НомерКИКЧисло) Тогда
			Если Не ЗначениеЗаполнено(ЛистТек.ОснКонтрИО)
				И Не ЗначениеЗаполнено(ЛистТек.ДоляПрямУч)
				И Не ЗначениеЗаполнено(ЛистТек.ДоляКосУч)
				И Не ЗначениеЗаполнено(ЛистТек.ДоляСмешУч)
				И Не ЗначениеЗаполнено(ЛистТек.ДоляУчСупДет)
				И Не ЗначениеЗаполнено(ЛистТек.ДоляУчНалРезРФ) Тогда 
				
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Необходимо указать доли участия или основание на листе А", "ЛистА", "ОснКонтрИО", ЛистТек.УИД));
			КонецЕсли;
			
			Если ЗначениеЗаполнено(ЛистТек.ДоляПрямУч)
				Или ЗначениеЗаполнено(ЛистТек.ДоляКосУч)
				Или ЗначениеЗаполнено(ЛистТек.ДоляСмешУч)
				Или ЗначениеЗаполнено(ЛистТек.ДоляУчСупДет)
				Или ЗначениеЗаполнено(ЛистТек.ДоляУчНалРезРФ) Тогда 
				Если (Не ЗначениеЗаполнено(ЛистТек.ДоляПрямУч) И Не ЗначениеЗаполнено(ЛистТек.ДоляКосУч)) Тогда
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Необходимо заполнить пункт 2.2 или 2.3 на листе А", "ЛистА", "ДоляПрямУч", ЛистТек.УИД));
				КонецЕсли;
				Если (ЗначениеЗаполнено(ЛистТек.ДоляПрямУч) И ЗначениеЗаполнено(ЛистТек.ДоляКосУч) И (Не ЗначениеЗаполнено(ЛистТек.ДоляСмешУч))) Тогда
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не заполнен пункт 2.4 на листе А", "ЛистА", "ДоляСмешУч", ЛистТек.УИД));
				КонецЕсли;
				Если ЛистТек.Код204 = "1" И Не ЗначениеЗаполнено(ЛистТек.ДоляУчСупДет) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"При Код204 = 1 необходимо заполнить долю совместного участия на листе А", "ЛистА", "ДоляУчСупДет", ЛистТек.УИД));
				ИначеЕсли ЛистТек.Код204 = "0" И ЗначениеЗаполнено(ЛистТек.ДоляУчСупДет) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"При Код204 = 0 доля совместного участия не заполняется на листе А", "ЛистА", "ДоляУчСупДет", ЛистТек.УИД));
				КонецЕсли;
				Если ЛистТек.Код205 = "1" И Не ЗначениеЗаполнено(ЛистТек.ДоляУчНалРезРФ) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"При Код205 = 1 необходимо заполнить долю совместного участия на листе А", "ЛистА", "ДоляУчНалРезРФ", ЛистТек.УИД));
				ИначеЕсли (ЛистТек.Код205 = "0" Или Не ЗначениеЗаполнено(ЛистТек.Код205))
					И ЗначениеЗаполнено(ЛистТек.ДоляУчНалРезРФ) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"При Код205 = 0 или его отсутствии доля совместного участия не заполняется на листе А", "ЛистА", "ДоляУчНалРезРФ", ЛистТек.УИД));
				КонецЕсли;
			КонецЕсли;
			
			Если (Не ЗначениеЗаполнено(ЛистТек.ПрСамПризн)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 1.3 на листе А", "ЛистА", "ПрСамПризн", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.НаимОргЛат)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено наименование организации на листе А", "ЛистА", "НаимОргЛат", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.СтрРег)
				Или СтрДлина(ЛистТек.СтрРег) < 3
				Или Не СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(ЛистТек.СтрРег) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена / неправильно заполнена страна регистрации на листе А", "ЛистА", "СтрРег", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.ДатаРегИО)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена дата регистрации на листе А", "ЛистА", "ДатаРегИО", ЛистТек.УИД));
			КонецЕсли;
			
			Для Инд = 1 По 5 Цикл
				СтрИмя = "Код" + (100 + Инд);
				Если (Не ЗначениеЗаполнено(ЛистТек[СтрИмя])) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не заполнен пункт "+(100+Инд)+" на листе А", "ЛистА", СтрИмя, ЛистТек.УИД));
				КонецЕсли;
			КонецЦикла;
			Для Инд = 1 По 4 Цикл
				СтрИмя = "Код" + (200 + Инд);
				Если (Не ЗначениеЗаполнено(ЛистТек[СтрИмя])) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не заполнен пункт "+(200+Инд)+" на листе А", "ЛистА", СтрИмя, ЛистТек.УИД));
				КонецЕсли;
			КонецЦикла;
			Если ЛистТек.Код102 = "1" И Не ЗначениеЗаполнено(ЛистТек.Код205) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"При Код102 = 1 необходимо заполнить Код205 на листе А", "ЛистА", "Код205", ЛистТек.УИД));
			ИначеЕсли (ЛистТек.Код102 = "0" Или ЛистТек.Код102 = "2") И ЗначениеЗаполнено(ЛистТек.Код205) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"При Код102 = 0 или Код102 = 2 Код205 не заполняется на листе А", "ЛистА", "Код205", ЛистТек.УИД));
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	ЛистыБЗаполнены = Ложь;
	ЕстьП81_1 = Ложь;
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистБ Цикл
		ЛистТек = Лист.Значение;
		Если ЗначениеЗаполнено(ЛистТек.НомерКИКЧисло) Тогда
			ЛистыБЗаполнены = Истина;
			Если (Не ЗначениеЗаполнено(ЛистТек.ПрСамПризн)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт признак самостоятельного признания на листе Б", "ЛистБ", "ПрСамПризн", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.НаимОргЛат)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено наименование организации листе Б", "ЛистБ", "НаимОргЛат", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.ОргФорм)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена организационная форма листе Б", "ЛистБ", "ОргФорм", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.НаимДокУчр)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено наименование документа на листе Б", "ЛистБ", "НаимДокУчр", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.ДатаУчрИСДата)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена дата учреждения на листе Б", "ЛистБ", "ДатаУчрИСДата", ЛистТек.УИД));
			КонецЕсли;
			Если (Не ЗначениеЗаполнено(ЛистТек.ПризПрим81)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен признак освобождения по статье 25.15 НК РФ на листе Б", "ЛистБ", "ПризПрим81", ЛистТек.УИД));
			КонецЕсли;
			ЕстьП81_1 = ЕстьП81_1 Или (ЛистТек.ПризПрим81 = "1");
			
			УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаОКСМ(ЛистТек, ТаблицаОшибок, "ЛистБ", "СтрРег", Истина);
			Для Инд = 6 По 12 Цикл
				СтрИмя = "Код" + (100 + Инд);
				Если (Не ЗначениеЗаполнено(Лист.Значение[СтрИмя])) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не заполнен пункт "+(100+Инд)+" на листе Б", "ЛистБ", СтрИмя, ЛистТек.УИД));
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
	ЛистыВЗаполнены = Ложь;
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистВ Цикл
		ЛистТек = Лист.Значение;
		Если ЗначениеЗаполнено(ЛистТек.НомерКИК) Тогда
			ЛистыВЗаполнены = Истина;
			Если (Не ЗначениеЗаполнено(ЛистТек.ПорОпрПрКИК)) Тогда
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 2 на листе В", "ЛистВ", "ПорОпрПрКИК", ЛистТек.УИД));
			КонецЕсли;
			Если ЛистТек.ПорОпрПрКИК = "1" И Не ЗначениеЗаполнено(ЛистТек.ДатаПослДнПерДата) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 3 на листе В при значении п.2 = 1", "ЛистВ", "ДатаПослДнПерДата", ЛистТек.УИД));
			КонецЕсли;
			Если ЛистТек.ПорОпрПрКИК = "1" И Не ЗначениеЗаполнено(ЛистТек.ДатаСостФОДата) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 4 на листе В при значении п.2 = 1", "ЛистВ", "ДатаСостФОДата", ЛистТек.УИД));
			КонецЕсли;
			Если ЛистТек.ПорОпрПрКИК = "2" И Не ЗначениеЗаполнено(ЛистТек.ДатаПослДнПерГл25Дата) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен пункт 5 на листе В при значении п.2 = 2", "ЛистВ", "ДатаПослДнПерГл25Дата", ЛистТек.УИД));
			КонецЕсли;
			Для Инд = 1 По 9 Цикл
				СтрИмя = "Код100" + Инд;
				Если (Не ЗначениеЗаполнено(Лист.Значение[СтрИмя])) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не заполнен пункт 100"+Инд+" на листе В", "ЛистВ", СтрИмя, ЛистТек.УИД));
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
	УсловиеНаличияЛистовВ = ЛистыВЗаполнены Или (Не ЛистыБЗаполнены)
		Или (ЗначениеЗаполнено(Титульная.ДатаУвПереход) И (Не ЗначениеЗаполнено(Титульная.ДатаУвОтказ)))
		Или ((Не ЗначениеЗаполнено(Титульная.ДатаУвПереход)) И (Не ЗначениеЗаполнено(Титульная.ДатаУвОтказ) И ЕстьП81_1));
	Если Не УсловиеНаличияЛистовВ Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Отсутствуют Листы В", "ЛистВ", "НомерКИКЧисло", Данные.ДанныеМногостраничныхРазделов.ЛистВ[0].Значение.УИД));
	КонецЕсли;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистГ Цикл
		ЛистТек = Лист.Значение;
		Если ЗначениеЗаполнено(ЛистТек.НомерКИК) Тогда
			
			Если Не ЗначениеЗаполнено(ЛистТек.НаимОрг) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено название организации", "ЛистГ", "НаимОрг", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.ДоляУчКосвИт) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена доля участия", "ЛистГ", "ДоляУчКосвИт", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.НомерПосл) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен номер последовательности", "ЛистГ", "НомерПосл", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.ДоляКосвПослИт) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена доля участия в последовательности", "ЛистГ", "ДоляКосвПослИт", ЛистТек.УИД));
			КонецЕсли;
			
			Строки = Данные.ДанныеДопСтрокБД.МнгСтр.НайтиСтроки(Новый Структура("УИД", ЛистТек.УИД));
			Инд = 0;
			Для Каждого Стр Из Строки Цикл 
				Инд = Инд + 1;
				Если Не ЗначениеЗаполнено(Стр.НомерУч) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"В свободных строках листа Г должен быть заполнен пункт 3.1", "ЛистГ", "НомерУч___" + Формат(Инд, "ЧГ="), ЛистТек.УИД));
				ИначеЕсли ЗначениеЗаполнено(Стр.НомерУч) И Не (ЗначениеЗаполнено(Стр.ДоляУчКосв) Или ЗначениеЗаполнено(Стр.ДоляУчПрям)) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"В свободных строках листа Г должен быть заполнен пункт 3.2 или 3.3", "ЛистГ", "ДоляУчКосв___" + Формат(Инд, "ЧГ="), ЛистТек.УИД));
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистГ1 Цикл
		Если ЗначениеЗаполнено(Лист.Значение.НомерРосОрг) Тогда 
			ЛистТек = Лист.Значение;
			Если ЗначениеЗаполнено(ЛистТек.ОГРН) 
				И Не РегламентированныеДанныеКлиентСервер.ОГРНСоответствуетТребованиям(ЛистТек.ОГРН, Истина, "") Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен/неправильно заполнен ОГРН на листе Г1", "ЛистГ1", "ОГРН", ЛистТек.УИД));
			КонецЕсли;
			УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИНН(ЛистТек, ТаблицаОшибок, "ЛистГ1", "ИННЮЛ", Истина, Истина);
			УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаКПП(ЛистТек, ТаблицаОшибок, "ЛистГ1", "КПП", Истина);
			Если Не ЗначениеЗаполнено(ЛистТек.НаимОрг) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено наименование организации на листе Г1", "ЛистГ1", "НаимОрг", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.ПрПредДокОсв) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнен признак на листе Г1", "ЛистГ1", "ПрПредДокОсв", ЛистТек.УИД));
			Иначе
				УведомлениеОСпецрежимахНалогообложения.ПроверитьПоказательНаСоотвествиеСписку(
					ЛистТек, СпискиВыбора, "ПрПредДокОсв", "ЛистГ1", Истина, "признак представленных документов", ТаблицаОшибок);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Лист Из Данные.ДанныеМногостраничныхРазделов.ЛистГ2 Цикл
		Если ЗначениеЗаполнено(Лист.Значение.НомерКИК) Тогда 
			
			ЛистТек = Лист.Значение;
			Если Не ЗначениеЗаполнено(ЛистТек.НаимОргЛат) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнено наименование организации латиницей на листе Г2", "ЛистГ2", "НаимОргЛат", ЛистТек.УИД));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(ЛистТек.СтрРег)
				Или СтрДлина(ЛистТек.СтрРег) < 3
				Или Не СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(ЛистТек.СтрРег) Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не заполнена / неправильно заполнена страна регистрации на листе Г2", "ЛистГ2", "СтрРег", ЛистТек.УИД));
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	УведомлениеОСпецрежимахНалогообложения.ПроверкаДатВУведомлении(Данные, ТаблицаОшибок);
	УведомлениеОСпецрежимахНалогообложения.ПолнаяПроверкаЗаполненныхПоказателейНаСоотвествиеСписку(
		"СпискиВыбора2023_1", "СхемаВыгрузкиФорма2023_1", Данные.Объект.ИмяОтчета, ТаблицаОшибок, Данные);
	
	Возврат ТаблицаОшибок;
КонецФункции

#КонецОбласти
#КонецЕсли
