#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ПолучитьТаблицуПримененияФорматов() Экспорт 
	Результат = Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюТаблицуПримененияФорматов();
	
	Стр = Результат.Добавить();
	Стр.ИмяФормы = "Форма2026_1";
	Стр.КНД = "1160303";
	Стр.ВерсияФормата = "5.01";
	
	Возврат Результат;
КонецФункции

Функция ДанноеУведомлениеДоступноДляОрганизации() Экспорт 
	Возврат Истина;
КонецФункции

Функция ДанноеУведомлениеДоступноДляИП() Экспорт 
	Возврат Истина;
КонецФункции

Функция ПолучитьОсновнуюФорму() Экспорт 
	Возврат "";
КонецФункции

Функция ПолучитьФормуПоУмолчанию() Экспорт 
	Возврат "";
КонецФункции

Функция ПолучитьТаблицуФорм() Экспорт 
	Результат = Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюТаблицуФормУведомления();
	
	Стр = Результат.Добавить();
	Стр.ИмяФормы = "Форма2026_1";
	Стр.ОписаниеФормы = "Приказ ФНС России от --.--.---- № ЕД-----/---@";
	Стр.ДатаНачала = '20210701';
	Стр.ДатаКонца = '20991231';

	Возврат Результат;
КонецФункции

Функция ЭлектронноеПредставление(Объект, ИмяФормы, УникальныйИдентификатор) Экспорт
	Если ИмяФормы = "Форма2026_1" Тогда
		Возврат ЭлектронноеПредставление_Форма2026_1(Объект, УникальныйИдентификатор);
	КонецЕсли;
КонецФункции

Функция СформироватьСписокЛистов(Объект) Экспорт
	Если Объект.ИмяФормы = "Форма2026_1" Тогда
		Возврат СформироватьСписокЛистов_Форма2026_1(Объект);
	КонецЕсли;
КонецФункции

Функция ПроверитьДокументСВыводомВТаблицу(Объект, ИмяФормы, УникальныйИдентификатор) Экспорт 
	Если ИмяФормы = "Форма2026_1"  Тогда 
		Возврат ПроверитьДокументСВыводомВТаблицу_Форма2026_1(
			УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект), УникальныйИдентификатор);
	КонецЕсли;
КонецФункции

Процедура ПроверкаТовар_Форма2026_1(ТаблицаОшибок, Товар, ИмяСтраницы)
	Если Не СтрПодобнаПоРегулярномуВыражению(Товар.ТНВЭД, "[0-9]{11}") Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан / неправильно указан ТНВЭД", ИмяСтраницы, "ТНВЭД", Товар.УИД));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Товар.НаимТов) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указано наименование товара", ИмяСтраницы, "НаимТов", Товар.УИД));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Товар.ПрПТ) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан признак подакцизности товара", ИмяСтраницы, "ПрПТ", Товар.УИД));
	КонецЕсли;
	Если Товар.ПрПТ = "0" И ЗначениеЗаполнено(Товар.КодПТ) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Код подакцизного товара не указывается", ИмяСтраницы, "КодПТ", Товар.УИД));
	ИначеЕсли Товар.ПрПТ = "1" И Не ЗначениеЗаполнено(Товар.КодПТ) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Необходимо указать код подакцизного товара", ИмяСтраницы, "КодПТ", Товар.УИД));
	КонецЕсли;
	Если Не СтрПодобнаПоРегулярномуВыражению(Товар.ОКЕИ, "[0-9]{3}") Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан / неправильно указан код ОКЕИ", ИмяСтраницы, "ОКЕИ", Товар.УИД));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Товар.КолТов) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указано количество товара", ИмяСтраницы, "КолТов", Товар.УИД));
	КонецЕсли;
	Если Не СтрПодобнаПоРегулярномуВыражению(Товар.КодВалют, "[0-9]{3}") Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан / неправильно указан код валюты", ИмяСтраницы, "КодВалют", Товар.УИД));
	КонецЕсли;
	Если Не СтрПодобнаПоРегулярномуВыражению(Товар.ОКСМПроисх, "[0-9]{3}") Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан / неправильно указан код страны", ИмяСтраницы, "ОКСМПроисх", Товар.УИД));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Товар.СтрПроисх) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указана страна происхождения", ИмяСтраницы, "СтрПроисх", Товар.УИД));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Товар.ОсобПризн) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан особый признак", ИмяСтраницы, "ОсобПризн", Товар.УИД));
	КонецЕсли;
	
	СтавкиАкцизаЗаполнены = УведомлениеОСпецрежимахНалогообложения.БлокЗаполнен(Товар, "АкцТвСт,АкцАдвСт");
	Если Товар.ПрПТ = "0" И СтавкиАкцизаЗаполнены Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Ставки акцизов не заполняются", ИмяСтраницы, ?(ЗначениеЗаполнено(Товар.АкцТвСт), "АкцТвСт", "АкцАдвСт"), Товар.УИД));
	КонецЕсли;
	Если Товар.ПрПТ = "1" И Не СтавкиАкцизаЗаполнены Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не заполнены ставки акцизов", ИмяСтраницы, "АкцТвСт", Товар.УИД));
	КонецЕсли;
	Если ЗначениеЗаполнено(Товар.АкцТвСт) И Не ЗначениеЗаполнено(Товар.АкцизТвБаза) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указана налоговая база акциза", ИмяСтраницы, "АкцизТвБаза", Товар.УИД));
	ИначеЕсли Не ЗначениеЗаполнено(Товар.АкцТвСт) И ЗначениеЗаполнено(Товар.АкцизТвБаза) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Налоговая база акциза не указывается", ИмяСтраницы, "АкцизТвБаза", Товар.УИД));
	КонецЕсли;
	Если ЗначениеЗаполнено(Товар.АкцАдвСт) И Не ЗначениеЗаполнено(Товар.АкцизАдвБаза) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указана налоговая база акциза", ИмяСтраницы, "АкцизАдвБаза", Товар.УИД));
	ИначеЕсли Не ЗначениеЗаполнено(Товар.АкцАдвСт) И ЗначениеЗаполнено(Товар.АкцизАдвБаза) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Налоговая база акциза не указывается", ИмяСтраницы, "АкцизАдвБаза", Товар.УИД));
	КонецЕсли;
	Если ЗначениеЗаполнено(Товар.АкцТвСт) И Не ЗначениеЗаполнено(Товар.СумАкцизТв) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указана сумма акциза", ИмяСтраницы, "СумАкцизТв", Товар.УИД));
	ИначеЕсли Не ЗначениеЗаполнено(Товар.АкцТвСт) И ЗначениеЗаполнено(Товар.СумАкцизТв) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Сумма акциза не указывается", ИмяСтраницы, "СумАкцизТв", Товар.УИД));
	КонецЕсли;
	Если ЗначениеЗаполнено(Товар.АкцАдвСт) И Не ЗначениеЗаполнено(Товар.СумАкцизАдв) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указана сумма акциза", ИмяСтраницы, "СумАкцизАдв", Товар.УИД));
	ИначеЕсли Не ЗначениеЗаполнено(Товар.АкцАдвСт) И ЗначениеЗаполнено(Товар.СумАкцизАдв) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Сумма акциза не указывается", ИмяСтраницы, "СумАкцизАдв", Товар.УИД));
	КонецЕсли;
	
	Если Товар.ОсобПризн = "0" Тогда
		Если Не ЗначениеЗаполнено(Товар.НДССт) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указана ставка НДС", ИмяСтраницы, "НДССт", Товар.УИД));
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Товар.НДСБаза) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указана база НДС", ИмяСтраницы, "НДСБаза", Товар.УИД));
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Товар.СумНДС) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указана сумма НДС", ИмяСтраницы, "СумНДС", Товар.УИД));
		КонецЕсли;
	Иначе
		Если ЗначениеЗаполнено(Товар.НДССт) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Ставка НДС не указывается", ИмяСтраницы, "НДССт", Товар.УИД));
		КонецЕсли;
		Если ЗначениеЗаполнено(Товар.НДСБаза) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"База НДС не указывается", ИмяСтраницы, "НДСБаза", Товар.УИД));
		КонецЕсли;
		Если ЗначениеЗаполнено(Товар.СумНДС) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Сумма НДС не указывается", ИмяСтраницы, "СумНДС", Товар.УИД));
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

Функция ПроверитьДокументСВыводомВТаблицу_Форма2026_1(Данные, УникальныйИдентификатор)
	ТаблицаОшибок = Новый СписокЗначений;
	УведомлениеОСпецрежимахНалогообложения.ПроверкаАктуальностиФормыПриВыгрузке(
		Данные.Объект.ИмяФормы, ТаблицаОшибок, ПолучитьТаблицуФорм());
	УведомлениеОСпецрежимахНалогообложения.ПроверкаДатВУведомлении(Данные, ТаблицаОшибок);
	УведомлениеОСпецрежимахНалогообложения.ПолнаяПроверкаЗаполненныхПоказателейНаСоотвествиеСписку(
		"СпискиВыбора2026_1", "СхемаВыгрузкиФорма2026_1",
		Данные.Объект.ИмяОтчета, ТаблицаОшибок, Данные);
	
	ЭтоЮЛ = РегламентированнаяОтчетностьПереопределяемый.ЭтоЮридическоеЛицо(Данные.Организация);
	Титульная = Данные.ДанныеУведомления.Титульная;
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаКодаНО(Титульная.КодНО, ТаблицаОшибок, "Титульная");
	Если Не ЗначениеЗаполнено(Титульная.ДАТА_ПОДПИСИ) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указана дата подписи", "Титульная", "ДАТА_ПОДПИСИ"));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Титульная.ОсПрЗаяв) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан особый признак заявителя", "Титульная", "ОсПрЗаяв"));
	КонецЕсли;
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИННКПП(ЭтоЮЛ, Титульная, ТаблицаОшибок);
	Если ЭтоЮЛ Тогда 
		Если Не ЗначениеЗаполнено(Титульная.НаимОрг) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указано наименование организации", "Титульная", "НаимОрг"));
		КонецЕсли;
	Иначе 
		Если Не ЗначениеЗаполнено(Титульная.Фамилия) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указана фамилия", "Титульная", "Фамилия"));
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Титульная.Имя) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указано имя", "Титульная", "Имя"));
		КонецЕсли;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Титульная.ПРИЗНАК_НП_ПОДВАЛ) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан признак подписанта", "Титульная", "ПРИЗНАК_НП_ПОДВАЛ"));
	КонецЕсли;
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаПодписантаНалоговойОтчетности(
		Данные, ТаблицаОшибок, "Титульная", ЭтоЮЛ Или Титульная.ПРИЗНАК_НП_ПОДВАЛ = "2");
	Если Титульная.ПРИЗНАК_НП_ПОДВАЛ = "2" И Не ЗначениеЗаполнено(Титульная.НаимДок) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан документ представителя", "Титульная", "НаимДок"));
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Титульная.НомКорр) Тогда 
		Если Не СтрПодобнаПоРегулярномуВыражению(Титульная.НомерДОПП,
				"(0[1-9]{1}|1[0-9]{1}|2[0-9]{1}|3[0-1])(0[1-9]|1[0-2])([0-9]{2})([0-9]{7})") Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан / неправильно указан номер корректируемого документа", "Титульная", "НомерДОПП"));
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Титульная.ДатаРегистр) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указана дата регистрации корректируемого документа", "Титульная", "ДатаРегистр"));
		КонецЕсли;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Титульная.Статус) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан статус", "Титульная", "Статус"));
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Титульная.НаимПрод) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указано наименование продавца", "Титульная", "НаимПрод"));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Титульная.РегНомПрод) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан регистрационный номер продавца", "Титульная", "РегНомПрод"));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Титульная.ОКСМПрод) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан код страны продавца", "Титульная", "ОКСМПрод"));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Титульная.АдресПрод) И Титульная.ОКСМПрод <> "643" Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан адрес продавца", "Титульная", "АдресПрод"));
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Титульная.НаимПерев) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указано наименование перевозчика", "Титульная", "НаимПерев"));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Титульная.РегНомПерев) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан регистрационный номер перевозчика", "Титульная", "РегНомПерев"));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Титульная.ОКСМПерев) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан код страны перевозчика", "Титульная", "ОКСМПерев"));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Титульная.ВидТС) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан вид транспорта", "Титульная", "ВидТС"));
	КонецЕсли;
	
	Если Титульная.ВидТС = "1" Тогда
		Если Не ЗначениеЗаполнено(Титульная.РегЗнакТС) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан регистрационный знак (номер)", "Титульная", "РегЗнакТС"));
		КонецЕсли;
	ИначеЕсли Титульная.ВидТС = "2" Тогда
		Если Не ЗначениеЗаполнено(Титульная.НомерНаклАвиа) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан номер грузовой авианакладной", "Титульная", "НомерНаклАвиа"));
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Титульная.ДатаНаклАвиа) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указана дата грузовой авианакладной", "Титульная", "ДатаНаклАвиа"));
		КонецЕсли;
	ИначеЕсли Титульная.ВидТС = "3" Тогда
		Если Не ЗначениеЗаполнено(Титульная.НомерНаклЖД) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан номер железнодорожной накладной", "Титульная", "НомерНаклЖД"));
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Титульная.ДатаНаклЖД) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указана дата железнодорожной накладной", "Титульная", "ДатаНаклЖД"));
		КонецЕсли;
	ИначеЕсли Титульная.ВидТС = "4" Тогда
		Если Не ЗначениеЗаполнено(Титульная.НомерКон) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан номер коносамента", "Титульная", "НомерКон"));
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Титульная.ДатаКон) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указана дата коносамента", "Титульная", "ДатаКон"));
		КонецЕсли;
	КонецЕсли;
	
	Для Каждого Элт1 Из Данные.ДанныеМногостраничныхРазделов.Договор Цикл 
		Договор = Элт1.Значение;
		
		Если Не ЗначениеЗаполнено(Договор.НомерДог) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан номер договора", "Договор", "НомерДог", Договор.УИД));
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Договор.ДатаДог) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указана дата договора", "Договор", "ДатаДог", Договор.УИД));
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Договор.ПрСпецТов) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан признак содержимого договора", "Договор", "ПрСпецТов", Договор.УИД));
		КонецЕсли;
		
		Если Договор.ПрСпецТов = "1" Тогда 
			Для Каждого Элт2 Из Данные.ДанныеМногостраничныхРазделов.Спецификация Цикл 
				Спецификация = Элт2.Значение;
				Если Спецификация.УИДРодителя <> Договор.УИД Тогда 
					Продолжить;
				КонецЕсли;
				Если Не ЗначениеЗаполнено(Спецификация.НомерСп) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указан номер спецификации", "Спецификация", "НомерСп", Спецификация.УИД));
				КонецЕсли;
				Если Не ЗначениеЗаполнено(Спецификация.ДатаСп) Тогда 
					ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
						"Не указана дата спецификации", "Спецификация", "ДатаСп", Спецификация.УИД));
				КонецЕсли;
				Для Каждого Элт3 Из Данные.ДанныеМногостраничныхРазделов.ТоварСпецификация Цикл 
					Товар = Элт3.Значение;
					Если Товар.УИДРодителя = Спецификация.УИД Тогда 
						ПроверкаТовар_Форма2026_1(ТаблицаОшибок, Товар, "ТоварСпецификация");
					КонецЕсли;
				КонецЦикла;
			КонецЦикла;
		ИначеЕсли Договор.ПрСпецТов = "2" Тогда 
			Для Каждого Элт2 Из Данные.ДанныеМногостраничныхРазделов.ТоварДоговор Цикл 
				Товар = Элт2.Значение;
				Если Товар.УИДРодителя = Договор.УИД Тогда 
					ПроверкаТовар_Форма2026_1(ТаблицаОшибок, Товар, "ТоварДоговор");
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
	Возврат ТаблицаОшибок;
КонецФункции

Функция ИдентификаторФайлаЭлектронногоПредставления(СведенияОтправки)
	Префикс = "NO_SPOTDOPP";
	Возврат Документы.УведомлениеОСпецрежимахНалогообложения.ИдентификаторФайлаЭлектронногоПредставления(Префикс, СведенияОтправки);
КонецФункции

Функция ОсновныеСведенияЭлектронногоПредставления(Объект, Данные)
	ОсновныеСведения = Документы.УведомлениеОСпецрежимахНалогообложения.НачальнаяИнициализацияОбщихРеквизитовВыгрузки(Объект);
	ИдентификаторФайла = ИдентификаторФайлаЭлектронногоПредставления(ОсновныеСведения);
	ОсновныеСведения.Вставить("ИдФайл", ИдентификаторФайла);
	ОсновныеСведения.Вставить("ВидТС", Данные.ДанныеУведомления.Титульная["ВидТС"]);
	ОсновныеСведения.Вставить("ОКСМПрод", Данные.ДанныеУведомления.Титульная["ОКСМПрод"]);
	ОсновныеСведения.Вставить("НомКорр", Данные.ДанныеУведомления.Титульная["НомКорр"]);
	
	СодерживоеДоговора = Новый Соответствие;
	Для Каждого Элт Из Данные.ДанныеМногостраничныхРазделов.Договор Цикл 
		СодерживоеДоговора.Вставить(Элт.Значение.УИД, Элт.Значение.ПрСпецТов);
	КонецЦикла;
	Для Каждого Элт Из Данные.ДанныеМногостраничныхРазделов.Спецификация Цикл 
		Элт.Значение.УИДРодителя = ?(СодерживоеДоговора[Элт.Значение.УИДРодителя] = "1", Элт.Значение.УИДРодителя, Неопределено);
	КонецЦикла;
	Для Каждого Элт Из Данные.ДанныеМногостраничныхРазделов.ТоварДоговор Цикл 
		Элт.Значение.УИДРодителя = ?(СодерживоеДоговора[Элт.Значение.УИДРодителя] = "2", Элт.Значение.УИДРодителя, Неопределено);
	КонецЦикла;
	
	НумерацияТоваров = Новый Соответствие;
	Для Каждого Стр Из СтрРазделить("ТоварДоговор,ТоварСпецификация", ",") Цикл 
		Для Каждого Элт Из Данные.ДанныеМногостраничныхРазделов[Стр] Цикл 
			НумерацияТоваров.Вставить(Элт.Значение.УИДРодителя, 0);
		КонецЦикла;
		Для Каждого Элт Из Данные.ДанныеМногостраничныхРазделов[Стр] Цикл 
			НумерацияТоваров[Элт.Значение.УИДРодителя] = НумерацияТоваров[Элт.Значение.УИДРодителя] + 1;
			Элт.Значение.Вставить("НомерТов", НумерацияТоваров[Элт.Значение.УИДРодителя]);
		КонецЦикла;
	КонецЦикла;
	
	Возврат ОсновныеСведения;
КонецФункции

Функция ЭлектронноеПредставление_Форма2026_1(Объект, УникальныйИдентификатор) 
	СведенияЭлектронногоПредставления = УведомлениеОСпецрежимахНалогообложения.СведенияЭлектронногоПредставления();
	ДанныеУведомления = УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект);
	Ошибки = ПроверитьДокументСВыводомВТаблицу_Форма2026_1(ДанныеУведомления, УникальныйИдентификатор);
	УведомлениеОСпецрежимахНалогообложения.СообщитьОшибкиПриПроверкеВыгрузки(Объект, Ошибки, ДанныеУведомления);
	
	ОсновныеСведения = ОсновныеСведенияЭлектронногоПредставления(Объект, ДанныеУведомления);
	СтруктураВыгрузки = Документы.УведомлениеОСпецрежимахНалогообложения.ИзвлечьСтруктуруXMLУведомления(Объект.ИмяОтчета, "СхемаВыгрузкиФорма2026_1");
	УведомлениеОСпецрежимахНалогообложения.ТиповоеЗаполнениеДанными(ДанныеУведомления, ОсновныеСведения, СтруктураВыгрузки);
	УведомлениеОСпецрежимахНалогообложения.ВыгрузитьДеревоВТаблицу(СтруктураВыгрузки, ОсновныеСведения, СведенияЭлектронногоПредставления);
	Возврат СведенияЭлектронногоПредставления;
КонецФункции

Функция СформироватьСписокЛистов_Форма2026_1(Объект) Экспорт
	Листы = Новый СписокЗначений;
	МакетУведомления = Отчеты[Объект.ИмяОтчета].ПолучитьМакет("Печать_Форма2026_1");
	Данные = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Объект.Ссылка, "ДанныеУведомления").Получить();
	
	ЭтоЮЛ = РегламентированнаяОтчетностьПереопределяемый.ЭтоЮридическоеЛицо(Объект.Организация);
	Титульная = Данные.ДанныеУведомления["Титульная"];
	
	ПечатнаяФорма = УведомлениеОСпецрежимахНалогообложения.НовыйПустойЛист();
	ОбластьШапка = МакетУведомления.ПолучитьОбласть("Шапка");
	ЗаполнитьЗначенияСвойств(ОбластьШапка.Параметры, Титульная);
	ОбластьШапка.Параметры.НаименованиеЗаявителя = ?(ЭтоЮЛ, Титульная.НаимОрг
		,СокрЛП(Титульная.Фамилия + " " + Титульная.Имя + " " + Титульная.Отчество));
	ОбластьШапка.Параметры.НомерПрицеп = Титульная.НомерПрицеп1 
		+ ?(ЗначениеЗаполнено(Титульная.НомерПрицеп1), ", " + Титульная.НомерПрицеп2, "");
	ПечатнаяФорма.Вывести(ОбластьШапка);
	УведомлениеОСпецрежимахНалогообложения.ПоложитьПФВСписокЛистов(Объект, Листы, ПечатнаяФорма, Листы.Количество() + 1, Ложь);
	
	ПечатнаяФорма = УведомлениеОСпецрежимахНалогообложения.НовыйПустойЛист();
	Лист2 = МакетУведомления.ПолучитьОбласть("Лист2");
	ЗаполнитьЗначенияСвойств(Лист2.Параметры, Титульная);
	ПечатнаяФорма.Вывести(Лист2);
	УведомлениеОСпецрежимахНалогообложения.ПоложитьПФВСписокЛистов(Объект, Листы, ПечатнаяФорма, Листы.Количество() + 1, Ложь);
	
	Для Каждого Элт1 Из Данные.ДанныеМногостраничныхРазделов.Договор Цикл 
		Таблица1 = Новый Массив;
		Таблица2 = Новый Массив;
		
		Договор = Элт1.Значение;
		ПечатнаяФорма = УведомлениеОСпецрежимахНалогообложения.НовыйПустойЛист();
		ПечатнаяФорма.ОриентацияСтраницы  = ОриентацияСтраницы.Ландшафт;
		ЗаголовокДоговор = МакетУведомления.ПолучитьОбласть("ЗаголовокДоговор");
		ЗаполнитьЗначенияСвойств(ЗаголовокДоговор.Параметры, Договор);
		НомПП = 0;
		Для Каждого Элт2 Из Данные.ДанныеМногостраничныхРазделов[?(Договор.ПрСпецТов = "1", "ТоварСпецификация", "ТоварДоговор")] Цикл 
			Товар = Элт2.Значение;
			Если Товар.УИДРодителя <> Договор.УИД Тогда 
				Продолжить;
			КонецЕсли;
			НомПП = НомПП + 1;
			Строка = МакетУведомления.ПолучитьОбласть("СтрокаТовары");
			ЗаполнитьЗначенияСвойств(Строка.Параметры, Товар);
			Строка.Параметры.НомПП = НомПП;
			Таблица1.Добавить(Строка);
			Строка = МакетУведомления.ПолучитьОбласть("СтрокаНалоги");
			ЗаполнитьЗначенияСвойств(Строка.Параметры, Товар);
			Строка.Параметры.НомПП = НомПП;
			Таблица2.Добавить(Строка);
			
			Если УведомлениеОСпецрежимахНалогообложения.БлокЗаполнен(Товар, "КодВалют,КурсВалют,БазаВалют") Тогда 
				ЗаполнитьЗначенияСвойств(ЗаголовокДоговор.Параметры, Товар);
			КонецЕсли;
		КонецЦикла;
		
		ПечатнаяФорма.Вывести(ЗаголовокДоговор);
		ПечатнаяФорма.Вывести(МакетУведомления.ПолучитьОбласть("ШапкаТовары"));
		Для Каждого Элт Из Таблица1 Цикл 
			ПечатнаяФорма.Вывести(Элт);
		КонецЦикла;
		
		ПечатнаяФорма.Вывести(МакетУведомления.ПолучитьОбласть("ШапкаНалоги"));
		Для Каждого Элт Из Таблица2 Цикл 
			ПечатнаяФорма.Вывести(Элт);
		КонецЦикла;
		УведомлениеОСпецрежимахНалогообложения.ПоложитьПФВСписокЛистов(Объект, Листы, ПечатнаяФорма, Листы.Количество() + 1, Ложь);
	КонецЦикла;
	
	Возврат Листы;
КонецФункции

#КонецОбласти

#КонецЕсли