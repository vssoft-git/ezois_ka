#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Пропускаем инициализацию, чтобы гарантировать получение формы при передаче параметра "АвтоТест".
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	Заголовок = Параметры.Заголовок;
	
	Элементы.ДекорацияТекстПодсказки.Заголовок = Параметры.ТекстПодсказки;
	Элементы.ДекорацияТекстПодсказки.Подсказка = Параметры.Комментарий;
	
	Элементы.ДекорацияНаименованиеРаздела.Заголовок = Параметры.НаименованиеРаздела;
	
	Если ЗначениеЗаполнено(Параметры.ОписаниеРаздела) Тогда
		Элементы.ДекорацияОписаниеРаздела.Заголовок = Параметры.ОписаниеРаздела;
	Иначе
		Элементы.ДекорацияОписаниеРаздела.Видимость = Ложь;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
