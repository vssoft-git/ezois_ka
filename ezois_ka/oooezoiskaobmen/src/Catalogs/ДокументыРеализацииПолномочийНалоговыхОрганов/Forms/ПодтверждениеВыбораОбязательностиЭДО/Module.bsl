
#Область ОписаниеПеременных

#КонецОбласти

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Обязателен  = Параметры.Обязателен;
	Организация = Параметры.Организация;
	Ссылка = Параметры.Ссылка;
	
	УправлениеФормой();

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ПодробнееОбработкаНавигационнойСсылки(Элемент, НавигационнаяСсылкаФорматированнойСтроки, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	ТребованияФНСКлиент.ПоказатьИнформациюоСроках(, Истина);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Сохранить(Команда)

	Результат = Новый Структура();
	Результат.Вставить("ЗапомнитьДляОрганизации", Запомнить);
	Результат.Вставить("Организация", Организация);
	Результат.Вставить("Обязателен", Обязателен);
	Результат.Вставить("Ссылка", Ссылка);
	
	Закрыть(Результат);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура УправлениеФормой()
	
	Если Обязателен Тогда
		Заголовок = НСтр("ru = 'Подтверждение приема отправлять не нужно'");
	Иначе
		Заголовок = НСтр("ru = 'Подтверждение приема отправлять нужно'");
	КонецЕсли;
	
	Элементы.РазъяснениеЭдоОбязателен.Видимость = Обязателен;
	Элементы.РазъяснениеЭдоНеОбязателен.Видимость = НЕ Обязателен;
	
	ТребованияФНС.ИзменитьОформлениеВопросаЗапомнитьПроЭДО(ЭтотОбъект, Организация, Обязателен);
	
КонецПроцедуры

#КонецОбласти

