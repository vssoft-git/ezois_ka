
#Область ОписаниеПеременных

#КонецОбласти

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	УправлениеФормой();
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ЧтоДелатьОбработкаНавигационнойСсылки(Элемент, НавигационнаяСсылкаФорматированнойСтроки, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	ОткрытьФорму("Справочник.ОписиИсходящихДокументовВНалоговыеОрганы.Форма.КакЗагрузитьДокументы1СЭДОВВидеXMLфайлов");
	
КонецПроцедуры

&НаКлиенте
Процедура ЭтоНеДокументы1СЭДОПриИзменении(Элемент)
	
	УправлениеФормой();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ЗагрузитьИзЭтойБазы(Команда)
	Закрыть("ЗагрузитьИзЭтойБазы");
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьИзДругойБазы(Команда)
	Закрыть("ЗагрузитьИзДругойБазы");
КонецПроцедуры

&НаКлиенте
Процедура КомандаПродолжить(Команда)
	Закрыть("Продолжить");
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура УправлениеФормой()
	
	Элементы.ФормаЗагрузитьИзЭтойБазы.Видимость = НЕ ЭтоНеДокументы1СЭДО;
	Элементы.ФормаЗагрузитьИзЭтойБазы.КнопкаПоУмолчанию = НЕ ЭтоНеДокументы1СЭДО;
	
	Элементы.ФормаЗагрузитьИзДругойБазы.Видимость = НЕ ЭтоНеДокументы1СЭДО;
	
	Элементы.ФормаКомандаПродолжить.Видимость = ЭтоНеДокументы1СЭДО;
	Элементы.ФормаКомандаПродолжить.КнопкаПоУмолчанию = ЭтоНеДокументы1СЭДО;
	
КонецПроцедуры



#КонецОбласти


