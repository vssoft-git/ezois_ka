// @strict-types

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ЗаголовокОбработаноВсего 	= Элементы.НадписьОбработаноВсего.Заголовок;
	ЗаголовокПровереноУспешно 	= Элементы.НадписьПровереноУспешно.Заголовок; 
	ЗаголовокПровереноСОшибками = Элементы.НадписьСОшибками.Заголовок;
	
	ЗаголовокОбработаноВсегоСклонение = ПолучитьСклоненияСтрокиПоЧислу("документ", Параметры.КоличествоДокументовВсего,,
		"ЧС=Количественное;Л=ru_RU", "ПД=Родительный")[0];
	
	Элементы.НадписьОбработаноВсего.Заголовок 	= СтрШаблон(ЗаголовокОбработаноВсего, ЗаголовокОбработаноВсегоСклонение);
	Элементы.НадписьПровереноУспешно.Заголовок 	= СтрШаблон(ЗаголовокПровереноУспешно, Параметры.КоличествоПровереныУспешно);
	Элементы.НадписьСОшибками.Заголовок 		= СтрШаблон(ЗаголовокПровереноСОшибками, Параметры.КоличествоСОшибками);
	
КонецПроцедуры

#КонецОбласти

