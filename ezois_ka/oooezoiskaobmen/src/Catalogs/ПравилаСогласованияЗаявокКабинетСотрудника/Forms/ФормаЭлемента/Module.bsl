#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Элементы.ФизическоеЛицо.Видимость = Не ЗначениеЗаполнено(Объект.Подразделение);
	Элементы.Подразделение.Видимость = Не ЗначениеЗаполнено(Объект.ФизическоеЛицо);
	Элементы.ШаблонДокумента.Видимость = ЗначениеЗаполнено(Объект.ШаблонДокумента);
	
КонецПроцедуры

#КонецОбласти