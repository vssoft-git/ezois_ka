#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиОбновления

Процедура ИзменитьТребуетЗаполнения(Параметры = Неопределено) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ТипыОперацийЕНП.Ссылка КАК Ссылка
		|ИЗ
		|	Справочник.ТипыОперацийЕНП КАК ТипыОперацийЕНП
		|ГДЕ
		|	ТипыОперацийЕНП.ТребуетЗаполнения";
	
	ТаблицаЗапроса = Запрос.Выполнить().Выгрузить();
	
	Для Каждого СтрокаЗапроса Из ТаблицаЗапроса Цикл
		ТипОперацийЕНП = СтрокаЗапроса.Ссылка.ПолучитьОбъект();
		ТипОперацийЕНП.ТребуетЗаполнения = Ложь;
		ТипОперацийЕНП.Записать();
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли