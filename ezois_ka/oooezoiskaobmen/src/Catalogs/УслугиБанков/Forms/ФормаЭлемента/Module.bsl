
#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	РассчитатьАктивность();
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура НачалоДействияПриИзменении(Элемент)
	РассчитатьАктивность();
КонецПроцедуры

&НаКлиенте
Процедура КонецДействияПриИзменении(Элемент)
	РассчитатьАктивность();
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура РассчитатьАктивность()
	
	ТекущаяДата = ОбщегоНазначенияКлиент.ДатаСеанса();
	Активна = (Не ЗначениеЗаполнено(Объект.НачалоДействия) ИЛИ Объект.НачалоДействия <= ТекущаяДата)
			И (Не ЗначениеЗаполнено(Объект.КонецДействия)  ИЛИ Объект.КонецДействия >= ТекущаяДата);
			
КонецПроцедуры

#КонецОбласти
