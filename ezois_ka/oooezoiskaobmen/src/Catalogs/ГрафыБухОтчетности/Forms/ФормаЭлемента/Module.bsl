
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	НеЗаполняется = Объект.НеЗаполняется;
	
	УправлениеВидимостьюЭлементовФормы();
	
	ДопАлгоритмы = Справочники.ГрафыБухОтчетности.ДополнительныеАлгоритмыГраф(
		ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Объект.Владелец,"ФормаОтчетности"));
	
	Для Каждого Алгоритм  Из ДопАлгоритмы Цикл
		Элементы.ДопАлгоритм.СписокВыбора.Добавить(Алгоритм.Ключ, Алгоритм.Значение);
	КонецЦикла;

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
Процедура НеЗаполняетсяПриИзменении(Элемент)
	
	Объект.НеЗаполняется = НеЗаполняется;
	Если НеЗаполняется Тогда
		Элементы.КодГода.Видимость = Ложь;
		Элементы.ДопАлгоритм.Видимость = Ложь;
	Иначе
		Элементы.КодГода.Видимость = Истина;
		Элементы.ДопАлгоритм.Видимость = Истина;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура УправлениеВидимостьюЭлементовФормы()
	
	Если НеЗаполняется Тогда
		Элементы.КодГода.Видимость = Ложь;
		Элементы.ВыводитьДанныеНаКонецГода.Видимость = Ложь;
		Элементы.ДопАлгоритм.Видимость = Ложь;
	КонецЕсли;

КонецПроцедуры

#КонецОбласти
