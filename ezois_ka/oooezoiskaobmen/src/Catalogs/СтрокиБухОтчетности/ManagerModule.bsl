#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Возвращает имена блокируемых реквизитов для механизма блокирования реквизитов БСП.
//
// Возвращаемое значение:
//	Массив - имена блокируемых реквизитов.
//
Функция ПолучитьБлокируемыеРеквизитыОбъекта() Экспорт
	
	Результат = Новый Массив;
	Результат.Добавить("КодСтроки");
	Результат.Добавить("КодСтрокиПрошлогоПериода");
	Результат.Добавить("КодЯчейки");
	Возврат Результат;
	
КонецФункции

// Возвращает коды строк дебиторской/кредиторской задолженности пояснений с особым расчетом.
//
// Возвращаемое значение:
//	Массив
//
Функция КодыСтрокДебиторскойКредиторскойЗадолженности() Экспорт
	
	КодыСтрок = Новый Массив;

	Строки = "5611, 5613, 5511, 5513";
	
	КодыСтрок = РегламентированнаяОтчетностьКлиентСервер.Список(Строки);
	
	Возврат КодыСтрок;
	
КонецФункции

#КонецОбласти

#КонецЕсли