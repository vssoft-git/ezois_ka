#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Если Не Отказ Тогда
		Если Не ЭтоГруппа Тогда
			//Ручной ввод доп. строки
			Если Не ЗначениеЗаполнено(КодЯчейки) И Не Регламентированная Тогда
				РеквизитыОсновнойСтроки = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ОсновнаяСтрока, "КодЯчейки, КодЯчейкиПрошлогоПериода");
				КодЯчейки = СтрЗаменить(РеквизитыОсновнойСтроки.КодЯчейки, "П000100", "П100100");
				КодЯчейкиПрошлогоПериода = СтрЗаменить(РеквизитыОсновнойСтроки.КодЯчейкиПрошлогоПериода, "П000100", "П100100");
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли