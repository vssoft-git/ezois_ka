
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	//++ НЕ УТ
	Если ЭтоУправлениеТорговлей() Тогда
	//-- НЕ УТ
		ВариантыОтчетовКлиент.ПоказатьПанельОтчетов("Планирование", ПараметрыВыполненияКоманды);
	//++ НЕ УТ
	Иначе
		ВариантыОтчетовКлиент.ПоказатьПанельОтчетов("БюджетированиеИПланирование", ПараметрыВыполненияКоманды);
	КонецЕсли;
	//-- НЕ УТ
КонецПроцедуры

//++ НЕ УТ

&НаСервере 
Функция ЭтоУправлениеТорговлей()
	Возврат ПолучитьФункциональнуюОпцию("УправлениеТорговлей");
КонецФункции
//-- НЕ УТ
