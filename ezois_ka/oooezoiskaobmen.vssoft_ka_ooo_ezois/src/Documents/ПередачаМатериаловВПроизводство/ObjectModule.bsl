
&Перед("ПередЗаписью")
Процедура vs_ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	// begin fix Designer27.10.2020 2:06:49
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Если ИТБК_ТехническийДокумент 
		и Не (ДополнительныеСвойства.Свойство("ИТБК_ЗаполнениеМатОтчета") 
			или РольДоступна("ПолныеПрава")) Тогда
		ОбщегоНазначения.СообщитьПользователю("Изменение технического документа запрещено!(Мат.Отчет)", Ссылка,,, Отказ);
	КонецЕсли;
	// end fix Designer27.10.2020 2:06:53
КонецПроцедуры
