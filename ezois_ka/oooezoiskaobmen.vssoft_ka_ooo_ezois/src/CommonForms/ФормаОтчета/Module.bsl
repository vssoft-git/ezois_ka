
&НаКлиенте
&ИзменениеИКонтроль("ОтчетСкомпоноватьРезультат")
Процедура vs_ОтчетСкомпоноватьРезультат(Команда)
	ОчиститьСообщения();
#Вставка
	Если Команда.Имя = "vs_ОтразитьМатериальныйОтчет" Тогда
		ЭтотОбъект.vs_ФормироватьДокументы = Истина;
	КонецЕсли;
#КонецВставки
	Сформировать();
	ПараметрыИзменены = Ложь;
КонецПроцедуры
