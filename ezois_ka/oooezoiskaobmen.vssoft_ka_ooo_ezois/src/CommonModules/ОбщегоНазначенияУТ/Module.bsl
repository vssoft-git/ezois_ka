
Функция vs_ПолучитьПредставлениеДоговора(Знач СчетДоговор = "", Знач Номер = "", Знач Дата = '00010101000000') Экспорт
	Слова = Новый Массив;
	Слова.Добавить(СокрЛП(СчетДоговор));
	Слова.Добавить("№");
	Если ЗначениеЗаполнено(Номер) Тогда
		Слова.Добавить(СокрЛП(Номер));
	КонецЕсли;
	Если ЗначениеЗаполнено(Дата) Тогда
		Слова.Добавить(СтрШаблон(НСтр("ru = 'от %1'"), Формат(Дата, "ДФ=dd.MM.yyyy")));
	КонецЕсли;
	
	Возврат СтрСоединить(Слова, " ");
КонецФункции
